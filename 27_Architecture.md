# CRDDアーキテクチャ工程

Version: v0.20.1
Status: Stable
Owner: Qual-Lab
Skill ID: `skill.architecture.integrate`
Last Updated: 2026-09-06
Related:
- [01_Principles.md](01_Principles.md)
- [02_Terminology.md](02_Terminology.md)
- [03_Documentation.md](03_Documentation.md)
- [10_Agent.md](10_Agent.md)
- [11_Skill.md](11_Skill.md)
- [16_Quality_Assurance.md](16_Quality_Assurance.md)
- [18_Context_Dependency.md](18_Context_Dependency.md)
- [24_UI_Behavior_Specification.md](24_UI_Behavior_Specification.md)
- [25_UI.md](25_UI.md)
- [26_Behavior_Specification.md](26_Behavior_Specification.md)
- [28_Implementation.md](28_Implementation.md)
- [29_Verification.md](29_Verification.md)
- [51_Document_Audit.md](51_Document_Audit.md)
- [52_Conformance_Audit.md](52_Conformance_Audit.md)
- [53_Gap_Impact_Audit.md](53_Gap_Impact_Audit.md)

---

> アーキテクチャを適用するプロジェクトでは、本書の`Phase Process Contract`をアーキテクチャ工程内の正本として使用する。

> この文書で分かること（非規範の案内）
>
> - 上流の要求・UX・UI・仕様を技術構造へどう変換するか
> - 境界、データ、インターフェース、品質、セキュリティをどう決めるか
> - 下流で判明した制約を上流へどう返すか
> - コーディング規則や実装制約をどこへ置くか
> - 実装へ渡す前に何を確認するか

<a id="1-purpose-and-boundary"></a>

# 1. 目的と適用範囲（Purpose and Boundary）

アーキテクチャは、承認済みプロダクトコンテキストを、現在の制約下で実現できるシステム境界、データ、インターフェース、セキュリティ、品質、操作、実装規則へ変換する工程である。

```text
振る舞い仕様 = 何が、どの条件で、どう振る舞うべきか
UI                     = 利用者がどう認識・操作・回復できるか
アーキテクチャ           = どの境界・構造・契約・制約で成立させるか
実装         = 承認済みアーキテクチャをコード等へどう具体化するか
検証           = 対象改訂版で実際に成立したか
```

アーキテクチャは要求、UX成果、UI表現、振る舞い仕様、コードの代替ではない。技術都合で上位意図や振る舞いを暗黙に弱めず、成立不能または新しい制約を発見した場合は代替案と影響を該当工程へ戻す。

アーキテクチャ成果物は現在の制約に対する選択結果であり、永続的な一般原則と同一視しない。

---

<a id="phase-process-contract"></a>

# 工程実行契約（Phase Process Contract）

本章はアーキテクチャ工程の入口、変換、責務網羅範囲、出口、工程ゲート、監査の正本である。本章の規範性と運用規模は[文書化](03_Documentation.md#48-normative-language)に従う。後続章は本契約を設計判断とスキル実行へ具体化し、独自の完了条件を持たない。

<a id="phase-entry-contract"></a>

## 工程入口契約（Phase Entry Contract）

アーキテクチャは、対象範囲について次を受け取る。

- 情報源となるREQ / UX / IAへのトレース
- 承認済みUI契約、視覚表現方針（Visual Direction）、適用するUIテーマ（UI Theme）/ UI部品（UI Component）/ UI設計パターン（UI Design Pattern）/ [外部視覚成果物（External Visual Artifact）](25_UI.md#external-visual-artifact)、振る舞い仕様と[対応レビュー](24_UI_Behavior_Specification.md#26-exit-and-pair-gate)結果
- [デザインシステム参照実装（Design System Reference）](25_UI.md#design-system-reference)の改訂版、対象プラットフォームへの変換要否
- 適用する品質懸念プロファイルと情報源／バージョン／対象範囲、品質、互換性、処理能力、セキュリティ、プライバシー、コンプライアンス、コストの条件
- 受入基準と検証義務
- 既存システムコンテキスト、データ、インターフェース、操作、デプロイ
- 技術、プラットフォーム、提供側、リソース等の既知制約
- 外部依存関係と利用側
- 網羅範囲の要約、未解決事項、人間レビュー結果
- UI／SPEC→アーキテクチャ工程移行レビュー結果、レビュー済みのUI／SPEC改訂版、または明示された`review_exception`
- UI／SPEC／対で発火した変更影響の伝播確認結果、情報源の改訂版、または明示された`propagation_exception`

部分引き渡しの場合は、承認された対象範囲、未決事項、暫定制約、リスク、後続担当責任者、人間承認も必要である。上位コンテキストが矛盾する場合はアーキテクチャで都合よく解釈せず、該当決定権限へ戻す。

<a id="transformation-contract"></a>

## 変換契約（Transformation Contract）

プロダクトコンテキストを、実装と検証に使えるアーキテクチャコンテキストへ変換する。必要に応じて次を定義する。

- システム／ドメイン／実行時部品境界
- データ / インターフェース契約と正本
- 内部状態／連番
- セキュリティ / プライバシーと品質属性
- 外部統合と互換性 / 移行
- 処理能力 / インフラストラクチャと並行処理 / 回復力
- 可観測性と操作
- 実装規則
- 適用対象のUIテーマ／デザイントークン（Design Token）／UI部品／論理画面の視覚表現成立方式

アーキテクチャは観測可能な振る舞いを新しく決めない。技術的制約から振る舞い変更が必要な場合は、振る舞い仕様またはUIへフィードバックし、人間判断後の契約を設計へ反映する。

性能、可観測性、障害回復、セキュリティ、移行、テスト容易性について検証義務、観測要件、必要なログ／メトリクス／制御点、環境条件を追加・更新し、[品質保証](16_Quality_Assurance.md)の検証設計へ接続する。単体試験の分岐網羅率を測定するためにアーキテクチャ上のビルド、生成、実行境界または計測方式へ条件がある場合は、その条件と正当な除外候補を実装へ渡す。

<a id="required-responsibility-coverage"></a>

## 必要な責務の網羅（Required Responsibility Coverage）

対象範囲全体について、次の責務を適用範囲で判定する。

| 責務 | アーキテクチャで明らかにすること |
|---|---|
| システムコンテキスト | アクター、外部システム、信頼境界、主要データフロー |
| ドメイン／実行時部品境界 | 責務、決定権限、依存方向、交換境界 |
| データ | モデル、所有責任、正本、状態遷移、整合性、トランザクション境界 |
| インターフェース / 統合 | API / IPC / イベント、同期性、エラー、バージョン、利用先／提供元の境界 |
| 内部状態／連番 | 処理順序、永続内部状態、一時内部状態、副作用、調整 |
| セキュリティ / プライバシー / コンプライアンス | 認証、実行許可、テナント、シークレット、データフロー、保持期間、監査 |
| 品質 / 操作 | 可用性、遅延、処理量、回復、保守性、コスト、運用責任 |
| 並行処理 / 回復力 | 冪等性、競合、タイムアウト、再試行、背圧制御、部分失敗、停止 |
| 互換性 / 移行 | 利用側、スキーマ、バージョン、移行期間、ロールバック、廃止順序 |
| 処理能力 / インフラストラクチャ | 負荷前提、リソース、割当上限、スケーリング、リージョン、デプロイ、失敗領域 |
| 技術構成 | プロダクト設定との境界、保存・配布・優先順位の成立方式、環境、シークレット参照、提供側パラメータ、リソース規模、既定値 / 上書きの決定権限 |
| UI／視覚表現の成立 | 適用対象の意味／部品層のデザイントークン、UIテーマ差分とモード、UI部品と実行時部品、論理画面／経路／画面領域、UI素材／モーション、外部視覚成果物の改訂版とビルド改訂版の対応、デザインシステム参照実装の改訂版とビルド改訂版の対応・対象プラットフォームへの変換方式 |
| 可観測性 | 指標、ログ、トレース、警告、監査、運用上の状態判定 |
| 実装規則 | コーディング / 依存関係 / 境界規則、禁止事項、実行可能な検査への接続 |
| 検証有効化支援 | 性能、可観測性、障害回復、セキュリティ、移行、テスト容易性の検証義務、必要なログ／メトリクス／制御点、環境条件、単体試験の分岐網羅率を測定するための条件、負荷 / 失敗 / 移行の検証観点 |
| トレース / 判断理由 | 情報源コンテキスト、判断、代替案、制約、根拠、未解決事項 |

複数の実行時部品、外部境界、非自明な状態、Authority、共有資源、取消またはRecoveryを含む対象では、文章だけを構造成立の根拠にしない。対象のアーキテクチャ正本に、適用される次の構造表現を置く。

- **全体／内部ブロックのテキスト図**: 主要な実行時部品または責務の塊、外部境界、依存方向、入力／出力、Authorityおよび状態／資源の流れを示す。file単位の一覧や物理directory treeだけで代替しない。
- **状態遷移表**: 各状態について、契機、事前条件、処置、Effect、次状態、失敗時の状態、cleanup／Recoveryおよび終了後観測を、対象に実在する範囲で示す。
- **状態遷移のテキスト図**: 状態遷移表から主要状態、許可された遷移、分岐、禁止遷移、Recovery再入場および終了点を視覚投影する。図だけに状態、Guardまたは処置を追加せず、厳密な遷移条件は状態遷移表が所有する。
- **ブロック間シーケンスのテキスト図**: 順序に意味がある呼出し、CanonicalなIdentity／Authority／状態の受渡し、応答、取消、逆順cleanupおよびRecovery再入場を時間順に示す。責務または状態定義を複製せず、ブロック表と状態遷移表を参照する。
- **クラス／型関係のテキスト図**: 非自明なDomain型、公開型、Port、Interfaceおよび実装の継承、実装、所有、多重度と依存関係を示す。全fileまたは全classの一覧にせず、交換可能性と不変条件の理解に必要な型だけを対象にする。
- **データフロー図（DFD）**: Actor、処理、Data Store、Trust／Process／Repository／Network境界の間で、どの分類のデータが生成、変換、保存または公開されるかを示す。制御順序や状態遷移をDFDへ重ねない。

図の種類ごとの記法は次を正本とする。これは見た目を一行の機械記法へ限定する規則ではない。枠線、上下左右の配置、余白、字下げおよび日本語の短い表示名を使い、視線の流れとまとまりを理解しやすくしてよい。同じ意味記号を複数行の箱または縦方向の矢印へ展開しても、接続元、接続先、方向およびlabelを一意に復元できなければならない。個別成果物の凡例は参加者名と対象固有のPayload名だけを補足し、矢印や囲みの意味を再定義しない。新しい記号が必要な場合は、既存記法で表せない意味を確認してから本表を更新する。

| 図の種類 | 記法 | 意味 |
|---|---|---|
| 全体／内部ブロック図 | `[名前]` | CRDDが責務とlifecycleを所有する内部ブロック |
| 全体／内部ブロック図 | `<<名前>>` | CRDDの直接所有外にあるActor、System、OSまたは外部境界 |
| 全体／内部ブロック図 | `[A] -- label --> [B]` | AからBへの許可された依存、呼出しまたは流れ。状態、Authority、Identityまたは資源を運ぶ場合は`label`を省略しない |
| 全体／内部ブロック図 | `┌─┐`、`│ │`、`└─┘` | `[名前]`または`<<名前>>`を複数行の視覚的な箱へ展開する枠線。箱の中に内部／外部の区別を明記する |
| 全体／内部ブロック図 | `│`、`▼`、`├`、`└` | 縦方向の接続、方向または分岐を読みやすく配置する補助線。接続の意味を示すlabelを近接して置く |
| 全体／内部ブロック図 | `==== 境界名 ====` | Trust、Process、Repository、Networkその他の意味上の境界 |
| 状態遷移表 | 一行一遷移 | `現在状態／契機／事前条件／処置／Effect／次状態／失敗時／cleanup・Recovery／終了後観測`を基本列とし、非該当は理由付きで示す |
| 状態遷移図 | `[S1: 状態名]` | 一つの状態。IDは図内で一意とし、表示名は状態遷移表の状態名と一致させる |
| 状態遷移図 | `[開始] --> [S1: 状態名]` | lifecycleの開始点と最初の状態。開始前のAuthorityまたはEffect非発行が重要な場合はlabelへ示す |
| 状態遷移図 | `[S1] -- 契機 [Guard] / 処置 --> [S2]` | 許可された遷移。契機、Guardおよび主要処置をこの順で示し、詳細は対応する状態遷移表の行が所有する |
| 状態遷移図 | `[S1] -x 契機 [Guard不成立]` | 明示的に禁止または拒否する遷移。矢印を別状態へ到達させず、Effect 0等の結果はlabelへ示す |
| 状態遷移図 | `[S1] -- 終了条件 --> [終了: completed]` | lifecycleの終了点。`completed`、`cancelled`、`blocked`等、実際に区別する終了状態を省略しない |
| 状態遷移図 | `┌─ 分岐条件 ─┐`と`├`／`└` | 同じ起点からのGuard分岐を視覚的にまとめる補助表現。各枝の接続先と条件を個別に示す |
| ブロック間シーケンス図 | `A -> B : label` | AがBへ要求、commandまたはeventを発行する。発行だけでは受理、完了またはEffect成立を意味しない |
| ブロック間シーケンス図 | `B --> A : label` | BがAへ結果または観測を返す。`label`に明記しない限りsettlementを意味しない |
| ブロック間シーケンス図 | `A -x B : label` | 条件不成立によりBへの呼出し、Authority発行またはEffectがないことを示す |
| ブロック間シーケンス図 | 参加者名の下の`│` | 参加者のlifecycleを縦に追うライフライン展開形。一行記法と同じ意味を、時間を上から下へ配置して表示する |
| ブロック間シーケンス図 | `│-- label -->│`／`│<-- label --│` | 左右に並べた参加者間の要求／結果。一行記法の`->`／`-->`と同じ意味であり、接続先のライフラインまで線を届かせる |
| ブロック間シーケンス図 | `│-x label   │` | 呼出し、Authority発行またはEffectがないこと。一行記法の`-x`と同じ意味であり、線を接続先へ到達させない |
| ブロック間シーケンス図 | `[condition]` | 直後の一行または字下げした範囲に適用するGuard。範囲を曖昧にしない |
| ブロック間シーケンス図 | `{Canonical Name}` | Consumerが再解釈せずexactに保持するIdentity、Authority、StateまたはPayload |
| ブロック間シーケンス図 | `cleanup: C -> B -> A` | 正常または異常終了時の回収順。要求順と異なる場合は必ず明示する |
| クラス／型関係図 | `class Name { +field: Type; +operation(): Result }` | 型と公開member。`+`はpublic、`-`はprivate、`#`はprotected。可視性を判定しない概念型では記号を省略する |
| クラス／型関係図 | `interface Name { operation(): Result }` | Portまたは交換可能な契約。言語上の`interface`でない場合は`type`等の実際の種別を使う |
| クラス／型関係図 | `Base <|-- Derived`／`Contract <|.. Implementation` | 左側の継承元を右側が継承する／左側の契約を右側が実装する |
| クラス／型関係図 | `Owner *-- Part`／`Whole o-- Part` | 左側が右側のlifecycleを所有するcomposition／所有しないaggregation |
| クラス／型関係図 | `A --> B : role [多重度]` | AからBへの参照または依存。多重度は`1`、`0..1`、`0..*`、`1..*`の閉集合で示す |
| クラス／型関係図 | 複数行の`┌─ class／interface ─┐` | memberを縦に並べる視覚表現。関係矢印は箱の外側へ接続し、矢印終端と多重度を省略しない |
| DFD | `<<E1: Actor／System>>` | CRDDのData Processing Boundary外にあるデータ源または利用先 |
| DFD | `(P1: 処理名)` | 入力データを検証、変換、決定または公開する処理。番号は図内で一意にする |
| DFD | `[(D1: Store名)]` | Data Storeまたは耐久記録。単なる一時変数や呼出しstackをStoreにしない |
| DFD | `A -- {分類} データ名 --> B` | AからBへ移動するデータ。データ名と情報分類を省略せず、commandや制御順序を描かない |
| DFD | `==== 境界名 ====` | Dataが越えるTrust、Process、RepositoryまたはNetwork境界。横断するflowを識別可能にする |

矢印の方向だけから同期／非同期、成功、Authority移譲、Effect成立またはcleanup完了を推定しない。これらが必要な場合は`label`または対応する状態遷移表へ明示する。省略記号、色、位置、線種または字下げだけへ安全上重要な意味を持たせない。ただし、位置、枠線、余白および字下げを視覚的な理解のために使うことは推奨する。規則が固定するのは意味の復元可能性であり、読みやすいレイアウトを禁止しない。

図表の種類ごとの視覚構成は次を基本とする。機械可読な一行記法は意味を確定するための最小形であり、人間向け成果物をその最小形だけで並べない。

| 図表 | 視覚構成の規則 |
|---|---|
| 全体／内部ブロック図 | 責務または境界ごとに箱をまとめ、主要な視線方向を上から下または左から右のどちらかへ揃える。外部境界、主要経路、失敗／Recovery経路を余白と分岐で区別し、交差線を最小化する |
| 状態遷移表 | 正常、待機／取消、失敗／Recovery等の意味群で行を並べ、状態名と次状態を視線で追える順序にする。各cellを長い説明文へせず、補足理由は表の直後へ分離する |
| 状態遷移図 | 通常経路を上から下または左から右へ揃え、待機、取消、失敗およびRecoveryを主要経路から分岐させる。各状態を箱で囲み、終了状態を枝ごとに明示する。線が交差する場合は図を責務またはlifecycle単位へ分割する |
| ブロック間シーケンス図 | 参加者を横に並べ、時間を上から下へ統一する。横幅や参加者数により追跡が難しい場合は、同じIdentityを示して意味の連続する複数panelへ分ける。正常経路と条件分岐、取消、逆順cleanupの間に余白を置く |
| クラス／型関係図 | 型を複数行の箱で示し、交換契約、実装、所有および主要な多重度が一望できる配置にする。memberを網羅列挙せず、関係線を箱の外へ置き、交差を最小化する |
| DFD | Actor、処理、Storeを形で区別し、主要なデータ移動を上から下または左から右へ揃える。Trust／Process／Repository／Network境界をflowが横断する位置へ置き、flow名と情報分類を線の近くへ示す。大きい対象は全体図と詳細図へ分け、単一の巨大図にしない |

```text
公開入口
   ↓
[入力・Authority境界]
   ↓
[主要実行ブロック] ──失敗／取消──> [cleanup・Recoveryブロック]
   ↓                                      │
[永続化・観測ブロック] <──再入場──────────┘
   ↓
公開結果・終了後状態
```

```text
[開始]
   │ 受付成功／Effect 0
   ▼
[S1: ready] -- 実行要求 [Authority有効] / Effect開始 --> [S2: running]
   │                                                       │
   │-x 実行要求 [Authority不明] / Effect 0                 ├─ 完了観測 --> [終了: completed]
   │                                                       └─ 取消要求 --> [S3: cleaning]
   │                                                                           │
   └──────────────── Recovery再入場 [Exact Identity] <─────────────────────────┤
                                                                               └─ cleanup確認 --> [終了: cancelled]
```

```text
┌─ interface ExecutionPort ─────────────┐
│ +execute(request): ExecutionResult     │
└────────────────────────────────────────┘
                  <|..
                    │ implements
┌─ class CoordinatorExecutionAdapter ───┐
│ -runtime: CoordinatorRuntime           │
│ +execute(request): ExecutionResult     │
└────────────────────────────────────────┘
                    │ uses [1]
                    ▼
        ┌─ class CoordinatorRuntime ─┐
        └────────────────────────────┘
```

```text
<<E1: 利用者／外部Client>>
          │ {internal} Objective Request
          ▼
==== Transport／Process境界 ==================
     (P1: 入力検証・Project実行)
          │ {internal} Canonical Project State
          ├──────────────────────────> [(D1: Project State Store)]
          │ {authorized-result} Public Projection
          ▼
<<E2: 許可された結果利用者>>
```

これらは説明用の装飾ではなく、実装所有者、結合単位および検証項目を導出する設計入力である。二重の正本を避けるため、ブロック表は責務と境界、クラス／型関係図は型の静的関係と交換契約、DFDはデータの生成・変換・保存・境界横断、状態遷移表はブロック内部の状態、条件、処置および終了後観測、状態遷移図は同表の主要経路を読み取るための視覚投影、シーケンス図はブロック間の時間順と受渡し、試験カタログは検証項目との対応だけを所有する。状態、遷移、境界および所有者を相互に対応させ、図には存在するが遷移表または実装へ接続しない経路、遷移表には存在するが図または結合試験へ接続しない経路を残さない。図と表が不一致の場合は図から条件を推定せず、固定候補前の設計不整合として停止する。

隣接ブロックの責務を切り分ける結合では一段、Identity、Authority、状態、Effect、cleanupまたはRecoveryが伝播する場合は一つ先を含む二段までを段階的な結合単位とする。三段を越えて公開機能全体を通る確認は総合試験またはE2Eとして扱う。結合順は、下位境界から原因を局所化する必要がある場合はボトムアップ、公開契約から未接続経路を発見する必要がある場合はトップダウンを選び、選択理由を検証設計へ接続する。単一の局所変換等で順序やlifecycle伝播を持たない対象だけ、理由付き非該当とできる。Mermaid等の特定rendererだけで読める図を唯一の正本にせず、Repository上のplain textで主要構造を復元可能にする。

すべてを全対象範囲へ機械的に記載する必要はない。状態遷移図は複数の正常・待機・取消・失敗・Recovery経路があり、表だけでは主要lifecycleを一望しにくい場合に適用する。クラス／型関係図は複数の型、Portまたは実装の関係が交換可能性や不変条件へ影響する場合、DFDはデータが外部Actor、Trust／Process／Repository／Network境界または耐久Storeをまたぐ場合に適用する。適用しない責務は`Not Applicable`として理由と人間確認を残す。単一図、主要正常パス、技術選定、プロトタイプの完成だけでアーキテクチャ完了としない。

Trust、Authority、Recoveryまたは安全上重要な結果を層間で運ぶAPI、IPC、callback、event、return値、fileまたは永続記録では、producer、搬送、consumerを抽象名だけで示さない。

実際のproducer variantとexact shape、順序、変換、対象範囲で把握できるproduction consumer、外部公開契約、拒否・判定不能時の処置を設計境界として識別可能にする。

耐久intent、receipt、pointer、recordは、別の呼出しまたはprocessで保護対象Effect／Recoveryを許可する十分な根拠または不可欠なAuthority predicateとして受理される場合だけAuthorityを担う。freshな現行Authorityへ再結合しなければ使えない通常のqueue、progress、checkpointまたはEvidenceは非該当とし、分類不能ならEffect 0で停止する。

発行条件成立前の失敗では非発行、exact intent発行後の失敗では同じintentのRecovery保持、retryでは別・拡大Authorityの非生成、置換・partial・unknownでは上書きしない停止を設計する。

単純な局所関数等、これらの境界が存在しない対象へ架空のproducer／Authority契約を作らない。

<a id="scope-and-coverage-state"></a>

## 対象範囲と網羅状態（Scope and Coverage State）

各上位コンテキストと各アーキテクチャ責務を、`Complete for Scope`、`Partial — Human Authorized`、`Blocked`、`Not Started`、`Not Applicable`で追跡する。

対象範囲は機能名だけでなく、アクター、データ、インターフェース、環境、構成差分、移行対象、既存利用側を含めて特定する。網羅範囲の要約は「設計文書がある」ではなく、必要な責務が判定済みかを示す。

<a id="human-decisions"></a>

## 人間による判断（Human Decisions）

各項目の人間の決定権限者は、システム／信頼境界、技術 / 提供側選択、データ所有関係、品質 / コストトレードオフ、セキュリティ / プライバシーリスク、互換性破壊、移行 / ロールバック方針、重大依存関係、`Not Applicable`、部分引き渡しを決定する。

AIは候補比較、不足、影響、設計案を提示できるが、上位契約変更、リスク受容、決定権限、重大トレードオフを自己承認しない。

アーキテクチャで対象項目の人間の決定権限者による判断、制約、学び、根拠、指摘事項を確定または変更した時点で、[変更影響の伝播確認](53_Gap_Impact_Audit.md#43-mandatory-propagation-trigger-and-closure)を実行する。

課題探索・要求形成、UX、IA、UI、SPECにある次の項目へ答える、または影響する候補を探索する。

- 未決事項と未解決事項
- 仮定と判断
- 制約

見つかった結果は、それぞれの正本へ反映する。上流の正本を更新した場合は、更新後改訂版からアーキテクチャ以降への影響を再探索・再監査するまで通常完了としない。

<a id="exit-and-handoff"></a>

## 完了条件と引渡し（Exit and Handoff）

通常引き渡し候補を人間のゲートへ提示する前に、次を行う。

1. 対象範囲／改訂版へ、契約確認と本工程の「必要な責務の網羅」「工程監査チェックリスト」に対する[専門品質確認を含む工程移行レビュー](10_Agent.md#72-phase-transition-review-and-remediation-loop)を実行する。
2. 移行に影響する指摘事項をアーキテクチャまたは責務を持つ工程で修正する。
3. 修正後改訂版を再レビューし、`Pass`を得る。
4. 対象内容と工程移行の人間の決定権限者が、内容とレビュー結果を確認して移行を決定する。

レビューの省略または未解消の指摘事項を伴う移行は、[人間が指示するレビュー例外](10_Agent.md#73-human-directed-review-exception)がある場合だけ通常経路と区別して扱う。

通常の実装への引き渡しは、対象範囲が`Complete for Scope`で、対象内容と実装への移行を人間の決定権限者が承認し、[`28_Implementation.md`](28_Implementation.md#phase-entry-contract)の受信条件を満たす場合に限る。

引き渡しでは、次を渡す。

- 承認済み対象範囲と情報源となるUI／SPEC
- アーキテクチャ境界
- データ / インターフェース / セキュリティ契約
- 互換性 / 移行と処理能力 / 操作
- 技術構成と実装規則
- 禁止事項と既知制約
- 受入基準と検証義務
- 網羅状態と未解決事項

部分引き渡しには、対象範囲、未定義設計、暫定制約、リスク、受信先、後続担当責任者の人間承認を必要とする。アーキテクチャ不成立をコードで暗黙解決させない。

<a id="phase-gate-criteria"></a>

## 工程移行の判定基準（Phase Gate Criteria）

- 情報源となるREQ / UX / IA / UI / SPECへトレースできる
- 境界、決定権限、データ所有関係、正本、インターフェースが判定済みである
- 内部状態／連番、失敗、並行処理、回復が説明・検証可能である
- 重要な状態、遷移、資源、ロック、AuthorityおよびEffectについて、所有者、開始／終了条件、正常・準正常・異常の経路、終了後に観測する条件を実装と検証へ渡せる
- 安全上重要な層間搬送では、実producer variant、exact shape、搬送順序、対象範囲で把握できるproduction consumerおよび外部公開契約を実装と検証へ渡せる
- セキュリティ / プライバシー / コンプライアンス、品質、操作、可観測性を適用範囲で定義している
- 適用する品質上の懸念をアーキテクチャ上の品質属性、成立方式、検証義務へ処置している
- 互換性 / 移行、処理能力 / インフラストラクチャ、依存関係を適用範囲で定義している
- プロダクト設定と技術構成を区別し、保存・配布・優先順位、環境差分、シークレット参照、提供側パラメータ等を適用範囲で定義している
- UIがUIテーマ、共通UI部品／UI設計パターン、論理画面の視覚表現成立方式を必要とする場合は、デザイントークン／モード、実行時部品、経路／画面領域、UI素材、改訂版の対応、代替動作を適用範囲で定義している
- 視覚的な論理画面を持つ対象範囲では、デザインシステム参照実装の改訂版、対象プラットフォームへの変換方式を定義している
- 実装規則、禁止事項、検証義務が実装前に明確である
- 単体試験の分岐網羅率測定へ影響するアーキテクチャ条件または除外候補がある場合、対象と理由を実装および検証設計へ渡している
- アーキテクチャが所有する検証義務、観測要件、必要なログ／メトリクス／制御点、環境条件を検証設計へ接続し、該当するアーキテクチャ／運用の専門観点でレビューしている
- 判断 / 判断理由、網羅範囲の不足、部分引き渡し承認が記録されている
- 発火した変更影響の伝播確認が`Pass`であり、必要な上流・同層正本更新と下流再探索・再監査が完了している
- 対象改訂版の工程移行レビューが、適用される専門観点をすべて評価したうえで`Pass`であり、移行に影響する指摘事項の是正と再レビューが完了している

<a id="phase-audit-checklist"></a>

## 工程監査チェックリスト（Phase Audit Checklist）

- 上位コンテキストにトレースしないアーキテクチャ判断
- SPECが所有する振る舞いやUIが所有する表現のアーキテクチャ内での再定義
- 境界、決定権限、データ所有関係、正本の重複または競合
- 失敗、並行処理、セキュリティ、プライバシー、移行、処理能力、依存関係、操作、可観測性、技術構成の適用判定漏れ
- プロダクト設定と技術構成の混同、既定値 / 上書きの決定権限または環境差分の未定義
- UIのUIテーマ／UI部品／論理画面契約をアーキテクチャが再定義している、またはデザイントークン／モード／実行時部品／UI素材／改訂版の対応が未定義で実装へ暗黙移管されている
- デザインシステム参照実装の改訂版・対象プラットフォームへの変換方式が未定義で実装へ暗黙移管されている
- アーキテクチャ設計パターン、技術、提供側を、理由のない一律規則として強制している状態
- 図やコード例だけで契約、制約、判断理由がない状態
- テストケースをアーキテクチャが所有、またはテスト可能性 / 検証義務を未定義にしている状態
- 重要な状態、遷移、資源、ロック、AuthorityまたはEffectの設計要素が、実装へ渡す識別可能な契約、検証義務または終了後観測のいずれにも接続されず、主要正常パスの説明だけで網羅済みとしている状態
- 分岐網羅率の測定に必要なビルド／生成／実行境界を未定義にしている、またはアーキテクチャ上の理由なく測定対象から除外していないか
- アーキテクチャの検証義務、観測要件、ログ／メトリクス／制御点または環境条件が検証設計へ未接続、もしくは該当する専門観点で未評価でないか
- 網羅範囲の要約、未解決事項、人間レビュー、実装規則の欠落
- アーキテクチャ判断、制約、学び、根拠、指摘事項に対応する上流未決事項 / 不足探索、正本反映、下流再探索、再監査の欠落
- 独立レビューまたは必要な専門観点の確認が未実施でないか。旧改訂版のレビュー流用、指摘事項未修正の持ち越し、監査実行完了を対象の合格とみなしていないか

---

# 2. アーキテクチャモデル

## 2.1. 境界・決定権限・依存関係

システムコンテキストでは、システム、アクター、外部システム、信頼境界、主要なデータフローを示す。ドメイン部品／実行時部品ごとに責務、決定権限、依存方向、公開インターフェースを定義し、同じ責務を複数の実行時部品が暗黙に所有しないようにする。

外部サービス、提供側、プラットフォーム固有処理は、交換・隔離・テストの必要性がある場合に接続部／提供側境界へ閉じ込める。すべての依存関係へ形式的な抽象化を追加せず、交換可能性、リスク、テスト可能性、提供側固有制約から判断する。

別のRepositoryまたは共通基盤との関係では、意味、原則、規則等を参照する[コンテキスト依存](18_Context_Dependency.md)と、パッケージ、実行時部品、スキーマ等を使用する成果物依存を分ける。アーキテクチャは通常・推移依存を含む成果物依存の実現方式、実行時境界、失敗・回復、セキュリティ、運用を所有する。採用プロジェクトまたは組織の管理範囲で、同じ成果物の意味、契約、採用版または更新判断を、決定権限またはリリース単位が異なる独立管理利用側の間で共有し横断調整する必要がある成果物依存、または重大リスクがある成果物依存は、同書の管理対象へ昇格する。外部提供元とのAPI契約、決定権限の違いまたは独立リリースだけでは昇格させない。参照元コンテキストの意味、版、上書きの決定権限を技術依存へ置き換えない。

```text
外部利用側 -> インターフェース境界 -> ドメイン責務 -> データ決定権限
外部提供側 <- 接続部境界          <- ドメイン責務
```

## 2.2. データ・正本・状態遷移

データごとに担当責任者、正式な情報源、書き込み側、読み取り側、分類、保持期間、削除、整合性、トランザクション境界を定義する。複数システムを統合する場合、一つのシステム全体を常に正本とせず、意味のある項目または集約単位単位で正本を決める。

複製、キャッシュ、読み取りモデル、派生データ、AIによる推論を正本データと同一視しない。鮮度、古さ、競合、修正、再構築の扱いを定義する。

UIに表示する更新通知、差し替え、鮮度表現はUI / SPECが正本である。アーキテクチャはスナップショット / バージョン、通知経路、整合性、再取得、競合検出を成立させる。

## 2.3. インターフェースと統合契約

各API、IPC、イベント、ファイル、バッチ、外部接続について、利用側 / 提供側、データ契約、決定権限、同期性、提供、順序保証、エラー、タイムアウト、再試行、冪等性、バージョン、可観測性を適用範囲で定義する。

```text
要求・応答 = 呼び出し側が結果または受付結果を待つ
一方向メッセージ  = 呼び出し側が同期結果を待たずに送る
プッシュ / イベント     = 提供側がイベントまたは更新を通知する
一括処理 / ファイル  = 集合または時点単位で受け渡す
```

この分類は実装方式を固定するためではなく、所有責任、失敗、提供保証、利用側の振る舞いを曖昧にしないために使う。

複数の下位操作、保証または利用側から構成され、部分成立を全体完成へ誤認し得る上位のインターフェース、Capabilityまたは実行境界では、利用可能になるために必要な保証を漏れなく列挙した集合を定義する。一部の操作、Adapter、分岐または単体試験が存在しても、必要な保証の一つでも未接続、未観測または判定不能なら、上位境界全体を利用可能、実装済みまたは検証済みとして公開しない。部分実装は、接続済みの操作と未成立の保証を分けて候補または部分接続として示し、利用側が完成状態へ読み替えられない停止または代替経路へ接続する。

上位Capabilityの完成を主張しない内部実装、一意な局所変換、単一結果または互いに独立した説明項目には、この上位完成判定を発火させない。対象が上位境界か、完成に必要な保証の所有者または利用側を特定できない場合は、保証を推測で作らず、対象限定の結果、不足情報および確認先を示して上位完成の主張を保留する。

外部境界を含む実行経路は、物理的なfile、directory、classまたは関数ではなく、独立して成立と終了を確認できる**結合単位**へ分ける。結合単位は、単一の主要責務だけでなく、その責務と同じ状態、Authorityまたは資源のlifecycleを所有する起動、観測、失敗処理、取消、cleanup、Recoveryおよび再入場を含む。付随機能という呼称だけを理由に別の結合単位または検証対象外へ退避しない。

| 結合単位の条件 | 設計で明らかにすること |
|---|---|
| 境界 | 入力、出力、隣接する利用側／提供側、外部環境 |
| 所有責任 | 主要責務、状態、Authority、Identityおよび資源の所有者 |
| lifecycle | 取得／開始、利用、完了、失敗、取消、cleanup、Recoveryおよび再入場のうち実在する段階 |
| 成立条件 | 隣接境界へ渡せる安定した契約と、完了を示す実観測 |
| 終了後条件 | 資源、Lock、Process、永続状態、Authorityおよび回復義務の最終状態 |
| 隔離可能性 | 隣接する結合単位と組み合わせ、実境界または安全に同等な境界で独立検証できること |

結合単位は、同じ資源、Identity、Authorityまたはtransactionの開始からsettlementまでを分断しない大きさとする。別の所有者、失敗隔離、再試行方針、永続化境界または置換可能な外部提供側を持つ箇所は別単位の候補とする。隣接境界との接続だけで正常、失敗、取消、回復および終了後状態を判定できないほど細分化せず、公開入口から複数の独立単位を横断して主要な利用者成果まで確認する範囲を一単位へまとめない。

外部環境との統合は、結合単位ごとに低い層から段階的に接続する。例えばContainer実行ではEngineへの基本接続だけで完成とせず、Task実行と資源回収、残存検出とRecovery、再起動と再入場等を、それぞれのlifecycle所有範囲に従う結合単位として確認した後、公開入口からの総合試験で組み合わせる。結合単位内の意味ケースと隣接境界は結合試験へ、単位間の全体compositionは総合試験へ渡す。

## 2.4. 状態・処理順序・副作用

失敗時に対象が所有するdurable／shared／externalな残存資源、またはRecovery／Authority義務を残し得る取得・初期化では、取得Effectから次のいずれか一つへ至る排他的なtransaction settlementを設計する。

- live owner capabilityを利用側へ安全に移送する
- cleanup完了を実観測する
- 対象に存在するexact Recovery Authorityを移送する
- Recovery Identityを安全に取得できないcleanup不明を、IDなしでoperatorへ移送する
- 取得Effect前に確認済み拒否へ閉じる

このtransactionには、対象範囲で到達し得るobserver、Identity取得、再検証、parserおよび初期化のthrow可能な境界を含め、各境界のcleanup、retentionおよび利用側への投影を同値分割して示す。所有Capability、Recovery Authorityまたは公開結果は対象に存在するものだけを確定対象とし、Recovery非該当の処理へIDを新設しない。read-only観測、取得Effect前に閉じる処理、または失敗時にも残存所有を生じず終了を決定論的に観測できる局所資源は、このtransaction契約の非該当とする。

資源、Authority、EffectまたはRecovery義務の不存在を安全条件に用いる場合、観測契約は`present`、`absent`、`unknown`を区別する。`absent`は、対象Identityに対する明示的な不存在応答からだけ成立させる。アクセス拒否、I/O失敗、競合、形式不正その他の観測不能を`false`、空値またはnot foundへ畳むBoolean APIやcatch-all既定値を、負の根拠として用いてはならない。判定不能は`unknown`として保持し、後続Effect、Authority発行またはcleanup確認済みへの遷移を止める。

Recovery Identityが対象に対して決定論的に確定した後は、排他的な巻戻し後に全残存資源の不存在をfreshに確認してRecovery不要へsettleした経路を除き、その後に到達し得る失敗、取消、観測不能、永続化失敗および公開失敗について、内部戻り値、耐久記録、再入場およびsettlementへ同じexact Recovery Identityを単調に保持する。次回の再入場でIdentityを再発見できることを、初回結果からIdentityを失ってよい根拠にしない。Recovery IdentityはRecovery Authorityと同一とは限らない。公開利用側へIdentityまたは回復参照を返すのは、当該対象の存在と参照の開示を認証、認可および情報分類上許可できる場合に限る。exact Identityを開示できる場合は同じIdentityを返し、開示できないが回復参照を返せる場合は、同じ内部Recovery Identityへ決定論的かつ追跡可能に結合した非Authorityの参照を返す。許可された利用側は、最初の結果から再入場まで同じ回復対象を相関できなければならない。未認証、未認可または情報境界外の利用側へ対象の存在、IdentityまたはRecovery Authorityを開示しない。

主要フローは、開始条件、アクター／契機、内部状態、実行時部品間の処理順序、永続化点、外部への副作用、成功・失敗・取消・回復を対応づける。SPECの状態とアーキテクチャ内部状態を区別し、内部都合の状態を利用者向けの意味として無断公開しない。

状態、遷移、資源、ロック、AuthorityまたはEffectが対象の成立性を左右する場合は、各設計要素について、所有者、取得／生成、利用可能になる条件、変更または消費、解放／失効、競合、判定不能時の処置、および終了後に観測する条件を必要な深さで対応づける。正常経路だけでなく、成立条件を満たす通常完了、処置を限定して完了または拒否できる準正常、失敗・取消・回復を要する異常を区別し、各経路から実装へ渡す契約と検証義務を辿れるようにする。専用の表、図、IDまたは機械可読成果物は一律に要求せず、既存のアーキテクチャ成果物から同じ対応を一意に再構成できればよい。

複数の状態、結果、資源観測、Authority、EffectまたはRecovery義務が共同で結果または利用側の処置を決める境界では、個別項目の許容値だけでなく、同時成立してよい組合せと禁止する相関を不変条件として定義する。成功、完了または利用可能という結果が、cleanup未確認、Effect不明、Authority未成立、未解決Recoveryその他の未成立条件と競合する場合は、成功側へ補正せず、利用側が必要な処置を失わない安全な状態へ単調化する。互いに独立した説明項目だけを返す対象へ相関を捏造せず、共同で決める処置または不変条件を特定できない場合は整合を推定せず、対象限定の結果と確認先を示す。

複数の入口が同じ副作用を起動する場合、共通の実行境界、冪等性キー、ロック、キュー、トランザクション等から適切な制御を選ぶ。常に単一ゲートへ集約するとは限らないが、入口ごとに防御が不一致になる構造は避ける。

## 2.5. 並行処理・失敗・回復

並行処理と失敗は、次の判断を一つの回復契約として扱う。

- 同時実行、再入、重複、古い書き込み、競合の発生条件
- 原子性境界と部分成功を許容する単位
- タイムアウト予算と層間の順序
- 再試行可能な失敗、再試行間隔、上限、冪等性
- 取消、ロールバック、補償、手動回復
- 安全な停止時の受付停止、処理中要求の排出、上限時間
- 依存関係停止、縮退モード、背圧制御、負荷遮断
- 回復後の内部状態、可観測性、再検証

部分成功を常に保持する、または常にロールバックするとは決めない。SPECの結果とデータ完全性に従い、原子性境界を設計する。再試行上限後の復帰を常に人間操作に限定せず、リスク、決定権限、回復可能性をSPECと整合させる。

不合格終了済みは実行許可、同意、安全、法的制約、不可逆操作等、判定不能時に許可側へ倒すリスクが高い境界へ適用する。一般的な表示状態や低リスクの可用性判定へ無条件に拡張せず、代替動作と利用者・利用側へ返す振る舞いをSPECと整合させる。

高コスト処理、バッチ、外部呼び出しでは、必要に応じて実行前に対象有無、適格性、割当上限を安価に判定する受入判定手順を設ける。対象なしの扱い、ログ / 指標 / UI通知の要否は、運用上可観測性と利用者へのフィードバックを損なわないよう対象範囲ごとに決める。

## 2.6. 品質・処理能力・インフラストラクチャ・運用

SPECの観測可能な品質条件と処理能力に関する振る舞いを、負荷前提、リソース、デプロイ、操作へ変換する。

利用者・組織が理解し変更する選好 / 方針 / 設定の意味と観測可能な振る舞いはIA、UI、SPECを正本とする。アーキテクチャは、それらを成立させる保存・配布・優先順位の技術方式と、環境変数、シークレット参照、提供側パラメータ、リソース規模等の技術構成を設計し、両者を無自覚に同じ設定体系へ混在させない。

- トラフィック、並行処理、データ / バッチ量、成長率、ピーク、季節変動
- 遅延、処理量、可用性、永続性、回復、コストの対象 / 制限
- 計算資源、メモリ、ストレージ、DB接続、キュー、キャッシュ、提供側の割当上限
- 自動拡縮、流量制限、背圧制御、負荷遮断、縮退モード
- リージョン、ネットワーク、失敗領域、バックアップ、復元、フェイルオーバー
- デプロイ、ロールバック、構成、シークレット、環境差分
- 指標、ログ、トレース、警告、運用手順の担当責任者、処理能力レビューの契機

過負荷時に何を守り、何を遅延・拒否・縮退するかはSPECへ戻して人間判断を得る。アーキテクチャは、その振る舞いを成立させるリソースと制御を設計する。

## 2.7. 互換性・移行・依存関係の発展

利用側から観測可能な互換性 / 移行期間の振る舞いは振る舞い仕様を正本とし、アーキテクチャは版管理、共存、移行、デプロイ、ロールバック、廃止順序を定義する。

API / インターフェースでは破壊的 / 非破壊を分類し、利用側一覧、複数バージョン共存、接続部 / 互換性層、機能フラグ、廃止条件を扱う。旧契約削除前に利用側移行と利用停止を根拠で確認する。

大規模なRefactor、責務分離または代替実装では、新しいComponent図だけで移行を設計しない。基準版のCapability、その成立を判断したVerification Evidence、置換後の所有者と実装、新しい利用側と実境界検証を対応付ける。設計上の置換完了は、新所有者が存在することではなく、基準版で承認済みの成立条件が新境界で保持されたか、人間の決定権限者が理由と影響を確認して変更した場合に限る。具体的な列挙、Git履歴と過去Evidenceの確認、旧実装の除去条件は[CRDDの保守](19_Maintenance.md#31-tracked-change-execution-contract)に従う。

スキーマ変更は既存データとデプロイ順序を含め、必要に応じて拡張 / 契約、補完処理、二重読み取り / 書き込み、バックアップ、ロールバック、安全な再実行、部分失敗を設計する。

ライブラリ、実行環境、提供側、プラットフォームの追加・更新は、必要性、保守状況、ライセンス、セキュリティ勧告、支援期間、破壊的変更、移行方法、代替を確認する。

### 実行環境が提供する意味の確認

OS、言語Runtime、Process API、Filesystem、Container、Networkまたは外部CLIの性質が、正しさ、安全性、回復、整合性または互換性を左右する場合は、利用する資源、その資源へ要求する保証、対象環境が実際に提供する意味、失敗・観測不能時の扱い、採用するArchitecture契約および反証方法を接続する。外部知識をCRDDの恒久的なPlatform正本へ複製せず、対象Platform、Runtimeと版、情報源または観測条件、確認時点および設計判断へ必要な範囲だけを保持する。

API呼出し、要求の発行、handleの取得、受理、Effectの成立、完了の通知、外部からの観測および耐久的な確定を同一視しない。必要な保証がどの段階で成立するかを定め、その前段階だけから成功、回収完了またはEffect不存在を推定しない。例えば、Process handle取得はOSの起動成功、`write()`呼出しは相手から観測可能な書込み完了、終了要求はProcessの終了確認を意味しない。

実行環境の意味は、十分な仕様または既存根拠で確定できるもの、重要だが曖昧で小規模実測により大きく不確実性を減らせるもの、実Systemまたは外部環境でだけ合理的に確認できるものへ分ける。小規模実測は、必要保証の重要性、意味上の不確実性および後工程での是正費用が高く、調査費用に見合う場合だけ本実装前に行う。好奇心、低Riskな実装詳細または全Platform知識の収集を理由に発火させず、実環境E2Eを省略する根拠にも用いない。

外部CLI、OS API、ContainerまたはServiceの挙動差を原因候補にする場合は、推測した原因を先に確定しない。必要保証を、入力、Authority、要求、受理、実Effect、観測および終了後条件へ分解し、判断を変える最小の実境界で候補原因を反証する。CLI optionの存在、help表示、正常終了code、Providerの完了申告または一つの副作用だけから、必要Capability全体の成立や不成立を断定しない。複数Providerまたは実行環境が同じRoleを担う場合は、各境界で同じ保証を個別に確認し、一方の結果を他方へ一般化しない。

### 外部境界の診断可能性

外部境界を設計する場合は、障害後に生の入力や機密情報を追加収集しなくても、必要保証がどの段階で止まったかを判別できる診断契約を実装と同時に設計する。診断は後付けのDebug機能ではなく、外部境界の成立条件である。対象に存在する段階だけを用い、存在しないEffect、Authorityまたは状態を診断のために新設しない。

| 観測段階 | 区別する内容 |
| --- | --- |
| 入力・構成 | 実行に使用した閉じた構成、対象Identityおよび設定値と、実観測値の区別 |
| 要求・受理 | 要求を発行したことと、外部境界が受理したこと |
| 開始・完了 | Process、Command、Taskまたは転送の開始と終了、および終了理由の閉じた区分 |
| Effect・結果搬送 | 外部Effectの成立、成果物の観測、結果の搬送および利用側受理 |
| 終了後 | cleanup、資源不存在、Authority失効、Recovery義務および観測不能 |

診断は、状態、件数、契約で定義した終了code区分、Hash、相関Identityまたは理由付き`unknown`を基本とする。Prompt、Command本文、Provider生出力、Credential、許可外のHost Pathまたは候補内容を、診断しやすさだけを理由に記録しない。診断の追加がAuthority、Sandbox、入力、タイミングまたは外部Effectを変える場合は、それ自体を設計変更として扱い、非介入性を反証する。

同じ外形失敗に複数の原因候補が残る設計では、最初の実境界試験または運用障害から次の処置を選べる粒度まで観測点を補う。実Provider呼出し、従量資源または長時間待機を伴う境界では、一回の実行で主要な原因層を分離できないことを既定にせず、実行前に観測表と相関方法を確認する。

必要なRuntime、版、情報源または観測方法を安全に確定できない場合は、前段階から後段階の成立を推定しない。必要保証が当該意味へ依存しない代替境界を選ぶ、対象環境を未対応／未評価として停止する、または不足根拠だけを得る限定確認へ進む、のいずれかへ明示的に閉じる。

基本方針は、環境の正当な更新と構成差を柔軟に受け入れ、必要な保証を確認できない危険を検出した場合だけ後続Effect前に安全に停止することである。実行環境の信頼は、既知の版、単一Hashまたは過去に確認したbyte数との一致だけへ一律に結合しない。必要な保証に応じて、由来、真正性、能力、互換性および一回の操作中の同一性を分けて確認する。環境更新を安全性の低下と同一視せず、必要保証を実測できる更新後の環境は受理できるようにする。一方、必要な真正性または能力を確認できない状態、未対応の互換性、観測中または実行中の差替え、および結果の解釈を変える不明な差分は、後続Effect前に安全に停止する。

| 対象 | 既定の確認 | 厳密な版／Hash固定を使う条件 |
| --- | --- | --- |
| Host上の一般Tool／CLI | 信頼する配置とPublisher、必要Capability、Protocol互換、操作中の同一Identity | 版そのものが安全契約、再現性または結果解釈を所有する場合 |
| Container／Build入力 | 固定Digest、lockfile、Toolchain Identity | 再現可能なBuildまたは隔離境界を保証する場合 |
| 署名済みRuntime／Native成果物 | 署名、Hash、依存閉包、発行者Trust Policy | 実行AuthorityまたはArtifact Integrityを発行する場合 |
| Protocol／Schema | 対応Revision、交渉可能範囲、未知値の拒否 | byte解釈または安全な相互運用を一意にする場合 |
| Host修復用Policy | 対象版、対象Artifact、事前状態、修復後観測 | Host状態を変更する限定修復を再現・制限する場合 |

厳密な固定を採用する場合は、「変化したから危険」ではなく、そのIdentity差がどの保証を失効させるかを示す。更新可能な外部環境へ過去のHashを恒久的な許可表として埋め込まず、Publisherの真正性と必要Capabilityを確認したうえで、操作開始時のIdentityをsnapshotし、その操作中の差替えを拒否する方式を優先する。

## 2.8. セキュリティ・プライバシー・AIのアーキテクチャ

セキュリティ、プライバシー、AIガバナンスは後付けの非機能項目ではなく、境界、データ、インターフェース、実行経路へ組み込む。

- 認証、認可、テナント、最小権限、機密情報
- 信頼境界、入力の来歴、外部データと指示の分離
- 許可リスト、スキーマの妥当性確認、ツール / 外部操作権限
- 同意、目的、データ最小化、提供側送信、リージョン、学習利用
- 観察済み事実、AIによる推論、人間確認済み情報の分離
- 保持、削除、訂正、同意の撤回、バックアップ / ログ
- 出力妥当性確認、人間の承認、比率 / コスト制限、監査、緊急停止

プロンプトの注意書き、UIの同意表示、リリース直前のマスキングだけをセキュリティ境界とみなさない。バッチ、キュー、再試行、管理者パスを含むすべての実行経路で同じ決定権限とデータ境界を強制する。

内部コンテキストから外部検索、外部AI、API、MCP、生成サービスその他の外部接続へ至る経路では、[外部情報境界](01_Principles.md#external-information-boundary)をアーキテクチャ上の信頼境界として扱う。接続母集団、情報分類の継承、派生・組合せ情報、許可した処理境界の目的・操作・送信先・利用主体・保持・二次利用、境界外の外部向け調査コンテキスト、送信前検査、外部結果の指示権限を分ける取込み境界、監査、拒否、失効・復旧および人間判断への移送を設計する。検索、生成、分析等の機能別経路が共通境界を迂回しないようにする。

セキュリティ設計は固定チェックリストだけで終えず、対象資産、脅威主体、信頼境界、攻撃面、攻撃経路、影響、既存制御、制御候補、残存リスク、検出、封じ込め、失効、回復および人間が判断するリスクを、対象に応じた[専門探索・収束契約](11_Skill.md#23-expert-exploration-and-convergence-contract)で合成・批評する。既存の脅威分類は候補生成に使えるが、分類項目を埋めたことを脅威網羅へ流用しない。

AIを含む対象では、直接・間接プロンプトインジェクション、悪意ある文書、ツールまたはMCPの汚染、コンテキスト流出、権限昇格、エージェント間漏洩、記憶・根拠の汚染、機微な推論、権限の幻覚、未承認の外部操作等から有力な攻撃経路を探索する。外部入力をデータとして処理する境界と、信頼済みの指示・方針・権限を変更できる境界を分ける。

重大な保護は、例えば「厳格管理情報は許可された開示判断なしに外部信頼境界を越えない」「読取専用監査主体は監査対象を変更しない」のようなセキュリティ不変条件として、情報源となる原則・要求・SPEC、実行時強制、監査ログ、検証義務および失敗時の閉じ方へ接続する。人間のリスク受容が必要な残存リスクを、制御実装または検証成功へ混ぜない。

## 2.9. 実装・コーディング規約の決定権限

プロジェクト固有のコーディング規則と実装規則の正本Markdownは`06_Architecture`へ置く。アーキテクチャは規則の意味、適用対象範囲、理由、禁止事項、例外承認、検査方法を定義する。

```text
06_Architecture/
├─ システム / データ / インターフェースアーキテクチャ
├─ セキュリティ / 運用 / 移行アーキテクチャ
└─ コーディング / 実装規則
```

コード、構成、移行、開発者テスト、ビルド成果物、およびリンター / フォーマッター / 静的解析ツール等の実行可能な強制手段は実装が所有する。アーキテクチャは具体的なテスト場合を所有せず、テスト可能性を確保する境界、必要環境、失敗 / 負荷 / 移行等の検証義務を渡す。

### UIと視覚表現の成立方式

UIがUIテーマ、再利用UI部品／UI設計パターン、または外部視覚成果物との改訂版対応を要求する場合、アーキテクチャは次の成立方式を適用範囲で定義する。対象範囲が視覚的な論理画面を持つ場合は、これらの要求の有無にかかわらず、デザインシステム参照実装に関する項目を定義する。

- 基本要素／意味／部品層のデザイントークンの関係
- UIテーマ差分軸と技術モード、既定値、永続化、代替動作の対応
- UI部品契約と実行時部品／項目インターフェース／UI部品状態／UI差分（UI Variant）の対応
- 論理画面／経路／画面領域、レスポンシブ対応 UI差分、UI素材、モーションの実装境界
- 外部視覚成果物の改訂版、デザイントークン／UI部品バージョン、ビルド改訂版の追跡と比較可能性
- デザインシステム参照実装の改訂版、対象プラットフォームへの変換方式、ビルド改訂版との追跡と比較可能性
- UIテーマ、UI部品、UI設計パターン変更時の互換性と移行

Figmaモード、CSS変数、デザイントークン形式等の特定ツールをCRDD共通方式として固定しない。UIが所有する視覚表現方針、UIテーマの意味、UI部品の利用規則をアーキテクチャ都合で変更しない。成立不能またはコスト / 性能 / プラットフォームの制約はUI / SPECへフィードバックする。

UIテーマ、共通UI部品、外部視覚成果物を使用しない対象範囲では、本責務を理由付きで`Not Applicable`にできる。ただし視覚的な論理画面を持つ対象範囲では、デザインシステム参照実装の改訂版と対象プラットフォームへの変換方式を`Not Applicable`の対象に含めない。視覚的な論理画面を持たない対象範囲の扱いは[UI](25_UI.md#design-system-reference)を正本とする。

## 2.10. 表現・判断・追跡

アーキテクチャはテキスト、表、スキーマ、図を、関係を最も明確に表す最小の形式で使う。図だけを正本にせず、境界、規則、制約、判断理由をテキストで追跡可能にする。複雑な構造や連番ではMermaid等の差分管理可能な図を利用してよい。

アーキテクチャへCRDD標準安定コンテキストIDを新規発行しない。情報源 `REQ-*` / `UX-*` / `IA-*` / `UI-*` / `SPEC-*`、変更トレース、実装、検証とは成果物参照と関係で接続する。

根拠はアーキテクチャ成果物内または最も近い親フォルダの`Evidence/`へ置く。選択結果はアーキテクチャの正本成果物の`Decision / Rationale`へ、コンテキスト、選択、理由、代替案、根拠、トレードオフ、影響、再評価契機を残す。全判断をルート直下の台帳へ複製しない。

---

<a id="3-guided-skill-adapter"></a>

# 3. スキル実行接続部（Guided Skill Adapter）

<a id="31-runtime-authority"></a>

## 3.1. 実行時の決定権限（Runtime Authority）

`skill.architecture.integrate`は、本書の工程実行契約を[`11_Skill.md`](11_Skill.md)の実行の状態遷移、ガイド付き対話、人間によるレビュー、引き渡しに従って実行するアーキテクチャ固有接続部である。本書では実行状態、一時停止 / 再開、共通の質問規則、サブエージェントの状態遷移を再定義しない。

## 3.2. アーキテクチャ固有の進行

| 手順 | 変換 | 出力 |
|---|---|---|
| 負荷 | UI / SPEC、品質、既存システム、制約を対応づける | アーキテクチャの網羅キュー |
| フレーム | システム／信頼境界、アクター、データ、外部依存関係を定める | システムコンテキスト |
| 構造 | ドメイン、実行時部品、データ、インターフェース、内部状態／連番を設計する | 構造契約 |
| 探索 | 主要な設計要因と設計上の緊張から、パターン知識、境界、状態所有、失敗を用いて意味の異なる構造候補を生成し、トレードオフ、前提感度、可逆性を比較する | 構造候補、判断候補、逆転境界、事前失敗分析 |
| 強化 | セキュリティ、失敗、並行処理、互換性、処理能力、操作を判定する | 運用上アーキテクチャ |
| 有効化 | 実装規則、テスト可能性、検証義務を定める | 実装への引き渡し |
| レビュー | 網羅範囲、判断、不足、リスクを人間が確認する | 承認済み / 一部完了 / 停止中 |

質問数や文書分割を進捗とみなさず、必要な責務の網羅と対象範囲で進捗を判定する。

### 設計要因・構造合成・評価（Drivers, Architecture Synthesis, and Evaluation）

[専門探索・収束契約](11_Skill.md#23-expert-exploration-and-convergence-contract)を、アーキテクチャでは次の順で具体化する。

```text
主要な設計要因と優先順位
同時に最大化できない設計上の緊張
既知パターンと構造原理の検索
境界、状態所有および失敗からの構造合成
意味の異なる構造候補と現状維持
品質、複雑性、運用、費用、移行、セキュリティおよび将来選択肢のトレードオフ
判断が依存する前提
前提変化に対する感度
可逆性と復旧可能性
失敗した状態から逆算する事前失敗分析
判断、残存不確実性および再評価契機
継続確認可能な適合性指標
```

設計要因には、利用者価値、品質特性、制約、想定負荷、障害許容度、セキュリティ、プライバシー、運用能力、費用、チーム能力、移行条件および将来変更を、対象に応じて含める。全項目を固定的に埋めず、判断を逆転させ得る要因を優先する。

設計要因から、単純さと将来移行、分離と統合、整合性と可用性、遅延と費用等、両方を同時に最大化できない設計上の緊張を特定する。緊張は製品上の価値と制約へ接続し、技術上の好みだけで優先順位を決めない。

構造合成では、既知のアーキテクチャ設計パターンを答えではなく候補生成の語彙として使う。対象に応じて、パターンが解く問題、成立しやすい条件、費用、典型的な失敗、避ける条件、組合せ可能性を確認し、次の順で具体構成へ適応する。

```text
問題と設計上の緊張
  ↓
構造原理
  ↓
候補となるパターン知識
  ↓
今回の制約への適応
  ↓
具体的な構造候補
```

パターン名または製品名を選ぶことをアーキテクチャ判断としない。レイヤー、ポートとアダプター、モジュール分割、イベント、パイプライン、プラグイン、クライアント／サーバー等の既知形式を参照する場合も、なぜ今回の分離軸として有力か、副作用を許容できるか、より単純な構成で足りないかを説明する。

構造候補を生成するときは、少なくとも次の専門推論を対象に応じて行う。

| レンズ | 確認すること |
|---|---|
| 境界推論（Boundary Reasoning） | 一緒に変更・拡張・失敗・信頼・所有・配布・取引すべき責務は何か、どこで変化と障害を閉じ込めるか |
| 状態所有推論（State Ownership Reasoning） | 誰が状態を所有し、何が正本・派生・一時・永続・キャッシュか、同期、陳腐化検知、再構築、喪失時の責任は何か |
| 失敗起点の推論 | 依存停止、ネットワーク分断、容量不足、処理停止、重複、部分書込、陳腐化、移行中断、外部契約変更等で、影響範囲、検知、縮退、復旧、データ完全性がどうなるか |
| 構造候補の差 | 何を分離軸または統合軸にした案か、どの設計上の緊張へ別の答えを出すか |

外部提供側が別である、ドメイン名が異なる、または既知パターンが使えるという事実だけで境界を増やさない。逆に、同一プロセス、同一Repositoryまたは同一配布単位を理由に、異なる決定権限、信頼境界、状態所有または障害領域を隠さない。

高影響、不可逆または不確実な判断では、実質的な代替を比較する。制約により一案しか成立しない場合は、現状維持または別方式が有力でない理由を説明し、形式的な代替を作らない。

前提感度では、負荷、データ量、SLA、障害許容度、チーム人数、運用費用、法的条件、外部依存またはリリース頻度等のどの変化で判断が逆転するかを確認する。逆転境界、未評価の有力な代替、重大な失敗仮説または復旧不能が残る場合は収束済みとしない。継続的に機械確認する価値がある重要特性だけを適合性指標へ変換し、全判断への指標追加を要求しない。

## 3.3. 対話による明確化

専門用語を要求する前に、人間が答えられる言葉で不足コンテキストを確認する。

| 話題 | 質問の意図 |
|---|---|
| 境界 | どこで動き、誰・何と接続する必要があるか |
| データ | 誰のデータを、どこへ、どの期間保存できるか |
| 決定権限 | 誰が読み書き・承認・外部実行できるか |
| 失敗 | 外部サービスや処理が停止したとき何を守るか |
| 完全性 | 失敗しても失ってはいけないデータや操作は何か |
| 発展 | 将来交換したいもの、既存運用で変えられないものは何か |
| 品質 | 利用規模、応答時間、可用性、復旧、コストの目安は何か |
| 操作 | 誰が異常を知り、停止・復旧・ロールバックするか |

回答を境界、データ、インターフェース、失敗、セキュリティ、品質、操作、移行、処理能力へ変換する。上位契約を弱める必要がある場合はアーキテクチャ内で確定しない。

## 3.4. 上位判断への移送とサブエージェントの使用

次の場合は該当決定権限へ戻す。

| 条件 | 移行先（Route） |
|---|---|
| 情報源振る舞い、品質条件、決定権限が不明 | SPEC / 人間の判断 |
| UIと実現可能な振る舞いが競合 | UI／SPECの対応レビュー |
| データ利用目的、同意、保持期間が不明 | 課題探索・要求形成 / 人間の判断 |
| 技術検証が必要 | アーキテクチャ技術検証 / 根拠取得 |
| リスク、コスト、互換性破壊の受容が必要 | 人間の決定権限 |
| アーキテクチャは成立するが実装方法の選択が主題 | 実装 |

サブエージェントを使う場合は[`10_Agent.md`](10_Agent.md)に従い、データ、セキュリティ、処理能力、移行、提供側比較等の限定対象範囲を委譲できる。アーキテクチャ全体の整合、判断、網羅範囲、引き渡しは親エージェントが統合し、人間確認を得る。

---

<a id="4-review-handoff-view-and-feedback"></a>

# 4. レビュー・引き渡し表示・フィードバック

## 4.1. アーキテクチャ固有の人間レビュー

人間レビューでは少なくとも次を確認する。

- 上位意図と振る舞いを技術都合で弱めていない
- 境界、決定権限、データ所有関係、正本が一意または競合管理されている
- 失敗、セキュリティ、プライバシー、互換性、処理能力のリスクを隠していない
- アーキテクチャ設計パターンと技術選択にコンテキスト、代替案、トレードオフがある
- 設計上の緊張、パターン知識、境界、状態所有および失敗から、有力な構造候補を生成した理由を説明できる
- 判断を支配する前提、判断が逆転する感度、可逆性、事前失敗分析および再評価契機を確認している
- 操作、移行、ロールバック、検証を実装後回しにしていない
- プロダクト設定と技術構成を区別し、成立方式と決定権限を実装へ渡している
- 一部完了 / `Not Applicable`の対象範囲、理由、担当責任者が明確である

## 4.2. アーキテクチャ成果物／引き渡し表示

`06_Architecture/01_Architecture.md`をアーキテクチャ工程の固定入口とする。対象範囲について、次を入口内または入口から辿れる正本成果物で参照可能にする。適用の深さでは入口や基本のファイル分割を変えず、記述、レビュー、根拠の深さを調整する。図、技術規則または外部成果物を分ける場合も、入口から決定権限、改訂版、網羅状態、未解決事項へ到達できるようにする。

```text
対象範囲 / 網羅範囲要約 / 未解決不足
情報源となるUI / SPEC / 品質要件 / 制約
システムコンテキスト／信頼境界
ドメイン / 部品責務
データ / 正式な情報源 / 状態遷移
インターフェース / 統合 / 外部依存関係
状態 / 順序 / 並行性 / 回復
セキュリティ／プライバシー／AI境界
品質 / 処理能力 / インフラストラクチャ / 運用
技術構成／プロダクト設定境界
UI / 視覚表現の実現 / UIテーマ・トークン・部品の対応表 / 視覚成果物・ビルド改訂版
互換性 / 移行 / ロールバック
実装 / コーディング規則
検証義務
判断 / 判断理由 / 根拠 / 人間レビュー
変更影響の伝播確認結果 / 情報源の改訂版 / 上流更新 / 下流再探索 / 伝播例外
工程移行レビュー結果 / レビュー済み改訂版 / 指摘事項処置 / レビュー例外
```

実装への引き渡しはこの表示を縮小再掲して受信条件を減らさず、アーキテクチャの正本成果物への参照と網羅状態付きで渡す。

## 4.3. アーキテクチャへのフィードバック

実装、検証、操作、インシデント、依存関係更新から、成立不能、性能差、失敗モード、セキュリティ不足、移行リスク等が得られた場合はアーキテクチャへ戻す。

同じ制約下の明確化は既存成果物を更新する。境界、決定権限、データの意味、主要技術、移行方針等を置換する場合は、旧判断を消さず、新しい判断 / 判断理由と成果物参照で履歴を接続する。

アーキテクチャで得た学びがSPECやUIの観測可能な振る舞いを変える場合は、アーキテクチャだけを更新せず、該当工程を再開する。

---

<a id="5-final-principle"></a>

# 5. 最終原則（Final Principle）

アーキテクチャは図やアーキテクチャ設計パターンを作って終わる工程ではない。

上位コンテキストを実現可能な境界、契約、制約、規則へ変換し、実装と検証が迷わず実行・検証できる状態を作る。
