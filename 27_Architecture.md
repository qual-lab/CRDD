# CRDDアーキテクチャ工程

Version: v0.16.0
Status: Stable
Owner: Qual-Lab
Skill ID: `skill.architecture.integrate`
Last Updated: 2026-08-10
Related:
- [01_Principles.md](01_Principles.md)
- [02_Terminology.md](02_Terminology.md)
- [03_Documentation.md](03_Documentation.md)
- [10_Agent.md](10_Agent.md)
- [11_Skill.md](11_Skill.md)
- [16_Quality_Assurance.md](16_Quality_Assurance.md)
- [18_Context_Dependency.md](18_Context_Dependency.md)
- [24_UI_Behavior_Specification.md](24_UI_Behavior_Specification.md)
- [25_UI.md](25_UI.md)
- [26_Behavior_Specification.md](26_Behavior_Specification.md)
- [28_Implementation.md](28_Implementation.md)
- [29_Verification.md](29_Verification.md)
- [51_Document_Audit.md](51_Document_Audit.md)
- [52_Conformance_Audit.md](52_Conformance_Audit.md)
- [53_Gap_Impact_Audit.md](53_Gap_Impact_Audit.md)

---

> アーキテクチャを適用するプロジェクトでは、本書の`Phase Process Contract`をアーキテクチャ工程内の正本として使用する。

> この文書で分かること（非規範の案内）
>
> - 上流の要求・UX・UI・仕様を技術構造へどう変換するか
> - 境界、データ、インターフェース、品質、セキュリティをどう決めるか
> - 下流で判明した制約を上流へどう返すか
> - コーディング規則や実装制約をどこへ置くか
> - 実装へ渡す前に何を確認するか

<a id="1-purpose-and-boundary"></a>

# 1. 目的と適用範囲（Purpose and Boundary）

アーキテクチャは、承認済みプロダクトコンテキストを、現在の制約下で実現できるシステム境界、データ、インターフェース、セキュリティ、品質、操作、実装規則へ変換する工程である。

```text
振る舞い仕様 = 何が、どの条件で、どう振る舞うべきか
UI                     = 利用者がどう認識・操作・回復できるか
アーキテクチャ           = どの境界・構造・契約・制約で成立させるか
実装         = 承認済みアーキテクチャをコード等へどう具体化するか
検証           = 対象改訂版で実際に成立したか
```

アーキテクチャは要求、UX成果、UI表現、振る舞い仕様、コードの代替ではない。技術都合で上位意図や振る舞いを暗黙に弱めず、成立不能または新しい制約を発見した場合は代替案と影響を該当工程へ戻す。

アーキテクチャ成果物は現在の制約に対する選択結果であり、永続的な一般原則と同一視しない。

---

<a id="phase-process-contract"></a>

# 工程実行契約（Phase Process Contract）

本章はアーキテクチャ工程の入口、変換、責務網羅範囲、出口、工程ゲート、監査の正本である。本章の規範性と運用規模は[文書化](03_Documentation.md#48-normative-language)に従う。後続章は本契約を設計判断とスキル実行へ具体化し、独自の完了条件を持たない。

<a id="phase-entry-contract"></a>

## 工程入口契約（Phase Entry Contract）

アーキテクチャは、対象範囲について次を受け取る。

- 情報源となるREQ / UX / IAへのトレース
- 承認済みUI契約、視覚表現方針（Visual Direction）、適用するUIテーマ（UI Theme）/ UI部品（UI Component）/ UI設計パターン（UI Design Pattern）/ [外部視覚成果物（External Visual Artifact）](25_UI.md#external-visual-artifact)、振る舞い仕様と[対応レビュー](24_UI_Behavior_Specification.md#26-exit-and-pair-gate)結果
- [デザインシステム参照実装（Design System Reference）](25_UI.md#design-system-reference)の改訂版、対象プラットフォームへの変換要否
- 適用する品質懸念プロファイルと情報源／バージョン／対象範囲、品質、互換性、処理能力、セキュリティ、プライバシー、コンプライアンス、コストの条件
- 受入基準と検証義務
- 既存システムコンテキスト、データ、インターフェース、操作、デプロイ
- 技術、プラットフォーム、提供側、リソース等の既知制約
- 外部依存関係と利用側
- 網羅範囲の要約、未解決事項、人間レビュー結果
- UI／SPEC→アーキテクチャ工程移行レビュー結果、レビュー済みのUI／SPEC改訂版、または明示された`review_exception`
- UI／SPEC／対で発火した変更影響の伝播確認結果、情報源の改訂版、または明示された`propagation_exception`

部分引き渡しの場合は、承認された対象範囲、未決事項、暫定制約、リスク、後続担当責任者、人間承認も必要である。上位コンテキストが矛盾する場合はアーキテクチャで都合よく解釈せず、該当決定権限へ戻す。

<a id="transformation-contract"></a>

## 変換契約（Transformation Contract）

プロダクトコンテキストを、実装と検証に使えるアーキテクチャコンテキストへ変換する。必要に応じて次を定義する。

- システム／ドメイン／実行時部品境界
- データ / インターフェース契約と正本
- 内部状態／連番
- セキュリティ / プライバシーと品質属性
- 外部統合と互換性 / 移行
- 処理能力 / インフラストラクチャと並行処理 / 回復力
- 可観測性と操作
- 実装規則
- 適用対象のUIテーマ／デザイントークン（Design Token）／UI部品／論理画面の視覚表現成立方式

アーキテクチャは観測可能な振る舞いを新しく決めない。技術的制約から振る舞い変更が必要な場合は、振る舞い仕様またはUIへフィードバックし、人間判断後の契約を設計へ反映する。

性能、可観測性、障害回復、セキュリティ、移行、テスト容易性について検証義務、観測要件、必要なログ／メトリクス／制御点、環境条件を追加・更新し、[品質保証](16_Quality_Assurance.md)の検証設計へ接続する。単体試験の分岐網羅率を測定するためにアーキテクチャ上のビルド、生成、実行境界または計測方式へ条件がある場合は、その条件と正当な除外候補を実装へ渡す。

<a id="required-responsibility-coverage"></a>

## 必要な責務の網羅（Required Responsibility Coverage）

対象範囲全体について、次の責務を適用範囲で判定する。

| 責務 | アーキテクチャで明らかにすること |
|---|---|
| システムコンテキスト | アクター、外部システム、信頼境界、主要データフロー |
| ドメイン／実行時部品境界 | 責務、決定権限、依存方向、交換境界 |
| データ | モデル、所有責任、正本、状態遷移、整合性、トランザクション境界 |
| インターフェース / 統合 | API / IPC / イベント、同期性、エラー、バージョン、利用先／提供元の境界 |
| 内部状態／連番 | 処理順序、永続内部状態、一時内部状態、副作用、調整 |
| セキュリティ / プライバシー / コンプライアンス | 認証、実行許可、テナント、シークレット、データフロー、保持期間、監査 |
| 品質 / 操作 | 可用性、遅延、処理量、回復、保守性、コスト、運用責任 |
| 並行処理 / 回復力 | 冪等性、競合、タイムアウト、再試行、背圧制御、部分失敗、停止 |
| 互換性 / 移行 | 利用側、スキーマ、バージョン、移行期間、ロールバック、廃止順序 |
| 処理能力 / インフラストラクチャ | 負荷前提、リソース、割当上限、スケーリング、リージョン、デプロイ、失敗領域 |
| 技術構成 | プロダクト設定との境界、保存・配布・優先順位の成立方式、環境、シークレット参照、提供側パラメータ、リソース規模、既定値 / 上書きの決定権限 |
| UI／視覚表現の成立 | 適用対象の意味／部品層のデザイントークン、UIテーマ差分とモード、UI部品と実行時部品、論理画面／経路／画面領域、UI素材／モーション、外部視覚成果物の改訂版とビルド改訂版の対応、デザインシステム参照実装の改訂版とビルド改訂版の対応・対象プラットフォームへの変換方式 |
| 可観測性 | 指標、ログ、トレース、警告、監査、運用上の状態判定 |
| 実装規則 | コーディング / 依存関係 / 境界規則、禁止事項、実行可能な検査への接続 |
| 検証有効化支援 | 性能、可観測性、障害回復、セキュリティ、移行、テスト容易性の検証義務、必要なログ／メトリクス／制御点、環境条件、単体試験の分岐網羅率を測定するための条件、負荷 / 失敗 / 移行の検証観点 |
| トレース / 判断理由 | 情報源コンテキスト、判断、代替案、制約、根拠、未解決事項 |

すべてを全対象範囲へ機械的に記載する必要はない。適用しない責務は`Not Applicable`として理由と人間確認を残す。単一図、主要正常パス、技術選定、プロトタイプの完成だけでアーキテクチャ完了としない。

<a id="scope-and-coverage-state"></a>

## 対象範囲と網羅状態（Scope and Coverage State）

各上位コンテキストと各アーキテクチャ責務を、`Complete for Scope`、`Partial — Human Authorized`、`Blocked`、`Not Started`、`Not Applicable`で追跡する。

対象範囲は機能名だけでなく、アクター、データ、インターフェース、環境、構成差分、移行対象、既存利用側を含めて特定する。網羅範囲の要約は「設計文書がある」ではなく、必要な責務が判定済みかを示す。

<a id="human-decisions"></a>

## 人間による判断（Human Decisions）

各項目の人間の決定権限者は、システム／信頼境界、技術 / 提供側選択、データ所有関係、品質 / コストトレードオフ、セキュリティ / プライバシーリスク、互換性破壊、移行 / ロールバック方針、重大依存関係、`Not Applicable`、部分引き渡しを決定する。

AIは候補比較、不足、影響、設計案を提示できるが、上位契約変更、リスク受容、決定権限、重大トレードオフを自己承認しない。

アーキテクチャで対象項目の人間の決定権限者による判断、制約、学び、根拠、指摘事項を確定または変更した時点で、[変更影響の伝播確認](53_Gap_Impact_Audit.md#43-mandatory-propagation-trigger-and-closure)を実行する。

課題探索・要求形成、UX、IA、UI、SPECにある次の項目へ答える、または影響する候補を探索する。

- 未決事項と未解決事項
- 仮定と判断
- 制約

見つかった結果は、それぞれの正本へ反映する。上流の正本を更新した場合は、更新後改訂版からアーキテクチャ以降への影響を再探索・再監査するまで通常完了としない。

<a id="exit-and-handoff"></a>

## 完了条件と引渡し（Exit and Handoff）

通常引き渡し候補を人間のゲートへ提示する前に、次を行う。

1. 対象範囲／改訂版へ、契約確認と本工程の「必要な責務の網羅」「工程監査チェックリスト」に対する[専門品質確認を含む工程移行レビュー](10_Agent.md#72-phase-transition-review-and-remediation-loop)を実行する。
2. 移行に影響する指摘事項をアーキテクチャまたは責務を持つ工程で修正する。
3. 修正後改訂版を再レビューし、`Pass`を得る。
4. 対象内容と工程移行の人間の決定権限者が、内容とレビュー結果を確認して移行を決定する。

レビューの省略または未解消の指摘事項を伴う移行は、[人間が指示するレビュー例外](10_Agent.md#73-human-directed-review-exception)がある場合だけ通常経路と区別して扱う。

通常の実装への引き渡しは、対象範囲が`Complete for Scope`で、対象内容と実装への移行を人間の決定権限者が承認し、[`28_Implementation.md`](28_Implementation.md#phase-entry-contract)の受信条件を満たす場合に限る。

引き渡しでは、次を渡す。

- 承認済み対象範囲と情報源となるUI／SPEC
- アーキテクチャ境界
- データ / インターフェース / セキュリティ契約
- 互換性 / 移行と処理能力 / 操作
- 技術構成と実装規則
- 禁止事項と既知制約
- 受入基準と検証義務
- 網羅状態と未解決事項

部分引き渡しには、対象範囲、未定義設計、暫定制約、リスク、受信先、後続担当責任者の人間承認を必要とする。アーキテクチャ不成立をコードで暗黙解決させない。

<a id="phase-gate-criteria"></a>

## 工程移行の判定基準（Phase Gate Criteria）

- 情報源となるREQ / UX / IA / UI / SPECへトレースできる
- 境界、決定権限、データ所有関係、正本、インターフェースが判定済みである
- 内部状態／連番、失敗、並行処理、回復が説明・検証可能である
- セキュリティ / プライバシー / コンプライアンス、品質、操作、可観測性を適用範囲で定義している
- 適用する品質上の懸念をアーキテクチャ上の品質属性、成立方式、検証義務へ処置している
- 互換性 / 移行、処理能力 / インフラストラクチャ、依存関係を適用範囲で定義している
- プロダクト設定と技術構成を区別し、保存・配布・優先順位、環境差分、シークレット参照、提供側パラメータ等を適用範囲で定義している
- UIがUIテーマ、共通UI部品／UI設計パターン、論理画面の視覚表現成立方式を必要とする場合は、デザイントークン／モード、実行時部品、経路／画面領域、UI素材、改訂版の対応、代替動作を適用範囲で定義している
- 視覚的な論理画面を持つ対象範囲では、デザインシステム参照実装の改訂版、対象プラットフォームへの変換方式を定義している
- 実装規則、禁止事項、検証義務が実装前に明確である
- 単体試験の分岐網羅率測定へ影響するアーキテクチャ条件または除外候補がある場合、対象と理由を実装および検証設計へ渡している
- アーキテクチャが所有する検証義務、観測要件、必要なログ／メトリクス／制御点、環境条件を検証設計へ接続し、該当するアーキテクチャ／運用の専門観点でレビューしている
- 判断 / 判断理由、網羅範囲の不足、部分引き渡し承認が記録されている
- 発火した変更影響の伝播確認が`Pass`であり、必要な上流・同層正本更新と下流再探索・再監査が完了している
- 対象改訂版の工程移行レビューが、適用される専門観点をすべて評価したうえで`Pass`であり、移行に影響する指摘事項の是正と再レビューが完了している

<a id="phase-audit-checklist"></a>

## 工程監査チェックリスト（Phase Audit Checklist）

- 上位コンテキストにトレースしないアーキテクチャ判断
- SPECが所有する振る舞いやUIが所有する表現のアーキテクチャ内での再定義
- 境界、決定権限、データ所有関係、正本の重複または競合
- 失敗、並行処理、セキュリティ、プライバシー、移行、処理能力、依存関係、操作、可観測性、技術構成の適用判定漏れ
- プロダクト設定と技術構成の混同、既定値 / 上書きの決定権限または環境差分の未定義
- UIのUIテーマ／UI部品／論理画面契約をアーキテクチャが再定義している、またはデザイントークン／モード／実行時部品／UI素材／改訂版の対応が未定義で実装へ暗黙移管されている
- デザインシステム参照実装の改訂版・対象プラットフォームへの変換方式が未定義で実装へ暗黙移管されている
- アーキテクチャ設計パターン、技術、提供側を、理由のない一律規則として強制している状態
- 図やコード例だけで契約、制約、判断理由がない状態
- テストケースをアーキテクチャが所有、またはテスト可能性 / 検証義務を未定義にしている状態
- 分岐網羅率の測定に必要なビルド／生成／実行境界を未定義にしている、またはアーキテクチャ上の理由なく測定対象から除外していないか
- アーキテクチャの検証義務、観測要件、ログ／メトリクス／制御点または環境条件が検証設計へ未接続、もしくは該当する専門観点で未評価でないか
- 網羅範囲の要約、未解決事項、人間レビュー、実装規則の欠落
- アーキテクチャ判断、制約、学び、根拠、指摘事項に対応する上流未決事項 / 不足探索、正本反映、下流再探索、再監査の欠落
- 独立レビューまたは必要な専門観点の確認が未実施でないか。旧改訂版のレビュー流用、指摘事項未修正の持ち越し、監査実行完了を対象の合格とみなしていないか

---

# 2. アーキテクチャモデル

## 2.1. 境界・決定権限・依存関係

システムコンテキストでは、システム、アクター、外部システム、信頼境界、主要なデータフローを示す。ドメイン部品／実行時部品ごとに責務、決定権限、依存方向、公開インターフェースを定義し、同じ責務を複数の実行時部品が暗黙に所有しないようにする。

外部サービス、提供側、プラットフォーム固有処理は、交換・隔離・テストの必要性がある場合に接続部／提供側境界へ閉じ込める。すべての依存関係へ形式的な抽象化を追加せず、交換可能性、リスク、テスト可能性、提供側固有制約から判断する。

別のRepositoryまたは共通基盤との関係では、意味、原則、規則等を参照する[コンテキスト依存](18_Context_Dependency.md)と、パッケージ、実行時部品、スキーマ等を使用する成果物依存を分ける。アーキテクチャは通常・推移依存を含む成果物依存の実現方式、実行時境界、失敗・回復、セキュリティ、運用を所有する。採用プロジェクトまたは組織の管理範囲で、同じ成果物の意味、契約、採用版または更新判断を、決定権限またはリリース単位が異なる独立管理利用側の間で共有し横断調整する必要がある成果物依存、または重大リスクがある成果物依存は、同書の管理対象へ昇格する。外部提供元とのAPI契約、決定権限の違いまたは独立リリースだけでは昇格させない。参照元コンテキストの意味、版、上書きの決定権限を技術依存へ置き換えない。

```text
外部利用側 -> インターフェース境界 -> ドメイン責務 -> データ決定権限
外部提供側 <- 接続部境界          <- ドメイン責務
```

## 2.2. データ・正本・状態遷移

データごとに担当責任者、正式な情報源、書き込み側、読み取り側、分類、保持期間、削除、整合性、トランザクション境界を定義する。複数システムを統合する場合、一つのシステム全体を常に正本とせず、意味のある項目または集約単位単位で正本を決める。

複製、キャッシュ、読み取りモデル、派生データ、AIによる推論を正本データと同一視しない。鮮度、古さ、競合、修正、再構築の扱いを定義する。

UIに表示する更新通知、差し替え、鮮度表現はUI / SPECが正本である。アーキテクチャはスナップショット / バージョン、通知経路、整合性、再取得、競合検出を成立させる。

## 2.3. インターフェースと統合契約

各API、IPC、イベント、ファイル、バッチ、外部接続について、利用側 / 提供側、データ契約、決定権限、同期性、提供、順序保証、エラー、タイムアウト、再試行、冪等性、バージョン、可観測性を適用範囲で定義する。

```text
要求・応答 = 呼び出し側が結果または受付結果を待つ
一方向メッセージ  = 呼び出し側が同期結果を待たずに送る
プッシュ / イベント     = 提供側がイベントまたは更新を通知する
一括処理 / ファイル  = 集合または時点単位で受け渡す
```

この分類は実装方式を固定するためではなく、所有責任、失敗、提供保証、利用側の振る舞いを曖昧にしないために使う。

## 2.4. 状態・処理順序・副作用

主要フローは、開始条件、アクター／契機、内部状態、実行時部品間の処理順序、永続化点、外部への副作用、成功・失敗・取消・回復を対応づける。SPECの状態とアーキテクチャ内部状態を区別し、内部都合の状態を利用者向けの意味として無断公開しない。

複数の入口が同じ副作用を起動する場合、共通の実行境界、冪等性キー、ロック、キュー、トランザクション等から適切な制御を選ぶ。常に単一ゲートへ集約するとは限らないが、入口ごとに防御が不一致になる構造は避ける。

## 2.5. 並行処理・失敗・回復

並行処理と失敗は、次の判断を一つの回復契約として扱う。

- 同時実行、再入、重複、古い書き込み、競合の発生条件
- 原子性境界と部分成功を許容する単位
- タイムアウト予算と層間の順序
- 再試行可能な失敗、再試行間隔、上限、冪等性
- 取消、ロールバック、補償、手動回復
- 安全な停止時の受付停止、処理中要求の排出、上限時間
- 依存関係停止、縮退モード、背圧制御、負荷遮断
- 回復後の内部状態、可観測性、再検証

部分成功を常に保持する、または常にロールバックするとは決めない。SPECの結果とデータ完全性に従い、原子性境界を設計する。再試行上限後の復帰を常に人間操作に限定せず、リスク、決定権限、回復可能性をSPECと整合させる。

不合格終了済みは実行許可、同意、安全、法的制約、不可逆操作等、判定不能時に許可側へ倒すリスクが高い境界へ適用する。一般的な表示状態や低リスクの可用性判定へ無条件に拡張せず、代替動作と利用者・利用側へ返す振る舞いをSPECと整合させる。

高コスト処理、バッチ、外部呼び出しでは、必要に応じて実行前に対象有無、適格性、割当上限を安価に判定する受入判定手順を設ける。対象なしの扱い、ログ / 指標 / UI通知の要否は、運用上可観測性と利用者へのフィードバックを損なわないよう対象範囲ごとに決める。

## 2.6. 品質・処理能力・インフラストラクチャ・運用

SPECの観測可能な品質条件と処理能力に関する振る舞いを、負荷前提、リソース、デプロイ、操作へ変換する。

利用者・組織が理解し変更する選好 / 方針 / 設定の意味と観測可能な振る舞いはIA、UI、SPECを正本とする。アーキテクチャは、それらを成立させる保存・配布・優先順位の技術方式と、環境変数、シークレット参照、提供側パラメータ、リソース規模等の技術構成を設計し、両者を無自覚に同じ設定体系へ混在させない。

- トラフィック、並行処理、データ / バッチ量、成長率、ピーク、季節変動
- 遅延、処理量、可用性、永続性、回復、コストの対象 / 制限
- 計算資源、メモリ、ストレージ、DB接続、キュー、キャッシュ、提供側の割当上限
- 自動拡縮、流量制限、背圧制御、負荷遮断、縮退モード
- リージョン、ネットワーク、失敗領域、バックアップ、復元、フェイルオーバー
- デプロイ、ロールバック、構成、シークレット、環境差分
- 指標、ログ、トレース、警告、運用手順の担当責任者、処理能力レビューの契機

過負荷時に何を守り、何を遅延・拒否・縮退するかはSPECへ戻して人間判断を得る。アーキテクチャは、その振る舞いを成立させるリソースと制御を設計する。

## 2.7. 互換性・移行・依存関係の発展

利用側から観測可能な互換性 / 移行期間の振る舞いは振る舞い仕様を正本とし、アーキテクチャは版管理、共存、移行、デプロイ、ロールバック、廃止順序を定義する。

API / インターフェースでは破壊的 / 非破壊を分類し、利用側一覧、複数バージョン共存、接続部 / 互換性層、機能フラグ、廃止条件を扱う。旧契約削除前に利用側移行と利用停止を根拠で確認する。

スキーマ変更は既存データとデプロイ順序を含め、必要に応じて拡張 / 契約、補完処理、二重読み取り / 書き込み、バックアップ、ロールバック、安全な再実行、部分失敗を設計する。

ライブラリ、実行環境、提供側、プラットフォームの追加・更新は、必要性、保守状況、ライセンス、セキュリティ勧告、支援期間、破壊的変更、移行方法、代替を確認する。

## 2.8. セキュリティ・プライバシー・AIのアーキテクチャ

セキュリティ、プライバシー、AIガバナンスは後付けの非機能項目ではなく、境界、データ、インターフェース、実行経路へ組み込む。

- 認証、認可、テナント、最小権限、機密情報
- 信頼境界、入力の来歴、外部データと指示の分離
- 許可リスト、スキーマの妥当性確認、ツール / 外部操作権限
- 同意、目的、データ最小化、提供側送信、リージョン、学習利用
- 観察済み事実、AIによる推論、人間確認済み情報の分離
- 保持、削除、訂正、同意の撤回、バックアップ / ログ
- 出力妥当性確認、人間の承認、比率 / コスト制限、監査、緊急停止

プロンプトの注意書き、UIの同意表示、リリース直前のマスキングだけをセキュリティ境界とみなさない。バッチ、キュー、再試行、管理者パスを含むすべての実行経路で同じ決定権限とデータ境界を強制する。

## 2.9. 実装・コーディング規約の決定権限

プロジェクト固有のコーディング規則と実装規則の正本Markdownは`06_Architecture`へ置く。アーキテクチャは規則の意味、適用対象範囲、理由、禁止事項、例外承認、検査方法を定義する。

```text
06_Architecture/
├─ システム / データ / インターフェースアーキテクチャ
├─ セキュリティ / 運用 / 移行アーキテクチャ
└─ コーディング / 実装規則
```

コード、構成、移行、開発者テスト、ビルド成果物、およびリンター / フォーマッター / 静的解析ツール等の実行可能な強制手段は実装が所有する。アーキテクチャは具体的なテスト場合を所有せず、テスト可能性を確保する境界、必要環境、失敗 / 負荷 / 移行等の検証義務を渡す。

### UIと視覚表現の成立方式

UIがUIテーマ、再利用UI部品／UI設計パターン、または外部視覚成果物との改訂版対応を要求する場合、アーキテクチャは次の成立方式を適用範囲で定義する。対象範囲が視覚的な論理画面を持つ場合は、これらの要求の有無にかかわらず、デザインシステム参照実装に関する項目を定義する。

- 基本要素／意味／部品層のデザイントークンの関係
- UIテーマ差分軸と技術モード、既定値、永続化、代替動作の対応
- UI部品契約と実行時部品／項目インターフェース／UI部品状態／UI差分（UI Variant）の対応
- 論理画面／経路／画面領域、レスポンシブ対応 UI差分、UI素材、モーションの実装境界
- 外部視覚成果物の改訂版、デザイントークン／UI部品バージョン、ビルド改訂版の追跡と比較可能性
- デザインシステム参照実装の改訂版、対象プラットフォームへの変換方式、ビルド改訂版との追跡と比較可能性
- UIテーマ、UI部品、UI設計パターン変更時の互換性と移行

Figmaモード、CSS変数、デザイントークン形式等の特定ツールをCRDD共通方式として固定しない。UIが所有する視覚表現方針、UIテーマの意味、UI部品の利用規則をアーキテクチャ都合で変更しない。成立不能またはコスト / 性能 / プラットフォームの制約はUI / SPECへフィードバックする。

UIテーマ、共通UI部品、外部視覚成果物を使用しない対象範囲では、本責務を理由付きで`Not Applicable`にできる。ただし視覚的な論理画面を持つ対象範囲では、デザインシステム参照実装の改訂版と対象プラットフォームへの変換方式を`Not Applicable`の対象に含めない。視覚的な論理画面を持たない対象範囲の扱いは[UI](25_UI.md#design-system-reference)を正本とする。

## 2.10. 表現・判断・追跡

アーキテクチャはテキスト、表、スキーマ、図を、関係を最も明確に表す最小の形式で使う。図だけを正本にせず、境界、規則、制約、判断理由をテキストで追跡可能にする。複雑な構造や連番ではMermaid等の差分管理可能な図を利用してよい。

アーキテクチャへCRDD標準安定コンテキストIDを新規発行しない。情報源 `REQ-*` / `UX-*` / `IA-*` / `UI-*` / `SPEC-*`、変更トレース、実装、検証とは成果物参照と関係で接続する。

根拠はアーキテクチャ成果物内または最も近い親フォルダの`Evidence/`へ置く。選択結果はアーキテクチャの正本成果物の`Decision / Rationale`へ、コンテキスト、選択、理由、代替案、根拠、トレードオフ、影響、再評価契機を残す。全判断をルート直下の台帳へ複製しない。

---

<a id="3-guided-skill-adapter"></a>

# 3. スキル実行接続部（Guided Skill Adapter）

<a id="31-runtime-authority"></a>

## 3.1. 実行時の決定権限（Runtime Authority）

`skill.architecture.integrate`は、本書の工程実行契約を[`11_Skill.md`](11_Skill.md)の実行の状態遷移、ガイド付き対話、人間によるレビュー、引き渡しに従って実行するアーキテクチャ固有接続部である。本書では実行状態、一時停止 / 再開、共通の質問規則、サブエージェントの状態遷移を再定義しない。

## 3.2. アーキテクチャ固有の進行

| 手順 | 変換 | 出力 |
|---|---|---|
| 負荷 | UI / SPEC、品質、既存システム、制約を対応づける | アーキテクチャの網羅キュー |
| フレーム | システム／信頼境界、アクター、データ、外部依存関係を定める | システムコンテキスト |
| 構造 | ドメイン、実行時部品、データ、インターフェース、内部状態／連番を設計する | 構造契約 |
| 強化 | セキュリティ、失敗、並行処理、互換性、処理能力、操作を判定する | 運用上アーキテクチャ |
| 有効化 | 実装規則、テスト可能性、検証義務を定める | 実装への引き渡し |
| レビュー | 網羅範囲、判断、不足、リスクを人間が確認する | 承認済み / 一部完了 / 停止中 |

質問数や文書分割を進捗とみなさず、必要な責務の網羅と対象範囲で進捗を判定する。

## 3.3. 対話による明確化

専門用語を要求する前に、人間が答えられる言葉で不足コンテキストを確認する。

| 話題 | 質問の意図 |
|---|---|
| 境界 | どこで動き、誰・何と接続する必要があるか |
| データ | 誰のデータを、どこへ、どの期間保存できるか |
| 決定権限 | 誰が読み書き・承認・外部実行できるか |
| 失敗 | 外部サービスや処理が停止したとき何を守るか |
| 完全性 | 失敗しても失ってはいけないデータや操作は何か |
| 発展 | 将来交換したいもの、既存運用で変えられないものは何か |
| 品質 | 利用規模、応答時間、可用性、復旧、コストの目安は何か |
| 操作 | 誰が異常を知り、停止・復旧・ロールバックするか |

回答を境界、データ、インターフェース、失敗、セキュリティ、品質、操作、移行、処理能力へ変換する。上位契約を弱める必要がある場合はアーキテクチャ内で確定しない。

## 3.4. 上位判断への移送とサブエージェントの使用

次の場合は該当決定権限へ戻す。

| 条件 | 移行先（Route） |
|---|---|
| 情報源振る舞い、品質条件、決定権限が不明 | SPEC / 人間の判断 |
| UIと実現可能な振る舞いが競合 | UI／SPECの対応レビュー |
| データ利用目的、同意、保持期間が不明 | 課題探索・要求形成 / 人間の判断 |
| 技術検証が必要 | アーキテクチャ技術検証 / 根拠取得 |
| リスク、コスト、互換性破壊の受容が必要 | 人間の決定権限 |
| アーキテクチャは成立するが実装方法の選択が主題 | 実装 |

サブエージェントを使う場合は[`10_Agent.md`](10_Agent.md)に従い、データ、セキュリティ、処理能力、移行、提供側比較等の限定対象範囲を委譲できる。アーキテクチャ全体の整合、判断、網羅範囲、引き渡しは親エージェントが統合し、人間確認を得る。

---

<a id="4-review-handoff-view-and-feedback"></a>

# 4. レビュー・引き渡し表示・フィードバック

## 4.1. アーキテクチャ固有の人間レビュー

人間レビューでは少なくとも次を確認する。

- 上位意図と振る舞いを技術都合で弱めていない
- 境界、決定権限、データ所有関係、正本が一意または競合管理されている
- 失敗、セキュリティ、プライバシー、互換性、処理能力のリスクを隠していない
- アーキテクチャ設計パターンと技術選択にコンテキスト、代替案、トレードオフがある
- 操作、移行、ロールバック、検証を実装後回しにしていない
- プロダクト設定と技術構成を区別し、成立方式と決定権限を実装へ渡している
- 一部完了 / `Not Applicable`の対象範囲、理由、担当責任者が明確である

## 4.2. アーキテクチャ成果物／引き渡し表示

`06_Architecture/01_Architecture.md`をアーキテクチャ工程の固定入口とする。対象範囲について、次を入口内または入口から辿れる正本成果物で参照可能にする。適用の深さでは入口や基本のファイル分割を変えず、記述、レビュー、根拠の深さを調整する。図、技術規則または外部成果物を分ける場合も、入口から決定権限、改訂版、網羅状態、未解決事項へ到達できるようにする。

```text
対象範囲 / 網羅範囲要約 / 未解決不足
情報源となるUI / SPEC / 品質要件 / 制約
システムコンテキスト／信頼境界
ドメイン / 部品責務
データ / 正式な情報源 / 状態遷移
インターフェース / 統合 / 外部依存関係
状態 / 順序 / 並行性 / 回復
セキュリティ／プライバシー／AI境界
品質 / 処理能力 / インフラストラクチャ / 運用
技術構成／プロダクト設定境界
UI / 視覚表現の実現 / UIテーマ・トークン・部品の対応表 / 視覚成果物・ビルド改訂版
互換性 / 移行 / ロールバック
実装 / コーディング規則
検証義務
判断 / 判断理由 / 根拠 / 人間レビュー
変更影響の伝播確認結果 / 情報源の改訂版 / 上流更新 / 下流再探索 / 伝播例外
工程移行レビュー結果 / レビュー済み改訂版 / 指摘事項処置 / レビュー例外
```

実装への引き渡しはこの表示を縮小再掲して受信条件を減らさず、アーキテクチャの正本成果物への参照と網羅状態付きで渡す。

## 4.3. アーキテクチャへのフィードバック

実装、検証、操作、インシデント、依存関係更新から、成立不能、性能差、失敗モード、セキュリティ不足、移行リスク等が得られた場合はアーキテクチャへ戻す。

同じ制約下の明確化は既存成果物を更新する。境界、決定権限、データの意味、主要技術、移行方針等を置換する場合は、旧判断を消さず、新しい判断 / 判断理由と成果物参照で履歴を接続する。

アーキテクチャで得た学びがSPECやUIの観測可能な振る舞いを変える場合は、アーキテクチャだけを更新せず、該当工程を再開する。

---

<a id="5-final-principle"></a>

# 5. 最終原則（Final Principle）

アーキテクチャは図やアーキテクチャ設計パターンを作って終わる工程ではない。

上位コンテキストを実現可能な境界、契約、制約、規則へ変換し、実装と検証が迷わず実行・検証できる状態を作る。
