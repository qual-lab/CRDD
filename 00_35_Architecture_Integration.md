# Architecture and Integration

Version: v0.3.0
Status: Stable
Owner: Qual-Lab
Last Updated: 2026-07-16
Related:
- [00_32_Testing_Quality.md](00_32_Testing_Quality.md)
- [00_34_Compatibility_Evolution.md](00_34_Compatibility_Evolution.md)
- [00_30_Product_Documentation.md](00_30_Product_Documentation.md)

---

> このドキュメントはPractice Guide（推奨知見）である。

---

# 1. Purpose

本ドキュメントは、システム構成をどう表現するか（設計表現規約）、UI・バックエンド・外部システムをどう結合するか（結合観点）、同時実行・輻輳にどう備えるか（耐障害性）の基本原則を定義する。

---

# 2. Basic Principle

```text
設計は、図ではなく、AIと人間が読めるテキストと表で表現する。
結合点は、契約（Contract）として明示的に定義する。
同時実行・過負荷は、起きる前提で設計する。
```

Architectureは「どう実現するか」を扱う。
「何が起きるべきか」は `04_Spec` に置き、「どう見えるか」は `05_UI` に置く。
Architecture文書が機能仕様や画面文言を持ち始めた場合は、該当箇所を `04_Spec` または `05_UI` へ分離し、Architecture側からリンクする。

---

# 3. Design Representation Conventions

CRDDでは、UML等の専用ツールを前提とした形式図を必須としない。テキストベースの表現を基本とする。理由は、[`00_15_Document.md`](00_15_Document.md)が定めるAI可読性・Git差分での追跡可能性を優先するためである。

## System / Component Structure

ブロック図は、テキストの矢印で表現する。

```text
【外部システム】
    ↓
【入力処理層】
    ↓
【中核データストア】
    ↓
【実行時サービス】
    ↓
【UI】
```

## Sequence（処理の時系列）

正式なシーケンス図の代わりに、「送信元 → 送信先: 内容」の簡易記法を使う。

```text
UI → Backend: リクエスト送信
Backend → UI: 受付応答（即時）
Backend → UI: 完了通知（非同期）
```

## State Transition（状態遷移）

状態遷移図の代わりに、状態と遷移条件を表で表現する。

| 現在の状態 | イベント | 次の状態 |
|---|---|---|
| Draft | 保存 | Active |
| Active | 一定期間未更新 | Archive |
| Archive | 再アクティビティ | Active |

## Data Model

クラス図・ER図の代わりに、スキーマ定義（DDL）または構造化された表でデータ構造を表現する。

## When Formal Diagrams Are Still Useful

テキスト表現で伝わらない複雑な構造（多数のコンポーネントが複雑に絡み合う場合等）では、Mermaid等のテキストベースの作図記法（コードとして差分管理できるもの）を補助的に使ってよい。バイナリ形式の作図ツール（Visio等）の成果物のみを正本にすることは避ける。

---

# 4. Integration Design

## UI ⇄ Backend ⇄ External System

結合点は、以下の3分類のいずれかとして設計する。

```text
Request-Response  = 呼び出し側が応答を待つ（同期的な要求応答）
Fire-and-Forget   = 呼び出し側が結果を待たない一方向通知
Server-Push       = 提供側が任意のタイミングで呼び出し側へ通知する
```

## Rule

```text
結合点ごとに、上記3分類のどれに該当するかを明示する
エラー発生時の伝播方法（呼び出し側にどう返すか）を結合点ごとに定義する
将来の移植・置き換えを見据え、ビジネスロジックとプラットフォーム固有の実装（通信方式等）を分離する
```

## Spec / UI / Architecture Contract

結合点を定義する場合、以下を分けて記録する。

| Layer | Owns |
|---|---|
| `04_Spec` | 入力、出力、状態、例外、受け入れ条件 |
| `05_UI` | 表示、操作、空状態、エラー文言、ユーザー導線 |
| `06_Architecture` | API、IPC、DB、処理順序、イベント通知、Provider境界 |

Architectureは、SpecやUIを上書きしない。
Architecture側で新しい制約が見つかった場合は、SPEC / UIへフィードバックし、Architecture Artifactへ結果を反映してDecision / Rationale Sectionに理由、Evidence、経緯を残す。

## External System Connector Boundary

外部システムとの連携を追加する場合、既存の中核処理に影響を与えない形で追加できるよう、安定したインターフェース（契約）を定義する。

```text
新しい外部連携を追加しても、既存の処理層・分析層のコードを変更せずに済む構造にする
外部連携の実装は、共通のインターフェース（例: データ取得・正規化の関数シグネチャ）に従う
```

## Field-Level Source of Truth

複数システムを統合する場合、どちらか一方のシステムをまるごと「正」とは扱わない。データ項目（フィールド）ごとに、どちらのシステムが真実源かを個別に割り当てる。

### Rule

```text
統合する項目ごとに、どちらのシステムが真実源かを個別に決める
一方のシステムを常に優位に置く暗黙のルールを作らない
真実源ではない側のシステムが持つ同名の項目は、キャッシュ・参照用コピーとして扱い、
書き込みの正本にしない
```

### Bad

```text
2つのシステムを統合する際、「システムAを常にマスタとする」と決めてしまう。実際には
一部の項目はシステムBの方が正確・最新であり、統合後にシステムB側の更新が反映されなくなる。
```

### Good

```text
どの項目をどちらのシステムが真実源として持つかを項目ごとの表として明示する。例えば
「進行状態」はシステムA、「最終更新日時」はシステムBが真実源、というように分担する。
```

## Adapter / Provider Pattern

外部サービス（AI Provider、決済サービス等）への依存は、差し替え可能な抽象化（Adapter）越しに扱う。

```text
Good: PaymentProvider（インターフェース）→ StripeProvider / MockProvider（実装）
Bad: 業務ロジックの中に特定サービスのSDK呼び出しが直接埋め込まれている
```

## Bad

```text
外部APIの呼び出しコードを、機能ロジックのあちこちに直接埋め込む。
テスト時にも実際の外部APIを叩く必要がある。
```

## Good

```text
外部APIの呼び出しを1つのAdapter/Providerに集約し、テスト時はMock実装に差し替えられるようにする。
```

## Read-Model Freshness Contract

UIが表示するデータは、取得した時点のスナップショットとして扱う。バックグラウンドの更新通知を受け取っても、画面に表示中のデータを黙って差し替えない。

### Rule

```text
背景更新は「更新があります」という明示的な合図として扱い、ユーザーの操作なしに表示内容を置き換えない
ユーザー自身の操作（保存・送信等）の結果は、合図を待たず即座に反映する
更新通知は「内容が変わった（利用者が気づくべき変化）」と「内部状態だけが変わった（バッジ等の
軽い表示更新のみでよい）」を区別して送る
内部状態の変化まで毎回「内容が変わった」扱いにすると、通知疲れを招く
```

### Bad

```text
一覧画面を開いている間にバックグラウンド処理が新しいデータを生成すると、画面が何の断りもなく
表示内容を差し替える。ユーザーは読んでいた内容が急に変わったことに戸惑う。
```

### Good

```text
バックグラウンド処理が新しいデータを生成した場合、画面は「新しい更新があります」という
バナーだけを表示し、ユーザーが明示的に更新を選ぶまで表示内容は変えない。ただし、内部の
処理状態（再試行回数のリセット等）だけが変わった場合は、バッジ表示の更新のみに留め、
バナーは出さない。
```

---

# 5. Concurrency, Contention & Resilience

## Self-Guarding Against Re-entry

定期実行・バッチ処理は、同じ処理が二重に動かないよう自己防御する。

```text
実行中フラグを持ち、実行中に同じ処理の再実行要求が来たらスキップする
```

## Graceful Shutdown Completes Cleanup Before Exit

プロセス終了要求を受けても、非同期のクリーンアップ（ロック解放、タイマー破棄、保留中の書き込みの完了・フラッシュ等）が終わる前にプロセスを終了しない。

### Rule

```text
終了要求は即座にプロセスを落とさず、まず新規処理の受付を止める
実行中の処理・保留中の書き込みが完了する（またはタイムアウトする）まで待ってから終了する
クリーンアップの完了を待たずに終了すると、ロック・タイマー・不完全な書き込みが残存する
クリーンアップ待機には上限を設け、無限に終了を引き延ばさない
```

### Bad

```text
ウィンドウを閉じる操作をそのままプロセス終了に直結させる。実行中だったバッチ処理や
書き込み中だったデータが、完了しないままプロセスごと消える。
```

### Good

```text
終了要求を受けたら、まずタイマーを止めて新規実行を止め、実行中の処理の完了を最大数秒
待ってから、リソースを解放してプロセスを終了する。待機中に完了しない場合も、上限時間で
打ち切って終了する。
```

## Unified Execution Gate

手動操作・自動スケジュール・テスト/シミュレーション実行など、同じ重い処理を起動しうる複数のトリガーがある場合、すべてのトリガーを同じ実行ゲート（単一のスケジューラ・単一の実行判定関数等）へ集約し、処理エンジン自体を複数の入口から直接呼ばせない。

### Rule

```text
手動トリガーは「次の実行機会にこの処理を行う」という要求フラグを立てるだけにし、処理エンジンを直接呼び出さない
自動実行・手動実行・テスト実行は、同じ実行ゲートを通ることで、多重実行防止・ログ記録・状態通知の扱いを一本化する
実行ゲートを迂回するサイドドア（緊急対応目的の直接呼び出し等）を作らない。迂回が必要になった場合は、
その迂回自体を新しい正式なトリガー種別として実行ゲートに組み込む
```

### Bad

```text
定期実行される処理と、ユーザーがボタンを押したときの処理を、別々のコードパスとして実装する。
両方が同時に走ると、多重実行防止のロックが片方にしか効いていないことに後から気づく。
```

### Good

```text
定期実行もユーザーの手動操作もテスト実行も、同じ実行ゲートへ「実行要求」を渡すだけにする。
実行ゲート内の多重実行防止・ログ記録・状態通知は一箇所に実装されているため、トリガーの
種類が増えても防御漏れが起きない。
```

## Peek Before Act

重い処理（AI呼び出し、バッチ処理等）を起動する前に、「そもそも処理対象があるか」を安価に確認する。対象が無ければ、本処理はもちろん、開始・終了のログやUIシグナルも出さない。

### Rule

```text
実行ゲートは、実際の処理本体を呼ぶ前に、対象の有無を判定する軽い確認ステップを持つ
対象が無い場合は、処理を静かにスキップする。開始/終了イベント・ログ・UI通知も発生させない
「今回は対象なしでした」というノイズの蓄積を避ける
```

### Bad

```text
定期実行のたびに重い処理を起動し、内部で対象が無いことを確認してから何もせず終了する。
その都度「開始しました」「終了しました」のログとUI表示が空振りで発生し続ける。
```

### Good

```text
定期実行の入口で、まず対象の有無だけを安価に確認する。対象が無ければ、ログもUI通知も
出さずに何もしない。対象がある場合だけ、開始イベントを出してから本処理を呼ぶ。
```

## Fail-Closed Status Resolution

ある状態（利用可否、権限の有無等）を判定するために必要な読み取り処理自体が失敗した場合、判定不能を「許可」側ではなく「制限」側の結果として扱う。

### Rule

```text
状態判定に必要な読み取りが失敗した場合、より制限的な状態にフォールバックする
「読み取れなかったので、とりあえず許可する」を既定にしない
これはセキュリティに限らず、業務上の利用可否・機能の有効/無効等、あらゆる状態判定に適用する
```

セキュリティ境界に限定した同種の原則は[`00_33_AI_Governance_Security.md`](00_33_AI_Governance_Security.md)「Security Boundaries Are Default-On」を参照する。本節はそれをセキュリティ以外の状態判定一般へ拡張したものである。

### Bad

```text
ある機能が使えるかどうかを判定するためのデータ読み取りがタイムアウトした場合、
例外を握りつぶして「使える」という既定値を返してしまう。
```

### Good

```text
状態判定に必要なデータの読み取りが失敗した場合は「使えない」側にフォールバックし、
読み取りが復旧するまでその機能を制限する。
```

## Coordinated Timeouts

複数レイヤーにタイムアウト設定がある場合、それぞれが独立に競合しないよう調整する。

## Bad

```text
通信ライブラリのデフォルトタイムアウト（例: 300秒）と、アプリ側で設定した独自タイムアウト（例: 60秒）が
それぞれ独立に動作し、意図しないタイミングでエラーが発生する。
```

## Good

```text
通信ライブラリのタイムアウト設定を明示的にアプリ側のタイムアウトより長く（または短く）設定し、
どちらが先に発火するかを意図的に決める。
```

## Retry & Backoff

失敗した処理を再試行する場合、間隔を段階的に広げる。

```text
1回目の失敗 → 即時 or 短い間隔で再試行
再試行を重ねるごとに間隔を広げる（例: 15分 → 1時間 → 6時間）
一定回数を超えたら再試行を打ち切り、人間が介入できる状態にする
```

### Retry Gate State Shape

再試行が上限に達して打ち切られた状態は、単なるエラー表示で終わらせず、明示的な状態として扱う。

```text
none/pending → processing → 成功 または blocked（再試行上限到達）
blocked状態からの復帰は、人間の明示的な操作でのみ行う
復帰操作は「成功したことにする」のではなく、「再度処理対象に戻す」だけに留める
```

### Bad

```text
再試行上限に達した処理を、内部的には失敗のまま放置し、UI上は単なるエラーメッセージだけを
表示する。ユーザーがそのエラーを解消する手段がない。
```

### Good

```text
再試行上限に達した処理はblocked状態としてUIに表示し、「再試行する」操作を用意する。
その操作は処理を再度キューへ戻すだけであり、成功を保証しない。次に失敗すれば再び
blocked状態に戻る。
```

## Partial-Success Batch Isolation

複数の処理単位をまとめて扱うバッチ処理が部分的に失敗した場合、失敗した単位のせいで成功した単位まで巻き戻さない。

### Rule

```text
バッチの一部が失敗しても、成功した単位の結果は保持する
失敗した単位だけを個別に再試行対象とする
バッチ全体を「成功」か「失敗」かの二値で扱わない。単位ごとの結果を追跡する
```

### Bad

```text
10件のうち1件の処理が失敗すると、バッチ全体をロールバックし、成功していた9件の結果も
失われる。次回は10件すべてを再実行する。
```

### Good

```text
10件のうち1件が失敗した場合、成功した9件の結果は確定させ、失敗した1件だけを次回の
再試行対象として個別に扱う。
```

## Congestion / Backpressure

同時に大量のリクエスト・処理が集中した場合（輻輳）への備えも設計段階で検討する。

```text
処理能力を超えるリクエストをそのまま受け入れず、キューイングまたは拒否（Rate Limiting）する
新しいリクエストを無制限に受け付けず、同時実行数に上限を設ける
過負荷時は、優先度の低い処理を後回しにする（Load Shedding）か、明示的にエラーを返す
```

この節は、CRDD自身にもまだ実践知見が少ない領域である。導入する際は、プロダクトの規模・負荷特性に応じて具体化する。

---

# 6. Minimum Rule

最低限、以下を守る。

```text
設計はテキスト・表で表現し、バイナリ形式のみの図に依存しない
結合点はRequest-Response / Fire-and-Forget / Server-Pushのいずれかに分類する
Spec / UI / Architectureの責務を混在させない
外部サービス依存はAdapter越しに扱う
UIは背景更新を明示的な合図として扱い、ユーザーの操作なしに表示内容を差し替えない
同じ処理を起動しうる複数のトリガーは、同じ実行ゲートへ集約する
バッチ処理は二重起動しない設計にする
終了要求を受けても、クリーンアップが終わるまでプロセスを落とさない
再試行上限到達は明示的な状態として扱い、復帰は人間の操作でのみ行う
バッチの部分失敗は、成功した単位を保持し、失敗した単位だけを再試行する
複数レイヤーのタイムアウトは競合しないよう調整する
```

---

# 7. Final Principle

設計は、描いて終わりではなく、AIと人間が繰り返し参照し、実装と照合できる形で残す。

結合点と同時実行への備えを明示的に設計することで、システムは「動いているうちは気づかない」問題を未然に防げる。
