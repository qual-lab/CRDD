# IA分析: 許可された作業領域だけをリモート利用する

成果物種別: IA分析
分析対象: [UX-000013](../../../02_UX/Definitions/UX-000013/ux_definition.md)
状態: 分析済み

## 1. UXから受け取る意味

| 観点 | この分析で受け取る内容 |
|---|---|
| 利用者 | プロジェクト運営者／PM |
| 場面 | リモート接続を開始・再接続する時 |
| 目的 | 許可された作業領域だけへ接続する |
| 得たい結果 | 場所が変わっても開示範囲を理解して安全に使える |
| 重要場面 | 利用可能情報を表示する時 |
| 避ける失敗 | 利用不能なリポジトリの存在や内容を推測表示する |
| 守る品質 | 現在の利用許可範囲（Grant）だけを開示し不足を補完しない |

画面、API、保存形式は決めず、上記の利用者成果を情報の単位と関係へ変換する。

## 2. 情報候補と関係

| 情報Object | 利用者にとっての意味 | 同一性と関係の基準 |
|---|---|---|
| 接続資格 | 接続を認証する資格 | 接続資格識別子（Credential ID） |
| 接続中の作業単位 | 一回の接続文脈 | 接続単位識別子（Session ID） |
| システム管理能力 | サーバー（Server）の管理能力 | 接続中の作業単位（Session）へ別途結合 |
| 利用可能領域 | 接続中の作業単位へ発行された作業領域集合 | Session IDとWorkspace IDの組で識別する |
| 作業領域 | Repositoryを公開する論理的なまとまり | Workspace IDで識別する |
| 公開関係 | Repositoryを作業領域へ公開する関係 | Workspace IDとRepository IDの組で識別する |
| リポジトリ | 公開関係の先にあり、現在の接続で利用可否を判断する正本境界 | Repository IDで識別する |

```text
[O: 接続資格]
      │ 接続を作る
      ▼
[O: 接続中の作業単位]
      ├── 別に付与される場合がある ──▶ [O: システム管理能力]
      │
      └── 発行される ──▶ [O: 利用可能領域]
                               │ 利用を許す
                               ▼
                         [O: 作業領域]
                               │ Repositoryを公開する
                               ▼
                         [O: 公開関係]
                               │ 公開対象を結ぶ
                               ▼
                         [O: リポジトリ]
```

図中の`[O:]`は情報Objectだけを表す。利用者、判断行為、利用者成果はObjectへ置き換えない。関係名と向きは、同じ対象を追跡し、誤った統合を避けるための情報上の関係であり、画面遷移や実装依存を表さない。

### Canonical化候補

| 接続先 | 分析Object | Canonical Object | 処置 | 判断理由 |
|---|---|---|---|---|
| IA-000009 | 接続資格 | 接続資格 | Same | 接続を認証する資格として保持する |
| IA-000009 | 接続中の作業単位 | 接続中の作業単位 | Same | 一回の認証済みSessionとして保持する |
| IA-000009 | システム管理能力 | 管理能力 | Rename | 内容閲覧Grantとは別のServer管理能力として保持する |
| IA-000009 | 利用可能領域 | 利用可能領域 | Same | Sessionに発行されたWorkspace集合として保持する |
| IA-000009 | 作業領域 | 作業領域 | Same | Repositoryを公開する論理的まとまりとして保持する |
| IA-000009 | 公開関係 | 公開関係 | Same | WorkspaceとRepositoryの公開関係として保持する |
| IA-000009 | リポジトリ | リポジトリ | Same | 現在の接続で利用可否を判断する正本境界として保持する |

この対応は分析結果からCanonical定義を再構築するための候補である。`Split`または`Merge`でも元の利用者向け意味を失わず、正式入力にないObjectを追加しない。

## 3. 状態・可視性・導線・責任

| 観点 | 分析結果 |
|---|---|
| 状態 | 利用可能（available）／接続資格が必要（credential_required）／開示制限（restricted）／利用不能（unavailable）／不明（unknown） |
| 可視性 | 「利用可能情報を表示する時」では、現在の利用許可範囲（Grant）だけを開示し不足を補完しないために必要な状態と不足を判断対象と同じ文脈で示す。詳細な識別情報と根拠は必要時に辿れるようにし、非開示対象は存在自体を推測表示しない。 |
| 導線 | 接続資格→接続中の作業単位→利用可能領域→作業領域→公開関係。システム管理能力は接続中の作業単位から別に辿る。結果なし、判断待ち、失敗または状態不明の場合も、根拠か安全な戻り先を失わない。 |
| 責任 | Credential・Workspace・Exposure・System Capabilityを構成する責任と、Session・Workspace Grantを発行する責任を分ける。 |
| 時間的な意味 | 利用可能（available）／接続資格が必要（credential_required）／開示制限（restricted）／利用不能（unavailable）／不明（unknown）の区別を保持する。現在値、古い値、履歴または不明を同じ正常状態へ丸めない。 |
| 情報の優先度 | まず「許可された作業領域だけへ接続する」の判断に必要な対象・状態・判断要否を示し、識別情報と根拠は必要時に辿れるようにする。 |
| 情報のまとまり | プロジェクト運営者／PMが「リモート接続を開始・再接続する時」に「場所が変わっても開示範囲を理解して安全に使える」を得るための対象・状態・根拠・次の行動を同じ文脈で扱う。 |
| 判断権限 | Repository正本を管理する主体がExposureを、CROS接続設定を管理する主体がCredential Grantを、利用者がSession内の次行動を決める。利用者は提示された情報から次の行動を選ぶが、正本の意味・状態を無断で変更しない。 |
| 重要な失敗 | 利用不能なリポジトリの存在や内容を推測表示する |
| 制約・対象外 | 画面、UI部品、API、DB、Class、保存形式および具体的な振る舞いをIAで確定しない。 |
| 人間判断 | UXから継承した未確認事項はプロジェクト運営者／PMを代表する利用者とQual-Labが判断する。IA固有の追加判断はない。正式入力にないObject、状態または責任を追加する必要が生じた場合は人間の決定権限者へ戻す。 |
| IAへ戻す条件 | UI／SPEC分析またはQuality Analysis / IAで、Object・同一性・関係・状態・可視性・時間的な意味が不足または競合すると判明した場合。 |
| 検証意図 | 「利用可能情報を表示する時」に、利用不能なリポジトリの存在や内容を推測表示することを避け、現在の利用許可範囲（Grant）だけを開示し不足を補完しないことを確認できる必要がある。 |

ここで示す主体は、情報契約上必要な機能責任を表し、特定の人物・組織・Componentへの割当を確定しない。後続工程は、この責任境界を保ったまま実際の主体へ割り当てる。

### 未確認事項と判断

| 区分 | 内容 |
|---|---|
| UXから継承する確認事項 | REQ-000011: プロジェクト運営者／PMが「許可された作業領域だけへ接続する」を行う際の判断基準、許容負担、利用環境および失敗後の選択 |
| 判断者 | プロジェクト運営者／PMを代表する利用者とQual-Lab。 |
| 現在判定 | 後続の実利用確認が必要。現在のUX定義をCanonical化する判断を止める事項ではない。 |
| 未確認時の影響 | 利用者成果、重要場面、失敗および品質期待を仮説として保持し、定量条件や実現方式を確定しない。 |
| IAで追加した未確認事項 | なし。正式入力にない定量条件、状態値または実現方式を追加しない。 |
| IA固有の追加人間判断 | なし。UXから継承した判断をIAで確定済みにしない。 |
| Discoveryへ戻す条件 | 想定した利用者、問題、望ましい変化または制約が誤っていると判明した場合。 |

## 4. 現実照合の参考情報（正式入力ではない）

この節は後続のReality Auditへ引き継ぐ参考情報であり、IA Candidateを導く正式入力ではない。前節までをUX Definitionから再導出した結果として優先する。


| 照合対象 | 結果 |
|---|---|
| UXから導いた意味 | 対象・状態・導線は前節までを正とする。 |
| 現行構造 | CROS Architectureに設計候補がある。システム管理能力（system administration）を内容閲覧へ流用しない。 |
| 採用境界 | 現行の名称や状態値は、UX由来の意味と一致する範囲だけを照合根拠にする。実装にだけ存在する区分はIA定義へ逆流させない。 |

## 5. IA処置

[IA-000009](../../Definitions/IA-000009/ia_definition.md)へ接続する。上記7軸を接続先へ保持し、複数UXを統合する場合も対象固有の状態、失敗、導線を消さない。UXから継承する未確認事項を、判断者と影響を含めて接続先へ保持する。

## 6. 後続工程が保持する意味

| 接続先 | 保持する意味 |
|---|---|
| UI（UX＋IAの正式入力） | 情報の優先度、可視性、状態差、まとまり、見つけ方および関係の理解を保持する。 |
| SPEC（UX＋IAの正式入力） | 対象の識別、関係、状態、可視条件、責任および時間的な意味を保持する。 |
| Quality Analysis / IA（伴走） | 「場所が変わっても開示範囲を理解して安全に使える」の成立条件、「利用不能なリポジトリの存在や内容を推測表示する」という失敗および前章の検証意図を保持する。 |

ArchitectureやSourceへ直接引き渡さない。UI／SPECは、UX DefinitionとIA Definitionの双方を正式入力として分析する。

## 7. 補足分析

なし。必須情報は第1章から第7章に保持する。現行文書・実装との比較は第4章の参考情報として分離し、Canonical IAの根拠には用いない。

## Checklist

- [x] 正式入力となるUX Definitionを一件だけ特定した
- [x] UXの利用者成果と重要な失敗を保持した
- [x] 情報候補と利用者にとっての意味を特定した
- [x] 同じ情報と異なる情報を識別する条件を処置した
- [x] 情報同士の関係を処置した
- [x] 全Canonical Object候補を分析Objectへ対応付け、暗黙の改名・分離・統合を残していない
- [x] Identity／RelationをCanonical側で再解釈させない変換根拠を残した
- [x] 状態と可視性を処置した
- [x] 時間的な意味を処置した
- [x] 情報の優先度・まとまり・見つけ方を評価した
- [x] 情報の責任者と判断権限を分けて評価した
- [x] 機能責任と実際の人物・組織・Componentへの割当を区別した
- [x] 欠損・誤認・古さ・競合・曖昧性を評価した
- [x] 人間判断の必要性を評価した
- [x] UXから継承する未確認事項・判断者・影響とIA固有事項を区別した
- [x] UI・SPEC・Quality Analysis / IAへの接続を区別した
- [x] 情報構造の検証意図を評価した
- [x] 画面・Component・DB・API・Classを先取りしていない
- [x] 現行UI・Architecture・Sourceから意味を逆輸入していない
- [x] 補足分析へ必須情報を退避していない
