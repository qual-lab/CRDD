# IA-000001のSPEC分析

成果物種別: SPEC分析（IA観点）
分析単位: `IA-000001`
状態: 分析済み

## 1. 正式入力

- IA定義: [IA-000001 検査対象・条件・指摘](../../../03_IA/Definitions/IA-000001/ia_definition.md)

UX、UIまたはREQを直接読んで不足を補完しない。この分析はIA定義から、振る舞いが区別すべき情報、状態、関係、可視性、導線、責任を導く。

## 2. 利用場面ごとに保持する意味

機械的不備を意味判断から分け、修正すべき場所と責任を理解する。

| 入力UX | 利用者／場面 | 保持する対象 | 区別する状態 | 根拠・判断への導線 |
|---|---|---|---|---|
| [UX-000001](../../../03_IA/Analysis/UX-000001/ia_analysis.md) | CRDD作成者・保守者／成果物をレビューへ渡す前 | 検査対象、検査条件、指摘、場所、修正責任 | 検査前／不備あり／機械確認済み。意味判断は別状態 | 対象→指摘→場所→所有成果物 |

この表は入力UXごとの意味保持先である。共有定義へ統合しても、各行の対象・状態・導線を共通語だけへ丸めない。

各入力UXの対象、状態、導線を共有語だけへ丸めず、行ごとにSPEC処置へ引き継ぐ。

## 3. 対象・識別・関係

| 対象 | 利用者にとっての意味 | 識別・関係 |
|---|---|---|
| 検査対象 | 確認する成果物または範囲 | 改訂版（Revision）と対象範囲で識別 |
| 検査条件 | 適用した機械規則 | 適用範囲（Profile）と規則（Rule）で識別 |
| 指摘 | 条件に反した観測 | 対象と場所と理由へ結合 |
| 修正責任 | 正すべき正本 | 指摘から所有成果物へ結合 |

```text
[O: 検査対象] --検査される--> [O: 検査条件]
[O: 検査条件] --生む--> [O: 指摘]
[O: 指摘] --属する--> [O: 修正責任]
```

上表は複数の入力UXを横断して利用する中心対象を示す。入力固有の対象は「利用場面」の対応表にも保持し、中心対象へ統合できない意味を欠落として扱う。

対象、識別、関係の全行を処置し、名前やPathから識別情報を推測して再構成しない。

## 4. 状態・可視性・時間的意味

入力UXで必要とされた状態は次のとおりである。状態実値を採用する場合は、下流工程で対応関係を定義し、この利用者向け区分を上書きしない。

- `UX-000001`: 検査前／不備あり／機械確認済み。意味判断は別状態

重要な状態、不足、判断要否は次の行動と同じ文脈で示す。観測不能を不存在や正常へ丸めない。

欠測、不明、非開示、競合、古い情報を同じ空値へ丸めない。一時的情報と永続的情報、表示可否と実行権限を別に扱う。

## 5. 導線・責任・失敗時の保持

- CRDD作成者・保守者: 成果物をレビューへ渡す前に「意味レビュー前に機械判定できる不備を落とす」ために必要な判断を行う。システムは「検査範囲や理由が分からない」を避けられるよう、同じ入力へ同じ指摘と修正可能な場所を返す。

各利用場面の導線は「利用場面」の対応表を正とする。結果なし、判断待ち、失敗、状態不明の場合も、根拠または安全な戻り先へ接続する。

### 制約

具体的な画面、UI部品、API、Schema、保存方式、Process構成は本定義で確定しない。正式な識別子や状態実値を用いる場合は平易な表示と対応付け、利用者が内部構造を知らなくても判断できるようにする。

入力不足、権限不足、観測不能または関係不整合では、既知の状態・識別情報・未解消義務を保持し、安全な拒否または再確認先を返す。IAが定義していない状態値や保存方式はここで創作しない。

### 下流へ保持する意味

UIは指摘と対象箇所を、VerificationはCheckerと専門確認の境界を具体化する。

下流工程は「利用場面」の各行を受入単位として扱い、中心対象だけを実装して入力固有の意味を落とさない。

## 6. SPEC処置

| SPEC候補 | 処置 | 判断理由 |
|---|---|---|
| [SPEC-000001](../../Definitions/SPEC-000001/spec_definition.md) | New | この情報構造を入力・状態・結果・開示条件へ変換する |

## 7. UX観点との統合時に確認すること

UX観点の利用者成果と組み合わせ、情報を保持するだけでなく、どの契機で何を返し、何を変更せず、失敗後にどこへ戻れるかをSPEC定義で確定する。
