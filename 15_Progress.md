<a id="crdd-progress"></a>

# CRDD進捗管理（Progress）

Version: v0.18.1
Status: Candidate
Released Baseline: v0.18.0
Owner: Qual-Lab
Last Updated: 2026-09-01
Related:
- [01_Principles.md](01_Principles.md)
- [02_Terminology.md](02_Terminology.md)
- [03_Documentation.md](03_Documentation.md)
- [11_Skill.md](11_Skill.md)
- [12_Change.md](12_Change.md)
- [13_Release.md](13_Release.md)
- [16_Quality_Assurance.md](16_Quality_Assurance.md)
- [21_Discovery.md](21_Discovery.md)
- [24_UI_Behavior_Specification.md](24_UI_Behavior_Specification.md)
- [29_Verification.md](29_Verification.md)
- [52_Conformance_Audit.md](52_Conformance_Audit.md)

---

> この文書で分かること（非規範の案内）
>
> - 進捗管理の最小対象を何にするか
> - 開発方式が違っても共通に持つ進捗情報は何か
> - 進捗をどの根拠から導き、どこから先を派生指標として扱うか
> - 進んでいることと健全であることをどう分けるか
> - 反復、時間枠、継続フロー等の方式をどう接続するか

<a id="1-purpose-and-boundary"></a>

# 1. 目的と適用範囲（Purpose and Boundary）

本書は、CRDDにおける進捗管理の最小対象、共通の進捗情報、算出の階層、健全性との分離、集約単位、開発方式の接続契約の正本である。

CRDDは特定の開発方式を要求しない。段階型、リスク駆動の反復型、時間枠型の反復、継続フロー型、保守運用のいずれでも、一つの機能、変更、不具合、調査、是正事項を成立させるために必要な責務そのものは大きく変わらない。方式ごとに別の進捗管理モデルを定義せず、共通の最小対象と共通の進捗情報を持たせ、方式の違いは対象範囲の切り方、期間の切り方、反復の目的、並行度、集約単位、方式固有の補助情報として表す。

本書は次を定義しない。各正本を参照する。

| 対象 | 正本 |
|---|---|
| 未完了事項の存在、判断状態、再評価 | [課題探索・要求形成の`01_Discovery`と`99_Roadmap`](21_Discovery.md#62-registry-scope-and-registration) |
| 変更の契機、影響、実装、検証、終了の追跡 | [変更](12_Change.md) |
| リリース準備状況、リリース判断、リリース記録 | [リリース](13_Release.md) |
| 各工程の入口、変換、必要な責務の網羅、出口、工程移行の判定基準 | `21`〜`29`の各工程文書 |
| 検証義務、検証設計、検証結果、現在の品質状態、Quality Center | [品質保証](16_Quality_Assurance.md)、[検証](29_Verification.md) |
| 個別のスキル実行状態 | [スキル](11_Skill.md) |

進捗情報は、これらの正本から導く二次情報である。進捗管理を理由に、要求、判断、設計、検証結果の正本を置き換えない。

Quality Centerに表示する検証計画対実績や割合は、品質保証の網羅状態と未成立を理解するための表示である。作業がどれだけ進んだかを示す進捗指標とは別に扱い、どちらからも品質成立またはリリース可否を自動推定しない。

進捗指標を、工程移行、検証完了、リリース可否、リスク受容または人間の承認の判定根拠にしない。これらの判断は、対象成果物、工程固有の基準、検証結果、残存リスクおよび人間の決定権限に基づいて行う。進捗指標は、状況把握、確認対象の選定、レビュー契機および予測に使用できる。この禁止は、CRDDの責務に基づく進捗を報告するかどうかにかかわらず適用する。本書のその他の規定は、CRDDの責務に基づく進捗を報告する対象範囲へ適用する。6.3と10章は本項を各対象について具体化したものであり、本項の適用範囲を狭めない。

<a id="operational-utility-boundary"></a>

## 1.1. 進捗と運用上の有用性評価の分離

進んだ量と、利用者または組織にとって有用だったかを分ける。運用上の有用性を評価する場合は、少なくとも採用可能な結果までの時間、人間が実際に拘束された時間、AI処理量、処理量、不要な反復、費用または利用枠の集中、および品質を別々に観測する。一つの総合点、Agent起動数、Turn数、処理時間またはProvider分散率だけから有用性を判定しない。

比較では、対象作業、難易度、開始・終了条件、観測期間、分母、並行実行および品質条件を示す。必要に応じて、単一Agentによる直接実行、CRDD Contextを利用するがCoordinatorを使わない実行、CRDDとCoordinatorを利用する実行を区別し、Context自体の効果と実行組織化の効果を混同しない。すべての作業を重複実行する必要はないが、代表作業の選択理由と一般化限界を残す。

品質は、速度、人間負荷、費用または処理量で相殺できる単なる加点項目にしない。後工程Finding、人間による追加修正、Recoveryおよび未成立を含む現在の品質状態を[`品質保証`](16_Quality_Assurance.md)から参照し、品質を維持または改善した範囲で効率を評価する。成功値は観測前に捏造せず、実OperationのProfileを蓄積した後に集約する。測定不能な時間、Provider利用量または人間Attentionを0として扱わず、取得不能、推定または代理指標を区別する。

---

<a id="2-lifecycle-slice"></a>

# 2. ライフサイクル単位（Lifecycle Slice）

<a id="21-definition"></a>

## 2.1. 定義

ライフサイクル単位は、一つの価値、変更、機能、不具合、調査、是正事項等について、適用されるCRDD責務を成立させる進捗管理上の最小対象である。

```text
ライフサイクル単位
  ├─ 課題探索・要求形成の責務
  ├─ UXの責務
  ├─ IAの責務
  ├─ UIの責務
  ├─ 振る舞い仕様の責務
  ├─ アーキテクチャの責務
  ├─ 実装の責務
  ├─ 検証の責務
  └─ リリースの責務
```

ライフサイクル単位は工程を直列に通ることを意味しない。責務は重なり合い、短い往復で行き来してよい。UIと振る舞い仕様は並行・反復して対として接続する。

<a id="22-identification"></a>

## 2.2. 識別

ライフサイクル単位へCRDD標準の新しい識別子種別を発行しない。既にその対象を識別している参照を使用する。

```text
未完了作業の登録簿の参照キー
CHG-*
REQ-*等の安定コンテキストID
外部Issueまたはプロジェクト固有キー
```

すべてのライフサイクル単位が変更トレースを持つとは限らない。調査、UX検証、文書のみの対応等では、変更トレースやリリースが適用されない場合がある。変更トレースの存在を、ライフサイクル単位として追跡する条件にしない。

<a id="23-applicable-responsibilities"></a>

## 2.3. 適用責務の選択

すべてのライフサイクル単位へ同じ責務を必須にしない。対象ごとに適用責務と完了条件を選び、適用しない責務は理由とともに`Not Applicable`とする。適用の判定は各工程文書の入口契約と必要な責務の網羅に従い、本書では再定義しない。

適用責務の選択は人間の決定権限が確認する。責務を`Not Applicable`にすることで、必要な検証、レビュー、工程移行の判定を回避しない。

---

<a id="3-progress-core"></a>

# 3. 進捗中核（Progress Core）

<a id="31-retained-information"></a>

## 3.1. 保持する情報

すべてのライフサイクル単位について、原則として同じ項目を取得可能にする。保存形式、ファイル、ツールは固定しない。

```text
識別と種別
対象範囲（対象、対象外、対象リリースまたは対象期間、基準改訂版、対象範囲の追加・除外）
期日（計画開始、計画完了、実績開始、実績完了、完了予測）
対応状態
停止要因
着手可能性
責務別進捗
根拠
判断待ち・承認待ち
依存関係
```

開発方式ごとに別のスキーマを作らず、方式固有の補助項目だけを9章に従って追加する。

<a id="32-work-state"></a>

## 3.2. 対応状態（Work State）

ライフサイクル単位の対応状態は、異なる対象を横断して集計するための粗い上位状態である。登録簿の対応状態、変更トレースの状態、工程の網羅状態を置き換えない。

| 表示名 | 状態値 | 意味 |
|---|---|---|
| 未計画 | `Unscheduled` | 対応時期または対象範囲が未確定である |
| 計画済み | `Planned` | 対応方針または対象時期を計画している |
| 実行確定 | `Committed` | 対象範囲の基準へ投入することを決定している |
| 対応中 | `In Progress` | 対象への実作業を開始している |
| 一時停止 | `Paused` | 人間の判断により意図的に対応を中断し、再開に必要な情報を保持している |
| 完了 | `Completed` | 適用される完了条件を満たした |
| 中止 | `Cancelled` | 完了せず終了すると決定した |

```text
Unscheduled
    ↓
Planned
    ↓
Committed
    ↓
In Progress
    ├─ Paused ──→ In Progress
    ├─ Cancelled
    └─ Completed
```

本図はライフサイクル単位の対応状態の一般的な進み方を示す。登録簿の項目、変更トレース、工程の網羅状態は、それぞれの正本が定める状態遷移に従い、本図がそれらを上書きしない。実行根拠が別の正本で確定している対象は、本図の途中段階を経ずに該当する対応状態へ写像してよい。変更トレースは作成時点で`In Progress`へ写像する。粒度間の対応は6.1と6.2を正本とする。

判断状態は本図の軸ではない。判断状態の値と遷移は[課題探索・要求形成](21_Discovery.md#63-decision-state-and-work-state)を正本とする。

`Committed`と`In Progress`を同一視しない。対象範囲への投入を決めたことと、実作業を開始したことは別の判断である。`Paused`と停止要因も同一視しない。`Paused`は人間が意図して止めた状態であり、停止要因は外部の阻害条件である。`Cancelled`は実作業の開始前を含むいずれの段階からも到達でき、`Paused`も人間が意図して停止したいずれの段階から取り得る。

`Paused`の共通の意味と取得可能にする情報は[用語](02_Terminology.md#42-status-terms)を正本とし、本書では繰り返さない。停止要因との違いは3.3に従う。

対応状態の前進、特に`Committed`と`Completed`は人間の決定権限が確認する。AIは根拠から候補を提示できるが、対応状態を自己確定しない。

<a id="33-blocker-readiness-current-responsibility"></a>

## 3.3. 停止要因・着手可能性・現在の責務

進行に関する情報を一つの状態軸へ混ぜない。次は対応状態と別に保持する。

| 項目 | 意味 | 扱い |
|---|---|---|
| 停止要因（Blocker） | 依存、判断待ち、情報不足、アクセス不足等により進行できない | どの対応状態にも重なる属性とし、理由と解除条件を記録する |
| 着手可能性（Readiness） | 適用される次の責務の入口契約を満たしている | 各工程文書の入口契約から導く |
| 現在の責務 | いま主に扱っている責務 | 責務別進捗から導く |

停止要因を対応状態の値にしない。`Blocked`という語は粒度の異なる三つの対象で使う。監査結果の`Blocked`は監査を完了できない状態を指す。工程の網羅状態の`Blocked`は、3.4に従って責務単位の停止を表し、責務別進捗ではそのまま使用する。ライフサイクル単位の対応状態には`Blocked`を置かず、停止要因として保持する。この境界は[課題探索・要求形成の判断状態と対応状態](21_Discovery.md#63-decision-state-and-work-state)と同じ扱いとする。

停止要因の記録と解除は、対象項目またはライフサイクル単位の対応状態に責任を持つ人間の決定権限が確認する。AIは、停止要因の候補提示、記録案の作成、根拠の更新を支援できるが、停止要因の確定または解除を自己決定しない。

レビュー中、検証中等の活動は独立した対応状態にせず、責務別進捗と現在の責務から導く。

<a id="34-responsibility-progress"></a>

## 3.4. 責務別進捗（Responsibility Progress）

責務別進捗は、各工程文書が定める対象範囲と網羅状態をライフサイクル単位ごとに集約したものである。新しい状態語彙を導入しない。

| 網羅状態 | 責務別進捗での扱い |
|---|---|
| `Not Applicable` | 適用責務から除外する |
| `Not Started` | 未着手 |
| `Blocked` | 停止（停止要因を伴う） |
| `Partial — Human Authorized` | 部分成立（人間が限定的な引き渡しを承認済み） |
| `Complete for Scope` | 対象範囲で成立 |

検証の責務は、[検証](29_Verification.md#scope-and-coverage-state)が定める網羅状態から導き、本節の表へ丸めない。`Not Applicable`の検証義務は適用責務から除外する。単純進捗率では、`Verified`だけを成立として数え、`Partially Verified — Human Authorized`は部分成立、`Failed`、`Blocked`および`Not Verified`は成立として数えない。

リリースの責務は、[リリース](13_Release.md)のリリース準備状況とリリース判断から導く。

<a id="35-evidence"></a>

## 3.5. 根拠

進捗と完了は、根拠から導く。少なくとも次のうち、対象に適用されるものを参照可能にする。

```text
人間による判断・承認
独立レビュー・監査の結果
正本成果物の改訂版と網羅状態
変更トレースの状態
実装成果物、コミット、プルリクエスト
テスト結果
検証結果と検証根拠
リリース記録
```

根拠のない進捗を報告しない。取得できない進捗を、あたかも観測した値のように示さない。

---

<a id="4-derivation-layers"></a>

# 4. 進捗の算出階層

進捗は一つの数値ではなく、次の階層で扱う。下位層は上位層から導き、上位層を置き換えない。

| 層 | 内容 | 性質 |
|---|---|---|
| 入力の正本 | 各項目の対応状態、対象範囲、期日、根拠 | 正本 |
| 一次集約 | 状態分布 | 導出値。追加の判断を要しない |
| 基本派生指標 | 単純進捗率 | 導出値。算出規則を明示する |
| 高度な派生指標 | 重み付き進捗率、工程進捗、完了予測 | 導出値。追加の入力または仮定を要する |

<a id="41-state-distribution"></a>

## 4.1. 一次集約（状態分布）

進捗の一次表示は状態分布とする。分布は数え方だけで決まり、重み付けや仮定を必要としない。

```text
対象責務 9 のうち（適用責務は Not Applicable を除く 8）
  Complete for Scope        4
  Partial — Human Authorized 2
  Not Started               2
  Blocked                   0
  Not Applicable            1
```

集約単位での一次表示も同様に、対応状態ごとの件数で示す。

<a id="42-simple-progress-rate"></a>

## 4.2. 基本派生指標（単純進捗率）

単純進捗率は、適用対象の件数だけで算出する。

```text
単純進捗率
=
成立した適用責務の数
÷
適用責務の数（Not Applicableを除く）
```

集約単位では、完了したライフサイクル単位の数を対象件数で割る。`Not Applicable`を分母へ含めない。部分成立を成立として数えない。部分成立を段階的な値として扱う場合は、その換算規則を明示する。

<a id="43-advanced-indicators"></a>

## 4.3. 高度な派生指標

重み付き進捗率、工程横断の工程進捗、完了予測日は、追加の入力または仮定を必要とする。これらは要求しない。使用する場合だけ、次を取得可能にする。

- 重みまたは仮定を指定した決定権限
- 重みまたは仮定の内容と適用対象範囲
- 算出規則
- 算出時点と、そのとき参照した根拠

重みは、人間が指定した場合にだけ考慮する。指定がない場合は重みを推定せず、単純進捗率と状態分布を使用する。CRDDは特定の重み体系、見積り手法、予測モデルを要求しない。

<a id="44-ai-boundary"></a>

## 4.4. AIによる算出の境界

AIは、根拠から状態分布と単純進捗率を算出し、不足、矛盾、陳腐化を指摘できる。次は自己確定しない。

- 対応状態の前進、特に`Committed`と`Completed`
- 適用責務の`Not Applicable`
- 重みまたは予測の前提
- 完了条件を満たしたという判定
- 期日または完了予測の確約

根拠が不足する場合は、進捗を推定で埋めず、不足している根拠と確認先を示す。

---

<a id="5-progress-and-health"></a>

# 5. 進捗と健全性の分離

進捗が進んでいることは、健全であることを意味しない。進捗と健全性（Health）は別に扱う。

```text
進捗    単純進捗率 75%（適用8責務中、成立6、部分2。部分成立は成立に数えない）
健全性  要注意（未解消の判断待ち2件、検証根拠が対象改訂版より古い）
```

健全性は、少なくとも次の信号から判定する。

```text
停止要因
判断待ち・承認待ち
対象範囲の未確定
根拠の不足
未完了のレビューまたは監査
未完了の検証
未解消の重大な指摘事項または重大不具合
依存関係の遅延
期日の超過
進捗情報自体の陳腐化
```

既定の表示は`Healthy`、`At Risk`、`Impaired`、`Unknown`とする。名称はプロジェクトで定義してよい。判定に使用した信号と、判定を確認した決定権限を取得可能にする。

健全性が`At Risk`または`Impaired`であることを理由に、進捗率を下方修正して一つの数値へ丸めない。両方を並べて示す。

---

<a id="6-mapping-to-existing-states"></a>

# 6. 既存の状態との対応

ライフサイクル単位の対応状態は上位状態であり、既存の状態を置き換えない。同じ対象が複数の状態を同時に持つ場合、それぞれの正本はそれぞれの文書である。

<a id="61-registry-mapping"></a>

## 6.1. 未完了作業の登録簿との対応

| 登録簿の対応状態 | ライフサイクル単位の対応状態 |
|---|---|
| `Unscheduled` | `Unscheduled` |
| `Ready for Review` | 直前の対応状態（`Unscheduled`または`Planned`）を維持する。再評価の結果が出るまで段階を進めない |
| `Planned` | `Planned` |
| `In Progress` | `In Progress` |
| `Completed` | `Completed` |
| `Cancelled` | `Cancelled` |

`Committed`と`Paused`に対応する登録簿の状態値はない。対象範囲への投入確定と意図的な停止は、進捗管理側の区別である。

登録簿の判断状態は進捗の軸ではない。判断状態が適用される項目のうち、判断状態が`Adopted`でない項目は、進捗の対象範囲へ含めない。変更トレース参照、明確な不具合、承認済み是正その他の実行・参照項目は判断状態を適用しないため、実行根拠への参照をもって進捗の対象範囲へ含める。適用される状態軸は[判断状態と対応状態](21_Discovery.md#63-decision-state-and-work-state)を正本とする。

<a id="62-change-trace-mapping"></a>

## 6.2. 変更トレースとの対応

| 変更トレースの状態 | ライフサイクル単位の対応状態 |
|---|---|
| `Open` | `In Progress` |
| `Ready for Verification` | `In Progress` |
| `Verified` | `In Progress` |
| `Ready for Release Handoff` | `In Progress` |
| `Released` | `Completed`（リリースの責務が適用される場合） |
| `Closed` | `Completed` |
| `Cancelled` | `Cancelled` |
| `Reopened` | `In Progress` |
| `Superseded` | `Cancelled`（置換先のライフサイクル単位を参照する） |

変更トレースの状態のほうが細かい。責務別進捗と根拠は変更トレース側から導き、粗い対応状態へ丸めた結果だけで検証や正本反映の完了を判定しない。

<a id="63-phase-coverage-mapping"></a>

## 6.3. 工程の網羅状態との対応

工程の網羅状態は3.4に従って責務別進捗へ対応づける。工程移行の判定基準と工程監査チェックリストは各工程文書を正本とし、進捗指標で代替しない。進捗指標を、工程移行の判定基準を満たした根拠にしない。

---

<a id="7-aggregation"></a>

# 7. 集約単位

同じライフサイクル単位の集合を、複数の視点で集約できるようにする。集約は同じ入力に対する[投影](02_Terminology.md#225-projection)であり、別の正本にしない。

```text
ライフサイクル単位
  ├─ 変更トレース単位
  ├─ 機能単位
  ├─ 不具合単位
  ├─ 責務・工程単位
  ├─ 反復または時間枠単位
  ├─ リリース単位
  ├─ 節目単位
  ├─ プロジェクト単位
  └─ 複数プロジェクト単位
```

算出の基本規則は集約単位によって変えない。変えるのは対象集合と絞り込みだけである。集約結果に、集約対象、対象時点、除外した対象と除外理由を添える。

集約の側で、ライフサイクル単位の対応状態、適用責務、根拠を変更しない。

---

<a id="8-ui-spec-pair-readiness"></a>

# 8. UIと振る舞い仕様の対の成立度

UIと振る舞い仕様は並列責務である。片方の責務別進捗が高くても、対として成立していなければ後続工程を開始できる状態とは限らない。

そのため、UIの責務別進捗と振る舞い仕様の責務別進捗に加えて、対の成立度を保持できるようにする。

```text
UIの責務別進捗           適用7項目中 成立5
振る舞い仕様の責務別進捗   適用7項目中 成立4
対の成立度               適用7項目中 両側成立3
```

対の成立度は、[UIと振る舞い仕様の対応関係](24_UI_Behavior_Specification.md)が定める必要な対応関係の網羅範囲に対して、UI側と振る舞い仕様側の両方が成立している項目から導く。対の成立度を、UIと振る舞い仕様の進捗の平均で代替しない。

対応レビューの結果、完了条件、監査チェックリストは[`24_UI_Behavior_Specification.md`](24_UI_Behavior_Specification.md)を正本とし、本書では再定義しない。

---

<a id="9-execution-method-mapping"></a>

# 9. 実行方式の接続契約（Execution Method Mapping）

CRDDは特定の開発方式を要求せず、外部の開発方式、フレームワーク、手法の用語、役割、儀式、規則を定義または再定義しない。方式を採用する場合は、次の対応だけを取得可能にする。

| 接続項目 | 内容 |
|---|---|
| 実行単位の対応 | 方式固有の実行単位（反復、時間枠、周期、継続フロー、工程、節目等）が、どのライフサイクル単位の集合に対応するか |
| 目標と完了条件の対応 | 方式固有の目標と完了条件が、CRDDのどの完了条件、工程移行の判定基準、検証結果へ対応するか |
| 状態の対応 | 方式固有の状態が、3.2の対応状態のどれに対応するか |
| 補助情報の所在 | 方式固有の補助情報をどこに保持し、どの正本成果物を参照するか |
| 対象選択の経路 | [未完了作業の登録簿](21_Discovery.md#62-registry-scope-and-registration)から、どの規則で対象範囲を選ぶか |

実行方式の採用または変更は、対象範囲の作業方式に責任を持つ人間の決定権限が確認する。AIは、5つの接続関係の候補提示、記録案の作成、整合確認を支援できるが、実行方式の採用または変更を自己決定しない。5つの接続関係を取得可能にしたことだけを、方式の採用、作業の完了、工程移行、検証または人間の判断の代わりにしない。

方式固有の目標、完了条件、儀式、役割は、CRDDの完了条件、工程移行の判定基準、独立レビュー、検証、リリース判断を置き換えない。方式固有の完了定義を満たしたことを、適用される検証と人間の判断を満たした根拠にしない。

方式固有の補助情報は共通の進捗中核へ追加する項目として扱い、方式ごとに別の進捗中核を作らない。特定の方式を中核へ取り込み、他方式との共通性を失わせない。

```text
共通の進捗中核
  ＋
方式固有の補助情報
```

---

<a id="10-placement-and-anti-patterns"></a>

# 10. 配置と避けるべき運用

進捗情報のためにCRDD標準の恒久フォルダを追加しない。進捗情報は、[未完了作業の登録簿](21_Discovery.md#62-registry-scope-and-registration)、変更トレース、各工程の正本成果物、検証・リリース記録、または外部の課題管理・進捗管理ツールから取得可能にする。外部ツールを使う場合は、場所、対象、対象時点、アクセス方法、正本成果物へ戻る経路を辿れるようにする。

次を避け、監査対象とする。

- 根拠のない進捗率、または取得できない進捗を観測値のように示している
- 重みまたは予測の前提を示さずに、重み付き進捗率または完了予測を確定値として扱っている
- 進捗指標を、工程移行、検証完了、リリース可否、リスク受容または人間の承認の判定根拠にしている
- 停止要因、判断待ち、レビュー中、検証中を対応状態の値へ混ぜ、実際の状態を表現できなくしている
- `Paused`と停止要因、`Committed`と`In Progress`を同一視している
- 進捗管理のために、要求、判断、設計、検証結果を進捗表示側へ複製している
- 開発方式ごとに別の進捗項目とスキーマを作り、集約できなくしている
- 方式固有の完了定義だけで、適用される検証と人間の判断を代替している
- 進捗情報が陳腐化したまま、健全性へ反映していない
- AIが推定した進捗、完了予測、`Not Applicable`を人間の確認なく確定している

---

# 11. 最終原則

```text
進捗は、作業量の報告ではない。
適用される責務がどこまで成立し、
何がまだ成立していないかを、
根拠とともに説明できる状態である。

進んでいることと、健全であることは違う。
どちらか一方だけを示して、もう一方を隠さない。
```
