# 視覚表現とアクセシビリティ

状態: UI定義の横断投影

## 1. 目指す認識

利用者が内部構造を覚えなくても、現在の対象、状態、根拠、判断要否および次の行動を短時間で見分けられることを目指す。装飾より、意味の近接、状態の区別、読取り順序を優先する。

```text
最初に認識する
  何が起きているか / 判断が必要か
        ↓
次に確認する
  影響 / 不完全性 / 根拠
        ↓
最後に選ぶ
  次の安全な行動 / 詳細
```

## 2. 表現原則

| 観点 | 原則 |
|---|---|
| 階層 | 結果・重大状態を先に、理由・根拠、次行動、内部詳細の順に置く |
| 状態 | 色だけで区別せず、状態名、影響、次の行動を併記する |
| 密度 | Overviewは判断に必要な情報へ絞り、根拠と詳細へ段階的に進める |
| 一貫性 | CLI、MCP、Workbenchで意味状態と用語を変えない |
| 不完全性 | missing、stale、restricted、conflicting、unknownを正常や0件へ畳まない |
| 危険操作 | 通常操作と視覚・文言で区別し、実行前に対象とEffectを示す |
| 秘密 | Credential、秘密値、生Provider出力、opaque Capabilityを表示しない |

## 3. 媒体差

CLIは行順、折返し、直接入力、終了後の読取りを確認する。MCPは構造化結果と人間向け要約を分ける。Workbenchは同じ意味を領域、階層、状態Variantとして視覚化する。媒体差は配置と操作方法に限定し、成功・停止・権限・回復の意味を変えない。

## 4. アクセシビリティ

- キーボードだけで主要操作、取消、戻る、Credential切替へ到達できる。
- フォーカス順は視覚順と意味順に一致させる。
- 拡大、折返し、文字量増加で重要条件や操作を失わない。
- 動きは状態変化の唯一の通知にせず、軽減設定時も同じ意味を示す。
- 支援技術の適合は実測なしに主張せず、対象プロファイルと未評価範囲を記録する。

## 5. 視覚案の現在状態

v0.21の最小Workbenchは論理構造と状態契約を先に固定する。最終配色、Typography、Component外観およびGUI Frameworkは未決であり、Prototypeと実画面を用いた視覚批評後に人間が判断する。現在のブランド素材は[公式素材のUI定義](Definitions/UI-000019/ui_definition.md)と[現行素材](assets/brand/)へ接続する。
