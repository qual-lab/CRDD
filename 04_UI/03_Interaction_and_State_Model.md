# 操作と表示状態

状態: UI定義の横断投影
情報源: [UI定義台帳](01_User_Interface.md#3-ui定義台帳)

この文書は全UIへ同じ実行状態を強制しない。下表で各UIに適用する変化を先に固定し、後続のFlowとVariantは該当するUIだけが使用する。

## UIごとの適用範囲

| UI | 主な性質 | 適用する変化 | 既定では適用しない変化 |
|---|---|---|---|
| `UI-000001` | 検査と指摘確認 | 未実行／検査中／合格／指摘あり／判定不能 | 取消・回復は検査実装が必要とする場合だけ |
| `UI-000002` | 委任と実行 | UX-000002のLifecycleは準備中／許可待ち／実行中／停止。UX-000003のTask Lifecycleは開始可能／実行中／入力・判断待ち／停止／完了／失敗。提案／受付可能／受付済み／拒否はLifecycleと別の受付Feedbackとして示す | 受付後の結果不明は新しい状態やTaskにせず、同じ依頼識別情報の再観測条件として示す。拒否は同じ提案の範囲見直しへ戻し、Lifecycleの停止へ畳まない |
| `UI-000003` | 失敗後処置 | 作用状態／再試行可否／回復義務／清掃確認 | 通常の閲覧状態 |
| `UI-000004` | 状況把握・節目の受入判断・比較 | Task未完了／Task完了／Objective判断待ち／Objective受入済み／Objective差戻し／Milestone判断待ち／Milestone受入済み／Milestone差戻し／一部取得／古い情報／競合／開示制限 | 委任の提案・受付・拒否・委任範囲の見直しは`UI-000002`が扱う。受入判断は対象Objective／Milestoneの根拠確認へ戻る |
| `UI-000005` | 実行観測と故障診断 | 観測済み／未観測／不明、境界別の利用可否 | 診断対象でない作用の再実行 |
| `UI-000006` | Repository選択とNavigation | 利用可能／未確認／曖昧／利用不能 | 取消・回復 |
| `UI-000007` | 公開要求と結果 | 受付前／受付済み／作用前失敗／作用後失敗／結果あり | Transport固有の独自状態 |
| `UI-000008` | 接続と利用範囲 | 接続資格必要／利用可能／開示制限／利用不能／不明 | Taskの取消・回復 |
| `UI-000009` | Meeting・Topic候補の処置 | 観測済み／候補／採用／却下 | Processの実行・回復 |
| `UI-000010` | Tool・構成選択 | 有効／無効／利用可能／利用不能／選択済み | 取消・回復 |
| `UI-000011` | 実行時データ管理 | 一時／保持必要／回復必要／清掃可能／不明 | 無関係なTask実行状態 |
| `UI-000012` | 引き渡しと再接続 | 準備／送信／受領／返却／切断／結果取得／回復必要／確定 | 新規実行による再開 |
| `UI-000013` | 信頼判断 | 識別／完全性確認／準拠／配布者信頼／品質確認を別軸表示 | 取消・回復、および単一の総合信頼状態 |
| `UI-000014` | 能力移行確認 | 維持／変更／廃止／未確認 | 取消・回復 |
| `UI-000015` | 変更・品質追跡 | 計画／進行／レビュー／是正／確認／判断必要／公開を別軸表示 | Processの取消・回復 |
| `UI-000016` | 外部送信と採否 | 未許可／許可済み／要求済み／受理済み／Effect不明／Effect成立／Effect成立・結果不明／結果観測／返却／候補／採用。Effect不明は成立を推測せず同じ依頼を再観測し、Effect成立・結果不明は成立済みEffectを保持して結果搬送または再観測へ戻る。採用／却下／保留は既存の判断（Decision）へ返る結果値として操作・認識でき、採用時だけ候補を採用状態へ進める | Effect不明またはEffect成立・結果不明からの自動再送／送信後の結果の自動採用／却下・保留をCandidate状態として追加すること |
| `UI-000017` | 過去情報の選択 | 現在有効／履歴／置換済み／不明 | 取消・回復 |
| `UI-000018` | 文書と図のNavigation | 下書き／レビュー済み／現在有効／履歴、作成／参照／理由付き非該当 | 取消・回復 |
| `UI-000019` | 素材と権利の確認 | 候補／採用済み／開示制限／取下げ済み | 取消・回復 |
| `UI-000020` | 実行記録の依頼と結果確認 | 準備中／公開中／記録済み／未記録／結果不明 | 結果不明時の自動再発行 |

## 1. 共通操作Flow

```text
[対象を選ぶ]
      │
      ▼
[現在状態と利用範囲を確認]
      │
      ├─ 不明 / 欠測 / 制限 ─→ [根拠・Coverageを確認]
      │
      ├─ 人間判断が必要 ─────→ [判断内容と影響を確認]
      │
      └─ 操作可能 ───────────→ [操作内容とEffectを確認]
                                      │
                                      ▼
                                  [実行する]
                                      │
                   ┌──────────────────┼──────────────────┐
                   ▼                  ▼                  ▼
                完了               停止               結果不明
                   │                  │                  │
                   ▼                  ▼                  ▼
              次の行動へ      理由別の処置へ      回復義務を保持
```

## 2. 取消・回復Flow

```text
実行中
  │ action: 取消
  ▼
取消要求済み ── 完了通知待ち
  │
  ├─ cleanup確認済み ─→ 停止結果 / 次の行動
  └─ cleanup不明 ─────→ 回復義務あり
                              │
                              ▼
                         回復処置中
                              │
                              ├─ 終了後確認済み ─→ 義務解消
                              └─ 観測不能 ───────→ 義務維持
```

取消要求、Process終了、資源回収、回復処置、回復義務解消を同じ状態へ畳まない。

## 3. 表示状態の共通Variant

| Variant | 利用者へ示す内容 | 主な操作 |
|---|---|---|
| 初期／未選択 | 選択対象と利用可能範囲 | 対象を選ぶ |
| 読み込み中 | 何をいつから取得中か | 待機、必要時だけ取消 |
| 空 | 0件なのか未取得なのか | 作成、再取得、情報源確認 |
| 一部取得 | 取得済み範囲、欠測、制限 | 利用可能範囲で続行、根拠確認 |
| 判断待ち | 判断主体、選択肢、影響、保留時 | 判断する、保留する |
| 操作中 | 対象、操作、取消可否 | 待機、許可時のみ取消 |
| 完了 | 結果、Effect、根拠、次行動 | 結果確認、次工程へ進む |
| 停止 | 理由、Effect有無、cleanup、再開条件 | 是正、回復、担当者へ渡す |
| 結果不明 | 不明な範囲、再実行禁止、回復参照 | 状態確認、専用回復 |
| 権限なし | 開示可能な範囲だけの理由 | Credential切替または終了 |
| 競合／古い情報 | 競合対象、観測時点、再読込み条件 | 正本確認、再取得 |

## 4. 操作の表示原則

- 主要操作は現在の利用者目的に直接進む一つを優先する。
- 不可逆操作、外部送信、Authority発行、再試行は対象・影響・境界を実行前に示す。
- 利用不能な操作は隠すか無効化するだけでなく、存在開示が許可される場合に理由と代替を示す。
- 成功表示はSystemの完了条件を満たした時だけ使い、受付、開始、要求発行または候補生成を完了と表示しない。

## 補足分析

なし。

## Checklist

- [x] 全UI Definitionを一件ずつ処置した
- [x] Interaction、Visible State、FeedbackおよびRecovery Presentationを区別した
- [x] 共通Variantを全UIへ一律適用していない
- [x] Behavior Ruleを先取りしていない
- [x] 個別UI Definitionの意味を再定義していない
