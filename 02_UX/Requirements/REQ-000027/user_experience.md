# REQ-000027の利用者体験分析

状態: UX意味再分析済み・独立レビュー待ち
要求: `REQ-000027` 外部Contextの送信・昇格境界
探索元: [EXP-000003](../../../01_Discovery/Explorations/EXP-000003_External_Context_Boundaries/exploration.md)

## 1. REQの一次分析

```text
内部Contextが無条件に送られ、外部反応が要求や権限として取り込まれる
        ↓
解決策を先に固定せず、人の仕事として読み替える
        ↓
UXとして必要
送信範囲を理解し帰還結果を候補として扱う
```

この要求で解くのは機能の有無だけではない。外部Context所有者・利用者が「不要情報を漏らさず人間判断を保って外部連携できる」状態へ進めないことを問題として扱う。具体的な画面、データ構造、API、Componentまたは数値閾値はここで確定しない。

| 観点 | 内容 |
|---|---|
| 解決する問題 | 内部Contextが無条件に送られ、外部反応が要求や権限として取り込まれる |
| UX Need | 送信範囲を理解し帰還結果を候補として扱う |
| 対象範囲 | 外部AI・API・MCPを使う人、情報所有者が、「内部Contextが無条件に送られ、外部反応が要求や権限として取り込まれる」状態から「送信先、目的、情報範囲、決定権限、持ち帰る内容を理解して制御できる」状態へ移る場面 |
| 対象外 | 具体的な画面、データ構造、実装方式および数値閾値の確定 |

## 2. 利用者・目標・成果

```text
外部Context所有者・利用者
        │
        │ wants to
        ▼
送信範囲を理解し帰還結果を候補として扱う
        │
        │ so that
        ▼
不要情報を漏らさず人間判断を保って外部連携できる
```

| 項目 | 内容 |
|---|---|
| Primary Persona | [Product横断の利用者像](../../02_Personas.md)の「外部Contextの所有者」 |
| 利用場面 | 外部AI・Tool・RepositoryへContextを送る時 |
| Goal | 送信範囲を理解し帰還結果を候補として扱う |
| Outcome | 不要情報を漏らさず人間判断を保って外部連携できる |
| REQ固有の差 | 送信先・目的・分類・最小情報を確認することが、この要求固有の成立条件になる |
| 根拠・確信度 | 探索元の課題と採用要求から導いた設計上の想定。個人属性、利用頻度および許容負担は未実測。 |

## 3. 利用者に起きる変化

```text
Before
────────────────
- 内部Contextが無条件に送られ、外部反応が要求や権限として取り込まれる
        │
        │ 外部Contextの送信・昇格境界が変える体験
        ▼
After
────────────────
- 送信先、目的、情報範囲、決定権限、持ち帰る内容を理解して制御できる
```

外部Contextの送信・昇格境界は、単に内部方式を成立させる要求ではない。「内部Contextが無条件に送られ、外部反応が要求や権限として取り込まれる」状態から、「送信先、目的、情報範囲、決定権限、持ち帰る内容を理解して制御できる」状態へ変わり、利用者が次の判断へ進めることまでを体験として扱う。

この変化で守るのは操作手順ではない。外部Context所有者・利用者が送信範囲を理解し帰還結果を候補として扱うことで、不要情報を漏らさず人間判断を保って外部連携できるようになることを守る。

## 4. UX成果への統合

全36要求を横断比較し、この要求から生じる成果候補をCanonical UX成果へ接続した。同じIDへ接続する場合も、要求固有の成立条件を失わない。

| UX成果候補 | 処置・接続先 | 判断理由とこの要求が補う内容 |
|---|---|---|
| 所有正本へ戻る候補操作 | `Same → UX-000004` | `REQ-000012`と同じ「所有正本へ戻る候補操作」を目指す。利用者が得る最終状態と主な失敗条件が同じであり、本要求「外部Contextの送信・昇格境界」はその成立条件を別の責務境界から補う。 |
| 出所付きContextと結果の往復 | `Same → UX-000010` | `REQ-000017`と同じ「出所付きContextと結果の往復」を目指す。利用者が得る最終状態と主な失敗条件が同じであり、本要求「外部Contextの送信・昇格境界」はその成立条件を別の責務境界から補う。 |
| Secretと不要情報を含めないContext | `Same → UX-000044` | `REQ-000017`と同じ「Secretと不要情報を含めないContext」を目指す。利用者が得る最終状態と主な失敗条件が同じであり、本要求「外部Contextの送信・昇格境界」はその成立条件を別の責務境界から補う。 |
| 外部送信範囲と同意の理解 | `New → UX-000054` | 「送信先、目的、操作、情報分類および許可範囲をEffect前に理解できる」を独立して変更・検証する成果である。REQ-000027では「送信範囲を理解し帰還結果を候補として扱う」をGoalとし、外部Effect前と結果昇格時に接続済みを包括許可とし結果を自動採用することを防ぐため、既存成果への条件追加ではなくNewとして追跡する。 |

## 5. 重要な体験

```text
外部AI・Tool・RepositoryへContextを送る時
        ↓
送信先・目的・分類・最小情報を確認する
        │
        ├─ ★ Critical: 外部Effect前と結果昇格時
        ├─ ⚠ Failure:  接続済みを包括許可とし結果を自動採用する
        └─ ✓ Quality:  Consent・Projection・Promotionを分離する
        ↓
不要情報を漏らさず人間判断を保って外部連携できる
```

### JourneyとSupporting Model

要求固有の区間は上のFlowが所有し、長い時間軸や複数主体との関係は次の既存成果物へ接続する。統合図をこの文書へ複製しない。

| Supporting Model | 処置 | 理由・参照先 |
|---|---|---|
| Experience Change | `作成` | 本文冒頭で、この要求が変える利用前後の仕事・認知・判断を示した。 |
| Experience Flow／Journey | `既存参照` | 本要求が関わる時間軸は[外部Contextを送信し結果を受け取るJourney](../../03_Experience_Map.md#外部contextを送受信する)を参照し、要求固有の区間は上表で示す。 |
| Service Blueprint | `既存参照` | 複数主体の協調が体験成立条件になるため、[共同Service Blueprint](../../04_Service_Blueprint.md#1-共同service-blueprint)を参照し、本要求固有の責任境界を次節で示す。 |
| User／Task Flow／Storyboard | `非該当` | 具体的な操作、画面遷移または利用環境の描写はIA／UIで具体化し、この要求分析では先取りしない。 |

### 責任境界

| 担い手 | この要求で担うこと | 越えてはならない境界 |
|---|---|---|
| 利用者（外部Context所有者・利用者） | 送信範囲を理解し帰還結果を候補として扱うために、提示された状態と根拠から次の行動を判断する | 不足情報や内部状態を推測で補うことを要求されない |
| 提供System／AI | 送信先・目的・分類・最小情報を確認するための状態、根拠および選択肢を示す | 接続済みを包括許可とし結果を自動採用する状態を成功・完了として表示しない |
| 運用・確認者 | 「Consent・Projection・Promotionを分離する」ことと、不要情報を漏らさず人間判断を保って外部連携できる状態へ到達できることを反証する | 未確認範囲や人間の判断を便宜的に上書きしない |

### 重要場面・失敗・品質期待の対応

| 重要場面 | 避ける失敗 | 品質期待 |
|---|---|---|
| 外部Effect前と結果昇格時 | 接続済みを包括許可とし結果を自動採用する | Consent・Projection・Promotionを分離する |

要求から得た追加の品質期待は次のとおりである。主Flowの品質を置き換えず、下流で具体化する観測点として保持する。

| 重要場面 | 避ける失敗 | 品質期待 |
|---|---|---|
| 結果または状態を最初に受け取る時 | 内部Contextが無条件に送られ、外部反応が要求や権限として取り込まれる | Secretや不要Contextを送らない。 |
| 結果を判断または引き継ぐ時 | 必要条件を満たしていないのに完了・正常と理解する | 外部内容を命令や事実へ自動昇格しない。 |
| 失敗・不足から次の行動を選ぶ時 | 成立不能の理由や回復先が分からないまま作業が止まる | 許可範囲外ではEffect前に止まる。 |

## 6. 下流への引き渡し

```text
このREQで確定した利用者成果
        │
        ├─→ IA: 必要な情報・関係・見つけ方
        ├─→ UI: 誤認させない表示・操作意図
        ├─→ SPEC: 振る舞い・失敗・体験品質の条件
        └─→ Verification: 仮説を破る反例と観測
```

### 妥当性確認と未確認事項

| 確認する仮説 | 観測方法 | 現在未確認の範囲 |
|---|---|---|
| 送信先、目的、情報範囲、決定権限、持ち帰る内容を理解して制御できることで、内部Contextが無条件に送られ、外部反応が要求や権限として取り込まれるという負担または誤認を減らせる。 | 代表シナリオの利用者確認、UX専門Review、および品質期待を破る反例による下流検証 | 役割ごとの利用頻度、許容待ち時間・操作負担、用語理解および支援技術差 |

### 工程別の引き渡し

| 引き渡し先 | 具体化する義務 |
|---|---|
| IA以降 | IAはInformation、Purpose、Authority、Sourceを分け、Communication／SPEC／Architectureは送受信境界を具体化する。 |

Discoveryへ戻す条件は、想定した利用者、問題または「送信範囲を理解し帰還結果を候補として扱う」という必要性が誤っていると分かった場合である。下流は実現方式を具体化してよいが、「不要情報を漏らさず人間判断を保って外部連携できる」という成果を無断で弱めない。
