# REQ-000030の利用者体験分析

状態: UX意味再分析済み・独立レビュー待ち
要求: `REQ-000030` 段階的実境界試験と回帰選択
探索元: [EXP-000010](../../../01_Discovery/Explorations/EXP-000010_Assurance_and_Regression/exploration.md)

## 1. REQの一次分析

```text
UT・IT・E2Eを区別せず繰り返し、失敗原因と残る不確実性が分からない
        ↓
解決策を先に固定せず、人の仕事として読み替える
        ↓
UXとして必要
試験層ごとの保証と未確認範囲を理解する
```

この要求で解くのは機能の有無だけではない。実装者・品質確認者が「最終E2E前に外部境界の問題を局所化できる」状態へ進めないことを問題として扱う。具体的な画面、データ構造、API、Componentまたは数値閾値はここで確定しない。

| 観点 | 内容 |
|---|---|
| 解決する問題 | UT・IT・E2Eを区別せず繰り返し、失敗原因と残る不確実性が分からない |
| UX Need | 試験層ごとの保証と未確認範囲を理解する |
| 対象範囲 | 変更担当者、Release判断者が、「UT・IT・E2Eを区別せず繰り返し、失敗原因と残る不確実性が分からない」状態から「どの成立条件をどの試験で確認し、何が未確認か理解して次の検証を選べる」状態へ移る場面 |
| 対象外 | 具体的な画面、データ構造、実装方式および数値閾値の確定 |

## 2. 利用者・目標・成果

```text
実装者・品質確認者
        │
        │ wants to
        ▼
試験層ごとの保証と未確認範囲を理解する
        │
        │ so that
        ▼
最終E2E前に外部境界の問題を局所化できる
```

| 項目 | 内容 |
|---|---|
| Primary Persona | [Product横断の利用者像](../../02_Personas.md)の「CRDD作成者・保守者」 |
| 利用場面 | 変更の検証計画を作る時 |
| Goal | 試験層ごとの保証と未確認範囲を理解する |
| Outcome | 最終E2E前に外部境界の問題を局所化できる |
| REQ固有の差 | UTからRTまでの対象と回帰選択を組み立てることが、この要求固有の成立条件になる |
| 根拠・確信度 | 探索元の課題と採用要求から導いた設計上の想定。個人属性、利用頻度および許容負担は未実測。 |

## 3. 利用者に起きる変化

```text
Before
────────────────
- UT・IT・E2Eを区別せず繰り返し、失敗原因と残る不確実性が分からない
        │
        │ 段階的実境界試験と回帰選択が変える体験
        ▼
After
────────────────
- どの成立条件をどの試験で確認し、何が未確認か理解して次の検証を選べる
```

段階的実境界試験と回帰選択は、単に内部方式を成立させる要求ではない。「UT・IT・E2Eを区別せず繰り返し、失敗原因と残る不確実性が分からない」状態から、「どの成立条件をどの試験で確認し、何が未確認か理解して次の検証を選べる」状態へ変わり、利用者が次の判断へ進めることまでを体験として扱う。

この変化で守るのは操作手順ではない。実装者・品質確認者が試験層ごとの保証と未確認範囲を理解することで、最終E2E前に外部境界の問題を局所化できるようになることを守る。

## 4. UX成果への統合

全36要求を横断比較し、この要求から生じる成果候補をCanonical UX成果へ接続した。同じIDへ接続する場合も、要求固有の成立条件を失わない。

| UX成果候補 | 処置・接続先 | 判断理由とこの要求が補う内容 |
|---|---|---|
| 試験層と現在の保証範囲の理解 | `New → UX-000057` | 「UT、IT、ST、UAT、RT等で何を確認し何が未確認か理解できる」を独立して変更・検証する成果である。REQ-000030では「試験層ごとの保証と未確認範囲を理解する」をGoalとし、外部境界を結合する各段階に単発成功や試験件数からLifecycle全体を保証することを防ぐため、既存成果への条件追加ではなくNewとして追跡する。 |
| 外部境界Lifecycleの段階検証 | `New → UX-000058` | 「最終E2E前に開始、利用、失敗、取消、回復、清掃の成立範囲を把握できる」を独立して変更・検証する成果である。REQ-000030では「試験層ごとの保証と未確認範囲を理解する」をGoalとし、外部境界を結合する各段階に単発成功や試験件数からLifecycle全体を保証することを防ぐため、既存成果への条件追加ではなくNewとして追跡する。 |
| 高負荷試験の明示的な実行選択 | `New → UX-000059` | 「PT／LTの時間・費用影響を理解し、実行有無を明示的に選べる」を独立して変更・検証する成果である。REQ-000030では「試験層ごとの保証と未確認範囲を理解する」をGoalとし、外部境界を結合する各段階に単発成功や試験件数からLifecycle全体を保証することを防ぐため、既存成果への条件追加ではなくNewとして追跡する。 |

## 5. 重要な体験

```text
変更の検証計画を作る時
        ↓
UTからRTまでの対象と回帰選択を組み立てる
        │
        ├─ ★ Critical: 外部境界を結合する各段階
        ├─ ⚠ Failure:  単発成功や試験件数からLifecycle全体を保証する
        └─ ✓ Quality:  開始から清掃まで段階的に反証する
        ↓
最終E2E前に外部境界の問題を局所化できる
```

### JourneyとSupporting Model

要求固有の区間は上のFlowが所有し、長い時間軸や複数主体との関係は次の既存成果物へ接続する。統合図をこの文書へ複製しない。

| Supporting Model | 処置 | 理由・参照先 |
|---|---|---|
| Experience Change | `作成` | 本文冒頭で、この要求が変える利用前後の仕事・認知・判断を示した。 |
| Experience Flow／Journey | `既存参照` | 本要求が関わる時間軸は[CRDD標準を変更・公開するJourney](../../03_Experience_Map.md#標準を変更検証公開する)を参照し、要求固有の区間は上表で示す。 |
| Service Blueprint | `非該当` | 本要求では複数主体の協調そのものを新しい体験成立条件にせず、必要な責任境界は次節で示す。 |
| User／Task Flow／Storyboard | `非該当` | 具体的な操作、画面遷移または利用環境の描写はIA／UIで具体化し、この要求分析では先取りしない。 |

### 責任境界

| 担い手 | この要求で担うこと | 越えてはならない境界 |
|---|---|---|
| 利用者（実装者・品質確認者） | 試験層ごとの保証と未確認範囲を理解するために、提示された状態と根拠から次の行動を判断する | 不足情報や内部状態を推測で補うことを要求されない |
| 提供System／AI | UTからRTまでの対象と回帰選択を組み立てるための状態、根拠および選択肢を示す | 単発成功や試験件数からLifecycle全体を保証する状態を成功・完了として表示しない |
| 運用・確認者 | 「開始から清掃まで段階的に反証する」ことと、最終E2E前に外部境界の問題を局所化できる状態へ到達できることを反証する | 未確認範囲や人間の判断を便宜的に上書きしない |

### 重要場面・失敗・品質期待の対応

| 重要場面 | 避ける失敗 | 品質期待 |
|---|---|---|
| 外部境界を結合する各段階 | 単発成功や試験件数からLifecycle全体を保証する | 開始から清掃まで段階的に反証する |

要求から得た追加の品質期待は次のとおりである。主Flowの品質を置き換えず、下流で具体化する観測点として保持する。

| 重要場面 | 避ける失敗 | 品質期待 |
|---|---|---|
| 結果または状態を最初に受け取る時 | UT・IT・E2Eを区別せず繰り返し、失敗原因と残る不確実性が分からない | 試験件数だけで品質を表示しない。 |
| 結果を判断または引き継ぐ時 | 必要条件を満たしていないのに完了・正常と理解する | 外部境界は開始から清掃・回復まで確認する。 |
| 失敗・不足から次の行動を選ぶ時 | 成立不能の理由や回復先が分からないまま作業が止まる | PT／LTを明示指示なしに実行しない。 |

## 6. 下流への引き渡し

```text
このREQで確定した利用者成果
        │
        ├─→ IA: 必要な情報・関係・見つけ方
        ├─→ UI: 誤認させない表示・操作意図
        ├─→ SPEC: 振る舞い・失敗・体験品質の条件
        └─→ Verification: 仮説を破る反例と観測
```

### 妥当性確認と未確認事項

| 確認する仮説 | 観測方法 | 現在未確認の範囲 |
|---|---|---|
| どの成立条件をどの試験で確認し、何が未確認か理解して次の検証を選べることで、UT・IT・E2Eを区別せず繰り返し、失敗原因と残る不確実性が分からないという負担または誤認を減らせる。 | 代表シナリオの利用者確認、UX専門Review、および品質期待を破る反例による下流検証 | 役割ごとの利用頻度、許容待ち時間・操作負担、用語理解および支援技術差 |

### 工程別の引き渡し

| 引き渡し先 | 具体化する義務 |
|---|---|
| IA以降 | Qualityは試験層と再実行選択を、ArchitectureはBlockとLifecycleを、VerificationはEvidence状態を具体化する。 |

Discoveryへ戻す条件は、想定した利用者、問題または「試験層ごとの保証と未確認範囲を理解する」という必要性が誤っていると分かった場合である。下流は実現方式を具体化してよいが、「最終E2E前に外部境界の問題を局所化できる」という成果を無断で弱めない。
