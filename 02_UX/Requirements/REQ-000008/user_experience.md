# REQ-000008の利用者体験分析

状態: UX意味再分析済み・独立レビュー待ち
要求: `REQ-000008` CROSなしで成立するRepository作業
探索元: [Repository単独作業](../../../01_Discovery/Explorations/EXP-000019_Repository_Local_Work/exploration.md)

## 1. REQの一次分析

```text
横断機能の設定有無が作業開始を左右する／別Repositoryの存在を意識してContextを選ぶ
        ↓
解決策を先に固定せず、人の仕事として読み替える
        ↓
UXとして必要
現在Repositoryだけで日常作業を完結する
```

この要求で解くのは機能の有無だけではない。Developerが「必要な時だけCROSへ移り普段の作業を複雑にしない」状態へ進めないことを問題として扱う。具体的な画面、データ構造、API、Componentまたは数値閾値はここで確定しない。

| 観点 | 内容 |
|---|---|
| 解決する問題 | 横断機能の設定有無が作業開始を左右する／別Repositoryの存在を意識してContextを選ぶ |
| UX Need | 現在Repositoryだけで日常作業を完結する |
| 対象範囲 | 単一Repositoryで作業するDeveloperが、「横断機能の設定有無が作業開始を左右する」状態から「対象Repositoryだけで開発を開始・完了できる」状態へ移る場面 |
| 対象外 | 具体的な画面、データ構造、実装方式および数値閾値の確定 |

## 2. 利用者・目標・成果

```text
Developer
        │
        │ wants to
        ▼
現在Repositoryだけで日常作業を完結する
        │
        │ so that
        ▼
必要な時だけCROSへ移り普段の作業を複雑にしない
```

| 項目 | 内容 |
|---|---|
| Primary Persona | [Product横断の利用者像](../../02_Personas.md)の「Developer」 |
| 利用場面 | Repositoryで作業を始める時 |
| Goal | 現在Repositoryだけで日常作業を完結する |
| Outcome | 必要な時だけCROSへ移り普段の作業を複雑にしない |
| REQ固有の差 | Local Contextで開始し不足時だけ横断を選ぶことが、この要求固有の成立条件になる |
| 根拠・確信度 | 探索元の課題と採用要求から導いた設計上の想定。個人属性、利用頻度および許容負担は未実測。 |

## 3. 利用者に起きる変化

```text
Before
────────────────
- 横断機能の設定有無が作業開始を左右する
- 別Repositoryの存在を意識してContextを選ぶ
        │
        │ CROSなしで成立するRepository作業が変える体験
        ▼
After
────────────────
- 対象Repositoryだけで開発を開始・完了できる
- 必要になった時だけProject横断入口へ進む
```

横断機能が便利でも、一つのRepositoryで完結する日常作業へServer、WorkspaceまたはFederationの理解を要求すると、基礎体験が悪化する。

この変化で守るのは操作手順ではない。Developerが現在Repositoryだけで日常作業を完結することで、必要な時だけCROSへ移り普段の作業を複雑にしないようになることを守る。

## 4. UX成果への統合

全36要求を横断比較し、この要求から生じる成果候補をCanonical UX成果へ接続した。同じIDへ接続する場合も、要求固有の成立条件を失わない。

| UX成果候補 | 処置・接続先 | 判断理由とこの要求が補う内容 |
|---|---|---|
| Repository単独利用 | `New → UX-000001` | 「Developerが現在Repositoryだけで日常作業を開始・完結でき、必要時だけ横断利用へ進める」を独立して変更・検証する成果である。REQ-000008では「現在Repositoryだけで日常作業を完結する」をGoalとし、横断利用へ切り替える判断にCROS未設定でLocal作業まで止まることを防ぐため、既存成果への条件追加ではなくNewとして追跡する。 |
| 必要時だけの横断移行 | `New → UX-000028` | 「Localで不足する理由を理解した時だけCROSへ進める」を独立して変更・検証する成果である。REQ-000008では「現在Repositoryだけで日常作業を完結する」をGoalとし、横断利用へ切り替える判断にCROS未設定でLocal作業まで止まることを防ぐため、既存成果への条件追加ではなくNewとして追跡する。 |
| Version Control非依存の日常作業 | `New → UX-000068` | 「未Commit状態や差し替え可能な履歴実装でも、通常の読取り・編集を続けられる」を独立して変更・検証する成果である。REQ-000008では「現在Repositoryだけで日常作業を完結する」をGoalとし、横断利用へ切り替える判断にCROS未設定でLocal作業まで止まることを防ぐため、既存成果への条件追加ではなくNewとして追跡する。 |

## 5. 重要な体験

```text
Repositoryで作業を始める時
        ↓
Local Contextで開始し不足時だけ横断を選ぶ
        │
        ├─ ★ Critical: 横断利用へ切り替える判断
        ├─ ⚠ Failure:  CROS未設定でLocal作業まで止まる
        └─ ✓ Quality:  Localを既定にし横断を任意に保つ
        ↓
必要な時だけCROSへ移り普段の作業を複雑にしない
```

### JourneyとSupporting Model

要求固有の区間は上のFlowが所有し、長い時間軸や複数主体との関係は次の既存成果物へ接続する。統合図をこの文書へ複製しない。

| Supporting Model | 処置 | 理由・参照先 |
|---|---|---|
| Experience Change | `作成` | 本文冒頭で、この要求が変える利用前後の仕事・認知・判断を示した。 |
| Experience Flow／Journey | `既存参照` | 本要求が関わる時間軸は[DeveloperのLocal Journey](../../03_Experience_Map.md#repositoryで日常作業を進める)を参照し、要求固有の区間は上表で示す。 |
| Service Blueprint | `非該当` | 本要求では複数主体の協調そのものを新しい体験成立条件にせず、必要な責任境界は次節で示す。 |
| User／Task Flow／Storyboard | `非該当` | 具体的な操作、画面遷移または利用環境の描写はIA／UIで具体化し、この要求分析では先取りしない。 |

### 責任境界

| 担い手 | この要求で担うこと | 越えてはならない境界 |
|---|---|---|
| 利用者（Developer） | 現在Repositoryだけで日常作業を完結するために、提示された状態と根拠から次の行動を判断する | 不足情報や内部状態を推測で補うことを要求されない |
| 提供System／AI | Local Contextで開始し不足時だけ横断を選ぶための状態、根拠および選択肢を示す | CROS未設定でLocal作業まで止まる状態を成功・完了として表示しない |
| 運用・確認者 | 「Localを既定にし横断を任意に保つ」ことと、必要な時だけCROSへ移り普段の作業を複雑にしない状態へ到達できることを反証する | 未確認範囲や人間の判断を便宜的に上書きしない |

### 重要場面・失敗・品質期待の対応

| 重要場面 | 避ける失敗 | 品質期待 |
|---|---|---|
| 横断利用へ切り替える判断 | CROS未設定でLocal作業まで止まる | Localを既定にし横断を任意に保つ |

要求から得た追加の品質期待は次のとおりである。主Flowの品質を置き換えず、下流で具体化する観測点として保持する。

| 重要場面 | 避ける失敗 | 品質期待 |
|---|---|---|
| 結果または状態を最初に受け取る時 | 横断機能の設定有無が作業開始を左右する | CROS未設定でも通常のRepository作業が成立する。 |
| 結果を判断または引き継ぐ時 | 必要条件を満たしていないのに完了・正常と理解する | AIが同じProject IDの兄弟Repositoryを推測探索しない。 |
| 結果を判断または引き継ぐ時 | 必要条件を満たしていないのに完了・正常と理解する | 横断Contextが必要になった理由と切替後に増える範囲が分かる。 |
| 失敗・不足から次の行動を選ぶ時 | 成立不能の理由や回復先が分からないまま作業が止まる | Remote都合のCredentialやWorkspaceをLocal作業へ要求しない。 |

## 6. 下流への引き渡し

```text
このREQで確定した利用者成果
        │
        ├─→ IA: 必要な情報・関係・見つけ方
        ├─→ UI: 誤認させない表示・操作意図
        ├─→ SPEC: 振る舞い・失敗・体験品質の条件
        └─→ Verification: 仮説を破る反例と観測
```

### 妥当性確認と未確認事項

| 確認する仮説 | 観測方法 | 現在未確認の範囲 |
|---|---|---|
| 対象Repositoryだけで開発を開始・完了できる／必要になった時だけProject横断入口へ進むことで、横断機能の設定有無が作業開始を左右するという負担または誤認を減らせる。 | 代表シナリオの利用者確認、UX専門Review、および品質期待を破る反例による下流検証 | 役割ごとの利用頻度、許容待ち時間・操作負担、用語理解および支援技術差 |

### 工程別の引き渡し

| 引き渡し先 | 具体化する義務 |
|---|---|
| IA以降 | IAはLocal Sourceを既定とし、横断Sourceを別状態にする。ArchitectureはLocal経路をCROS Serverの可用性へ依存させず、回帰試験はCROSなしの開始と完了を守る。 |

Discoveryへ戻す条件は、想定した利用者、問題または「現在Repositoryだけで日常作業を完結する」という必要性が誤っていると分かった場合である。下流は実現方式を具体化してよいが、「必要な時だけCROSへ移り普段の作業を複雑にしない」という成果を無断で弱めない。
