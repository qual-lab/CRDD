# REQ-000020の利用者体験分析

成果物種別: UX分析

状態: UX再統合済み・独立再レビュー待ち
分析対象: [REQ-000020 欠測・競合を保つリポジトリ横断統合](../../../01_Discovery/Definitions/REQ-000020/requirement.md)

## 1. 要求の一次分析

```text
リポジトリごとの状態を手で合成し、不足を見落とす／最新値らしい一つを選んでしまう
        ↓
解決策を先に固定せず、人の仕事として読み替える
        ↓
利用者に必要なこと
複数リポジトリを不完全性付きで一つのプロジェクトとして見る
```

この要求で解くのは機能の有無だけではない。プロジェクト運営者／CROS利用者が「欠測・制限・競合を保ったまま横断判断できる」状態へ進めないことを問題として扱う。具体的な画面、データ構造、API、構成要素または数値閾値はここで確定しない。

| 観点 | 内容 |
|---|---|
| 解決する問題 | リポジトリごとの状態を手で合成し、不足を見落とす／最新値らしい一つを選んでしまう |
| 利用者に必要なこと | 複数リポジトリを不完全性付きで一つのプロジェクトとして見る |
| 対象範囲 | 複数リポジトリを横断するプロジェクト運営者が、「リポジトリごとの状態を手で合成し、不足を見落とす」状態から「一つのプロジェクト全体の表示で、取得済み・欠測・競合を同時に確認する」状態へ移る場面 |
| 対象外 | 具体的な画面、データ構造、実装方式および数値閾値の確定 |

## 2. 利用者・目標・成果

```text
プロジェクト運営者／CROS利用者
        │
        │ 望んでいる
        ▼
複数リポジトリを不完全性付きで一つのプロジェクトとして見る
        │
        │ その結果として
        ▼
欠測・制限・競合を保ったまま横断判断できる
```

| 項目 | 内容 |
|---|---|
| 主な想定利用者 | [製品全体の利用者像](../../02_Personas.md)の「プロジェクト運営者／PM」 |
| 利用場面 | Federated プロジェクトを開く時 |
| 目的 | 複数リポジトリを不完全性付きで一つのプロジェクトとして見る |
| 得られる結果 | 欠測・制限・競合を保ったまま横断判断できる |
| この要求に固有の差 | 各リポジトリの識別情報・情報源状態・網羅範囲を解決することが、この要求固有の成立条件になる |
| 根拠・確信度 | 探索元の課題と採用要求から導いた仮説。プロジェクト運営者／PMが「複数リポジトリを不完全性付きで一つのプロジェクトとして見る」を判断する場面で、各リポジトリの識別情報・情報源状態・網羅範囲を解決することが実際に成果へ影響するかは未確認。 |

## 3. 利用者に起きる変化

```text
変更前
────────────────
- リポジトリごとの状態を手で合成し、不足を見落とす
- 最新値らしい一つを選んでしまう
        │
        │ 欠測・競合を保つリポジトリ横断統合が変える体験
        ▼
変更後
────────────────
- 一つのプロジェクト全体の表示で、取得済み・欠測・競合を同時に確認する
- 情報源ごとの値と統合不能理由を理解する
```

複数リポジトリを束ねると、一部の取得失敗や値の競合を一つの正常値へ丸めたくなる。しかし利用者が必要なのは、見栄えのよい統合結果より、判断に使える正確な全体像である。

この変化で守るのは操作手順ではない。プロジェクト運営者／CROS利用者が複数リポジトリを不完全性付きで一つのプロジェクトとして見ることで、欠測・制限・競合を保ったまま横断判断できるようになることを守る。

## 4. 利用者成果への統合

```text
REQ-000020
   │
   ├─ Same → UX-000009 プロジェクトの現在地を根拠と不完全性付きで理解する
   └─ Same → UX-000011 プロジェクト・リポジトリ・基点フォルダを区別して対象を選ぶ
```

| 利用者成果 | 処置 | 判断理由 | この要求が補う内容 |
|---|---|---|---|
| プロジェクトの現在地を根拠と不完全性付きで理解する | `Same → UX-000009` | 既存UXの利用者: プロジェクト運営者／PM<br>現在要求の利用者: プロジェクト運営者／PM<br>利用者の差: 既存UXと現在要求はいずれも「プロジェクト運営者／PM」であり、利用者の違いはない<br>既存UXの利用のきっかけ: プロジェクト状況を確認する時<br>現在要求の利用のきっかけ: Federated プロジェクトを開く時<br>利用のきっかけの差: 既存UXの「プロジェクト状況を確認する時」に対して現在要求は「Federated プロジェクトを開く時」を具体化するが、同じ「プロジェクトの現在地を根拠と不完全性付きで理解する」が必要になる開始条件の差であり、独立した成果境界ではない<br>既存UXで得られる結果: 物理構成を意識せずプロジェクトの現在地を理解し、欠測・制限・競合・古さと情報源へ戻れる<br>現在要求で得られる結果: 横断統合の欠測・制限・競合を完全な状態へ畳まず、情報源へ戻りながらプロジェクトの現在地を理解できる<br>得られる結果の差: 現在要求は「プロジェクトの現在地を根拠と不完全性付きで理解する」をこの要求の場面で成立させる得られる結果を具体化しており、要求全体の中心となる利用者成果や別のUX成果へ置き換えていない<br>既存UXでの失敗: 欠測・制限・競合・古さを完全なプロジェクト現在値として信じる<br>現在要求での失敗: 読めないリポジトリを推測補完し、Federated プロジェクトを完全な状態として表示する<br>失敗の差: 複数リポジトリ化で欠測原因が増えるが、不完全な現在地を完全と誤認する失敗は同じである<br>同じ利用者成果へ統合できる理由: 横断統合の網羅範囲は同じプロジェクト理解成果に必要な情報・品質条件である | 各リポジトリの識別情報・情報源状態・網羅範囲を解決することが、この要求固有の成立条件になる |
| プロジェクト・リポジトリ・基点フォルダを区別して対象を選ぶ | `Same → UX-000011` | 既存UXの利用者: プロジェクト運営者／PM<br>現在要求の利用者: プロジェクト運営者／PM<br>利用者の差: 既存UXと現在要求はいずれも「プロジェクト運営者／PM」であり、利用者の違いはない<br>既存UXの利用のきっかけ: 参照または操作対象を選ぶ時<br>現在要求の利用のきっかけ: Federated プロジェクトを開く時<br>利用のきっかけの差: 既存UXの「参照または操作対象を選ぶ時」に対して現在要求は「Federated プロジェクトを開く時」を具体化するが、同じ「プロジェクト・リポジトリ・基点フォルダを区別して対象を選ぶ」が必要になる開始条件の差であり、独立した成果境界ではない<br>既存UXで得られる結果: 論理プロジェクトを一つに見ながら、参照・実行・回復の対象リポジトリと基点フォルダを取り違えずに選べる<br>現在要求で得られる結果: Federated 表示から参照・実行・回復の対象リポジトリ／基点フォルダを特定し、別対象へ作用せず選べる<br>得られる結果の差: 現在要求は「プロジェクト・リポジトリ・基点フォルダを区別して対象を選ぶ」をこの要求の場面で成立させる得られる結果を具体化しており、要求全体の中心となる利用者成果や別のUX成果へ置き換えていない<br>既存UXでの失敗: 論理プロジェクトと作用対象リポジトリ／基点フォルダを混同し、別の場所へ参照・実行・回復を行う<br>現在要求での失敗: 複数リポジトリを束ねた表示から、どのリポジトリが情報源または作用対象か見失う<br>失敗の差: 横断統合時の選択場面が加わるが、プロジェクトと物理対象を取り違える失敗は同じである<br>同じ利用者成果へ統合できる理由: リポジトリ Bindingは同じ対象選択成果を横断構成で成立させる条件である | 各リポジトリの識別情報・情報源状態・網羅範囲を解決することが、この要求固有の成立条件になる |

`Same`／`New`は技術用語の近さや件数目標では決めない。「利用者は、どの状況で、何をするためにシステムと関わり、何ができるようになるか」が同じかを比較する。提供能力、必要情報、品質、妥当性確認または下流の実現要素は、独立UXへ分割せず対応する成果の成立条件として保持する。

## 5. 重要な体験

### この要求での利用の流れ

```text
Federated プロジェクトを開く時
        ↓
各リポジトリの識別情報・情報源状態・網羅範囲を解決する
        │
        ├─ ★ 重要場面: 複数の情報源を統合する場面
        ├─ ⚠ 失敗:  読めない情報源を推測補完し完全表示する
        └─ ✓ 守る品質:  partial・restricted・stale・conflictingを保持する
        ↓
欠測・制限・競合を保ったまま横断判断できる
```

### サービス提供の流れの処置

処置: `作成`

```text
【利用者・責任者】プロジェクト運営者／PM
        │ 利用者行動: 網羅範囲を確認して横断判断または掘り下げを選ぶ
        ▼
【利用者接点】Federated プロジェクトを開く時
        │
        ├─ 時間差: リポジトリごとの観測後に横断統合を合成するため、情報源間に時点差がある
        ├─ 完了時: リポジトリ別情報源状態、網羅範囲、欠測・制限・競合
        └─ 失敗時: 読めないリポジトリと競合項目を各正本責任者へ返す
                     │
                     ▼
             【回復・判断する人】リポジトリごとの正本責任者
                     │ 返却された事実と判断不能範囲を確認
                     └─ 次の行動: 部分表示で判断するか、情報源解決を依頼する

---------------- 可視境界 ----------------
                     │ 時間関係: リポジトリごとの観測後に横断統合を合成するため、情報源間に時点差がある
                     ▼
【提供側】提供システム
        └─ 提供責務: 利用可能な情報源だけを統合し不完全性を保持する
```

この図は、利用者行動、利用者が観測する接点、提供責務および失敗時の引き渡しを示す。内部構成要素の構造やプロトコルは下流工程で具体化する。

### 製品全体の整理への接続

- 利用の流れの統合先: [プロジェクトの現在地を判断する](../../03_Experience_Map.md#プロジェクトの現在地を判断する)
- サービス提供の流れの統合先: [共同のサービス提供の流れ](../../04_Service_Blueprint.md#1-共同のサービス提供の流れ)
- 横断成果物はこの個別分析から共通パターンを合成する。この要求での利用の流れ、責任境界または品質の代替にはしない。

### この要求での責任境界

| 担い手 | この要求で担うこと | 越えてはならない境界 |
|---|---|---|
| プロジェクト運営者／PM | 網羅範囲を確認して横断判断または掘り下げを選ぶ | 読めない情報を推測で補わない |
| リポジトリごとの正本責任者 | 担当情報と現在の開示可否を提供する | 他リポジトリの状態を複製して正本化しない |
| 提供システム | 利用可能な情報源だけを統合し不完全性を保持する | 非開示情報源の存在や内容を漏らさない |

### 補足する品質

- 結果または状態を最初に受け取る時: 情報源ごとの取得状態と観測時点を保持する。（避ける失敗: リポジトリごとの状態を手で合成し、不足を見落とす）
- 結果を判断または引き継ぐ時: `missing`と`restricted`と`unavailable`を混同しない。（避ける失敗: 必要条件を満たしていないのに完了・正常と理解する）
- 結果を判断または引き継ぐ時: 競合値をAIが勝手に一つへ統合しない。（避ける失敗: 必要条件を満たしていないのに完了・正常と理解する）
- 失敗・不足から次の行動を選ぶ時: 一部だけの結果でも分かる範囲と判断できない範囲を示す。（避ける失敗: 成立不能の理由や回復先が分からないまま作業が止まる）


## 6. 下流への引き渡し

```text
この要求で確定した利用者成果
        │
        ├─→ IA: 必要な情報・関係・見つけ方
        ├─→ UI: 誤認させない表示・操作意図
        ├─→ SPEC: 振る舞い・失敗・体験品質の条件
        └─→ Verification: 仮説を破る反例と観測
```

### 妥当性確認と未確認事項

| 確認する仮説 | 観測方法 | 現在未確認の範囲 |
|---|---|---|
| 一つのプロジェクト全体の表示で、取得済み・欠測・競合を同時に確認する／情報源ごとの値と統合不能理由を理解することで、リポジトリごとの状態を手で合成し、不足を見落とすという負担または誤認を減らせる。 | 代表シナリオの利用者確認、利用者体験の専門レビュー、および品質期待を破る反例による下流検証 | プロジェクト運営者／PMが「複数リポジトリを不完全性付きで一つのプロジェクトとして見る」を行う際の判断基準、許容負担、利用環境および失敗後の選択 |

### 工程別の引き渡し

| 引き渡し先 | 具体化する義務 |
|---|---|
| 下流工程 | IAは項目単位のProvenanceと網羅範囲を持つ。SPECは統合決定表と情報の新しさ条件を定め、Architectureは情報源との接続部の失敗をプロジェクト全体成功へ隠さない。 |

Discoveryへ戻す条件は、想定した利用者、問題または「複数リポジトリを不完全性付きで一つのプロジェクトとして見る」という必要性が誤っていると分かった場合である。下流は実現方式を具体化してよいが、「欠測・制限・競合を保ったまま横断判断できる」という成果を無断で弱めない。
