# REQ-000006の利用者体験分析

成果物種別: UX Analysis

状態: UX再統合済み・独立再レビュー待ち
要求: `REQ-000006` Local MCP Transport間の意味統一
探索元: [EXP-000015](../../../01_Discovery/Analysis/EXP-000015/exploration.md)

## 1. REQの一次分析

```text
Transportごとに結果、状態、エラー、許可の意味が変わる
        ↓
解決策を先に固定せず、人の仕事として読み替える
        ↓
UXとして必要
stdioとHTTPで同じ意味の操作を行う
```

この要求で解くのは機能の有無だけではない。MCP／Tool利用者・統合者が「Transportを変えても結果を読み替えなくてよい」状態へ進めないことを問題として扱う。具体的な画面、データ構造、API、Componentまたは数値閾値はここで確定しない。

| 観点 | 内容 |
|---|---|
| 解決する問題 | Transportごとに結果、状態、エラー、許可の意味が変わる |
| UX Need | stdioとHTTPで同じ意味の操作を行う |
| 対象範囲 | Local MCPをstdioまたはHTTPで使う人・Toolが、「Transportごとに結果、状態、エラー、許可の意味が変わる」状態から「利用する接続方法を変えても同じ依頼が同じ意味の結果と停止理由を返す」状態へ移る場面 |
| 対象外 | 具体的な画面、データ構造、実装方式および数値閾値の確定 |

## 2. 利用者・目標・成果

```text
MCP／Tool利用者・統合者
        │
        │ wants to
        ▼
stdioとHTTPで同じ意味の操作を行う
        │
        │ so that
        ▼
Transportを変えても結果を読み替えなくてよい
```

| 項目 | 内容 |
|---|---|
| Primary Persona | [Product横断の利用者像](../../02_Personas.md)の「Developer」 |
| 利用場面 | 利用する接続方式を選ぶ時 |
| Goal | stdioとHTTPで同じ意味の操作を行う |
| Outcome | Transportを変えても結果を読み替えなくてよい |
| REQ固有の差 | 同じ公開契約へ要求を渡すことが、この要求固有の成立条件になる |
| 根拠・確信度 | 探索元の課題と採用要求から導いた仮説。Developerが「stdioとHTTPで同じ意味の操作を行う」を判断する場面で、同じ公開契約へ要求を渡すことが実際に成果へ影響するかは未確認。 |

## 3. 利用者に起きる変化

```text
Before
────────────────
- Transportごとに結果、状態、エラー、許可の意味が変わる
        │
        │ Local MCP Transport間の意味統一が変える体験
        ▼
After
────────────────
- 利用する接続方法を変えても同じ依頼が同じ意味の結果と停止理由を返す
```

Local MCP Transport間の意味統一は、単に内部方式を成立させる要求ではない。「Transportごとに結果、状態、エラー、許可の意味が変わる」状態から、「利用する接続方法を変えても同じ依頼が同じ意味の結果と停止理由を返す」状態へ変わり、利用者が次の判断へ進めることまでを体験として扱う。

この変化で守るのは操作手順ではない。MCP／Tool利用者・統合者がstdioとHTTPで同じ意味の操作を行うことで、Transportを変えても結果を読み替えなくてよいようになることを守る。

## 4. UX成果への統合

```text
REQ-000006
   │
   ├─ Same → UX-000008 故障した境界と影響範囲を理解する
   └─ New  → UX-000012 入口を変えても同じ仕事を続ける
```

| UX成果 | 処置 | 判断理由 | この要求が補う内容 |
|---|---|---|---|
| 故障した境界と影響範囲を理解する | `Same → UX-000008` | 既存UXのActor: Runtime導入・運用者<br>現在REQのActor: Developer<br>Actor差: 既存UXの「Runtime導入・運用者」に対して現在REQは「Developer」だが、両者とも「故障した境界と影響範囲を理解する」を利用・確認する当事者であり、役割差だけでは別Outcomeにならない<br>既存UXのTrigger: Runtime Componentを置換する時<br>現在REQのTrigger: 利用する接続方式を選ぶ時<br>Trigger差: 既存UXの「Runtime Componentを置換する時」に対して現在REQは「利用する接続方式を選ぶ時」を具体化するが、同じ「故障した境界と影響範囲を理解する」が必要になる開始条件の差であり、独立した成果境界ではない<br>既存UXのOutcome: 接続・認証・実行・結果搬送またはProvider境界のどこで止まり、何が利用可能かを理解できる<br>現在REQのOutcome: stdioまたはHTTPの一方が失敗した時、接続・処理・結果搬送のどこで止まり、残る利用可能範囲を理解できる<br>Outcome差: 現在REQは「故障した境界と影響範囲を理解する」をこの要求の場面で成立させるOutcomeを具体化しており、REQ全体のPrimary Outcomeや別のUX成果へ置き換えていない<br>既存UXのFailure: 故障した入口・実行・結果搬送の境界が分からず、利用可能な範囲まで全体故障とみなす<br>現在REQのFailure: stdioとHTTPの一方で意味または結果が崩れた時、接続失敗と処理失敗を区別できない<br>Failure差: Transport固有の発生点が加わるが、利用者が故障箇所と影響範囲を理解できない失敗は同じである<br>同一Outcomeへ統合できる理由: Transport差は故障範囲理解に必要な診断条件であり、独立Outcomeではない | 同じ公開契約へ要求を渡すことが、この要求固有の成立条件になる |
| 入口を変えても同じ仕事を続ける | `New → UX-000012` | 本要求が「Workbench、MCP、CLIまたはAIの入口を変えても、同じ入力・権限・状態・結果を用いて仕事を続けられる」という独立した利用者成果を最初に定義する。 | 同じ公開契約へ要求を渡すことが、この要求固有の成立条件になる |

Same／Newは技術用語の近さや件数目標では決めない。「利用者は、どの状況で、何をするためにSystemと関わり、何ができるようになるか」が同じかを比較する。Capability、Information、Quality、Validationまたは下流の実現要素は、独立UXへ分割せず対応する成果の成立条件として保持する。

## 5. 重要な体験

### このREQのJourney

```text
利用する接続方式を選ぶ時
        ↓
同じ公開契約へ要求を渡す
        │
        ├─ ★ Critical: Transport境界を越えた意味同値
        ├─ ⚠ Failure:  接続方式ごとに状態や結果が変わる
        └─ ✓ Quality:  入力・認可・結果の意味を共通化する
        ↓
Transportを変えても結果を読み替えなくてよい
```

### Service Blueprintの処置

処置: `作成`

```text
[U: Developer]
        │ 利用者行動: 利用環境に合う接続方式を選び、同じ意図の仕事を依頼する
        ▼
[T: 利用する接続方式を選ぶ時]
        │
        ├─ 時間差: 各Transportの要求と応答は一回の利用内で比較し、長期の回復状態は持たない
        ├─ 完了時: 接続・処理・結果搬送の各成否と同じ意味の結果
        └─ 失敗時: 意味差が生じたTransportと影響する操作だけを運用・確認者へ返す
                     │
                     ▼
             [R: 運用・確認者]
                     │ 返却された事実と判断不能範囲を確認
                     └─ 次の行動: 正常な入口を選ぶか、不一致を修正対象として止める

---------------- 可視境界 ----------------
                     │ 時間関係: 各Transportの要求と応答は一回の利用内で比較し、長期の回復状態は持たない
                     ▼
[S: 提供System]
        └─ 提供責務: 接続方式にかかわらず同じ意味の入力・権限・結果を保つ
```

この図は、利用者行動、利用者が観測する接点、提供責務および失敗時の引き渡しを示す。内部Componentの構造やProtocolは下流工程で具体化する。

### 横断Synthesisへの接続

- Journeyの横断統合先: [Repositoryで日常作業を進める](../../03_Experience_Map.md#repositoryで日常作業を進める)
- Service Blueprintの横断統合先: [共同Service Blueprint](../../04_Service_Blueprint.md#1-共同service-blueprint)
- 横断成果物はこの個別分析から共通パターンを合成する。このREQのJourney、責任境界または品質の代替にはしない。

### このREQでの責任境界

| 担い手 | この要求で担うこと | 越えてはならない境界 |
|---|---|---|
| Developer | 利用環境に合う接続方式を選び、同じ意図の仕事を依頼する | 接続方式ごとの内部契約を覚えることを要求されない |
| 提供System | 接続方式にかかわらず同じ意味の入力・権限・結果を保つ | Transport固有の都合を業務結果へ混入しない |
| 運用・確認者 | 両経路の成功・失敗・結果搬送が意味同値であることを確認する | 一方の単発成功から他方を推定しない |

### 補足する品質

- 結果または状態を最初に受け取る時: Transport差を業務状態として見せない。（避ける失敗: Transportごとに結果、状態、エラー、許可の意味が変わる）
- 結果を判断または引き継ぐ時: 同じ入力・権限なら同じ意味になる。（避ける失敗: 必要条件を満たしていないのに完了・正常と理解する）
- 失敗・不足から次の行動を選ぶ時: 接続失敗とRuntime失敗を区別できる。（避ける失敗: 成立不能の理由や回復先が分からないまま作業が止まる）


## 6. 下流への引き渡し

```text
このREQで確定した利用者成果
        │
        ├─→ IA: 必要な情報・関係・見つけ方
        ├─→ UI: 誤認させない表示・操作意図
        ├─→ SPEC: 振る舞い・失敗・体験品質の条件
        └─→ Verification: 仮説を破る反例と観測
```

### 妥当性確認と未確認事項

| 確認する仮説 | 観測方法 | 現在未確認の範囲 |
|---|---|---|
| 利用する接続方法を変えても同じ依頼が同じ意味の結果と停止理由を返すことで、Transportごとに結果、状態、エラー、許可の意味が変わるという負担または誤認を減らせる。 | 代表シナリオの利用者確認、UX専門Review、および品質期待を破る反例による下流検証 | Developerが「stdioとHTTPで同じ意味の操作を行う」を行う際の判断基準、許容負担、利用環境および失敗後の選択 |

### 工程別の引き渡し

| 引き渡し先 | 具体化する義務 |
|---|---|
| 下流工程 | SPECは公開結果とTransport errorを分け、ArchitectureとVerificationは両入口の意味同値性を保証する。 |

Discoveryへ戻す条件は、想定した利用者、問題または「stdioとHTTPで同じ意味の操作を行う」という必要性が誤っていると分かった場合である。下流は実現方式を具体化してよいが、「Transportを変えても結果を読み替えなくてよい」という成果を無断で弱めない。
